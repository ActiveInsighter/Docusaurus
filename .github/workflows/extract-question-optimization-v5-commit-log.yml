name: Extract question optimization v5 commit failure

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/extract-question-optimization-v5-commit-log.yml"

permissions:
  actions: read
  contents: write

jobs:
  extract:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Download job log tail
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
          JOB_ID: "89749537945"
        run: |
          gh api "repos/${GH_REPOSITORY}/actions/jobs/${JOB_ID}/logs" > /tmp/job.log
          tail -n 220 /tmp/job.log > .github/question-optimization-v5-commit-failure.log
      - name: Commit details
        run: |
          rm .github/workflows/extract-question-optimization-v5-commit-log.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: record question optimization v5 commit failure [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main
