---
sidebar_position: 1
title: 竟成 · 计算机组成原理 · 第1章 计算机系统概述
---

{/* 此文件由 scripts/convert-408-mock-exercises.mjs 自动生成，请勿直接修改。 */}

# 竟成 · 计算机组成原理 · 第1章 计算机系统概述

## 1.2 计算机系统层次结构

### 1.2.3 计算机软件

1. **【竟成·模拟二-12】** 下列软件中，不属于系统软件的是（）。

<div className="choice-options">
<div className="choice-option-row">A. 编译程序 &emsp;&emsp; B. 操作系统 &emsp;&emsp; C. 文本处理程序 &emsp;&emsp; D. 数据库管理系统</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 系统软件用于管理计算机资源、控制系统运行，或为其他软件提供基础运行环境。编译程序属于语言处理程序，操作系统负责管理软硬件资源，数据库管理系统负责数据库的组织与管理，三者都属于系统软件。

文本处理程序主要帮助用户完成文字录入、编辑和排版等具体任务，属于应用软件，因此选择 C。

- A：编译程序是系统软件，负责将高级语言源程序翻译为目标程序。
- B：操作系统是最基本的系统软件。
- C：文本处理程序面向具体应用需求，属于应用软件。
- D：数据库管理系统属于系统软件，为数据库应用提供基础服务。

</details>

---

2. **【竟成·模拟七-12】** 在高级语言源程序要执行时，将高级语言转换为机器语言，并且翻译一句执行一句，这样的程序为（）。

<div className="choice-options">
<div className="choice-option-row">A. 汇编程序 &emsp;&emsp; B. 链接程序 &emsp;&emsp; C. 编译程序 &emsp;&emsp; D. 解释程序</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 解释程序的工作特点是对高级语言源程序逐句进行翻译，并立即执行已经翻译的语句，即“翻译一句，执行一句”，通常不生成可独立保存的目标程序。因此选择 D。

- A：汇编程序将汇编语言源程序翻译成机器语言目标程序。
- B：链接程序将多个目标模块及所需库文件组合成可执行文件。
- C：编译程序通常一次性翻译整个高级语言源程序，生成目标程序后再执行。
- D：解释程序边翻译边执行，与题意一致。

</details>

---

### 1.2.6 计算机系统的工作原理

3. **【竟成·模拟四-12】** 下列关于计算机的说法正确的是（）。

<div className="choice-options">
<div className="choice-option-row">A. 计算机做的所有工作必须先编写成程序才能完成</div>
<div className="choice-option-row">B. 存储在存储器内部的指令和数据在形式上有所差别</div>
<div className="choice-option-row">C. CPU的时钟频率越高，计算机的运行速度越快</div>
<div className="choice-option-row">D. 计算机系统即为硬件系统</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 计算机采用存储程序工作方式，需要通过程序中的指令序列规定数据处理步骤。计算机能够完成的自动处理工作，都必须由程序加以描述并交由计算机执行，因此 A 正确。

- A：程序是计算机自动完成任务的指令集合，表述正确。
- B：指令和数据在存储器中都以二进制形式存放，在形式上没有本质差别。
- C：计算机性能不仅由主频决定，还与指令条数、平均 $\displaystyle CPI$、存储系统和体系结构等因素有关。
- D：完整的计算机系统由硬件系统和软件系统共同组成。

</details>

---

4. **【竟成·模拟五-12】** 下列属于冯·诺依曼结构计算机工作方式的基本特点的是（）。

<div className="choice-options">
<div className="choice-option-row">A. 指令按地址访问并顺序执行 &emsp;&emsp; B. 采用十进制方式存储和处理数据</div>
<div className="choice-option-row">C. 指令存储器和数据存储器分离 &emsp;&emsp; D. 访问存储器时按内容读取数据</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 冯·诺依曼结构采用“存储程序”工作方式，程序和数据预先存入存储器，CPU 按程序计数器给出的地址取指。除转移指令改变执行顺序外，指令通常按地址顺序依次执行，因此选择 A。

- A：体现了按地址取指和程序顺序执行的基本方式，正确。
- B：计算机内部采用二进制表示和处理信息，而不是十进制。
- C：冯·诺依曼结构的指令和数据存放在同一存储器中；指令存储器和数据存储器分离是哈佛结构的典型特征。
- D：普通主存按地址访问；按内容访问是相联存储器的特征。

</details>

---

## 1.3 计算机的性能指标

### 1.3.1 计算机的主要性能指标

5. **【竟成·模拟一-12】** 某机器M的主频为100MHz。程序P中最初没有左移2位指令，对P进行优化，将程序中所有的乘4指令都换成左移2位指令，得到优化后的程序P1。已知在M上执行程序P时访问乘4指令的Cache命中率为90%，命中后的CPI为94，程序执行时间为12s。执行程序P1时访问左移2位指令的Cache命中率为99%，命中后的CPI为3，程序执行时间为10s。当Cache未命中时都需要额外的100个时钟周期进行访存取指，则P中乘4指令被换成左移2位指令的数量是（）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle 1\times 10^6$ &emsp;&emsp; B. $\displaystyle 2\times 10^6$ &emsp;&emsp; C. $\displaystyle 3\times 10^6$ &emsp;&emsp; D. $\displaystyle 4\times 10^6$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 乘 4 指令的 Cache 未命中率为 $\displaystyle 10\%$，其平均 $\displaystyle CPI$ 为：

$$
\displaystyle
CPI_{乘4}=94+10\%\times100=104
$$

左移 2 位指令的 Cache 未命中率为 $\displaystyle 1\%$，其平均 $\displaystyle CPI$ 为：

$$
\displaystyle
CPI_{左移}=3+1\%\times100=4
$$

每替换一条指令可减少的时钟周期数为：

$$
\displaystyle
104-4=100
$$

优化前后程序执行时间减少：

$$
\displaystyle
12-10=2\mathrm{s}
$$

主频为 $\displaystyle 100\mathrm{MHz}$，对应减少的总时钟周期数为：

$$
\displaystyle
2\times100\times10^6=2\times10^8
$$

设被替换的乘 4 指令共有 $\displaystyle N$ 条，则：

$$
\displaystyle
100N=2\times10^8
$$

解得：

$$
\displaystyle
N=2\times10^6
$$

因此选择 B。

- A、C、D：均与优化所减少的总时钟周期数不符。

</details>

---

6. **【竟成·模拟三-12】** 某基准程序在主频为200MHz的处理器上运行，编译生成的指令类型及数目如下表所示，该基准程序的执行时间和有效MIPS分别是（）。

| 指令类 | CPI | 指令条数 |
| ------ | --- | -------- |
| A      | 1   | 4500     |
| B      | 3   | 200      |
| C      | 1   | 500      |
| D      | 2   | 800      |

<div className="choice-options">
<div className="choice-option-row">A. 8500ns、118 &emsp;&emsp; B. 170μs、235 &emsp;&emsp; C. 85μs、118 &emsp;&emsp; D. 17000ns、235</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** 题面无正确选项；按题面数据应为 $\displaystyle 36\mu\mathrm{s}$、约 $\displaystyle 166.7\mathrm{MIPS}$

**解析：** 程序执行的总指令条数为：

$$
\displaystyle
N=4500+200+500+800=6000
$$

总时钟周期数为各类指令的“指令条数乘以 $\displaystyle CPI$”之和：

$$
\displaystyle
\begin{aligned}
C
&=4500\times1+200\times3+500\times1+800\times2\\
&=4500+600+500+1600\\
&=7200
\end{aligned}
$$

主频为 $\displaystyle 200\mathrm{MHz}$，一个时钟周期为：

$$
\displaystyle
\dfrac{1}{200\times10^6}=5\mathrm{ns}
$$

因此程序执行时间为：

$$
\displaystyle
T=7200\times5\mathrm{ns}=36000\mathrm{ns}=36\mu\mathrm{s}
$$

有效 MIPS 为：

$$
\displaystyle
\begin{aligned}
\mathrm{MIPS}
&=\dfrac{6000}{36\times10^{-6}\times10^6}\\
&=\dfrac{6000}{36}\\
&\approx166.7
\end{aligned}
$$

所以依据题面表格，正确结果应为 $\displaystyle 36\mu\mathrm{s}$、约 $\displaystyle 166.7\mathrm{MIPS}$，四个选项均不匹配，题目数据或选项应存在排版错误。

- A、B、C、D：其执行时间均不等于 $\displaystyle 7200$ 个时钟周期在 $\displaystyle 200\mathrm{MHz}$ 主频下对应的 $\displaystyle 36\mu\mathrm{s}$，相应 MIPS 也与总指令条数 $\displaystyle 6000$ 不符。

</details>

---

7. **【竟成·模拟六-12】** 假定计算机$\displaystyle M_1$和$\displaystyle M_2$具有相同的指令集体系结构(ISA)，$\displaystyle M_1$的主频为800MHz，某基准程序P在$\displaystyle M_1$和$\displaystyle M_2$上运行分别需要12s和8s，$\displaystyle M_2$的平均CPI为$\displaystyle M_1$的1.5倍，则$\displaystyle M_2$的主频为（）。

<div className="choice-options">
<div className="choice-option-row">A. 800MHz &emsp;&emsp; B. 1.2GHz &emsp;&emsp; C. 1.5GHz &emsp;&emsp; D. 1.8GHz</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 两台机器具有相同的 ISA，对同一个基准程序执行的指令条数相同。设指令条数为 $\displaystyle N$，$\displaystyle M1$ 的平均 $\displaystyle CPI$ 为 $\displaystyle CPI_1$，则：

$$
\displaystyle
\begin{aligned}
T_1&=\dfrac{N\times CPI_1}{f_1}=12\mathrm{s},\\
T_2&=\dfrac{N\times1.5CPI_1}{f_2}=8\mathrm{s}.
\end{aligned}
$$

两式相除：

$$
\displaystyle
\dfrac{T_1}{T_2}
=\dfrac{CPI_1/f_1}{1.5CPI_1/f_2}
=\dfrac{f_2}{1.5f_1}
$$

代入 $\displaystyle T_1/T_2=12/8=1.5$、$\displaystyle f_1=800\mathrm{MHz}$：

$$
\displaystyle
1.5=\dfrac{f_2}{1.5\times800\mathrm{MHz}}
$$

因而：

$$
\displaystyle
f_2=1.5\times1.5\times800\mathrm{MHz}=1800\mathrm{MHz}=1.8\mathrm{GHz}
$$

故选择 D。

- A、B、C：均不足以抵消 $\displaystyle M2$ 平均 $\displaystyle CPI$ 增大到 $\displaystyle 1.5$ 倍的影响，同时实现运行时间由 $\displaystyle 12\mathrm{s}$ 缩短到 $\displaystyle 8\mathrm{s}$。

</details>
