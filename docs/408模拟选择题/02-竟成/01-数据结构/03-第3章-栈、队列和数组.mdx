---
sidebar_position: 3
title: 竟成 · 数据结构 · 第3章 栈、队列和数组
---

{/* 此文件由 scripts/convert-408-mock-exercises.mjs 自动生成，请勿直接修改。 */}

# 竟成 · 数据结构 · 第3章 栈、队列和数组

## 3.1 栈

### 3.1.1 栈的基本概念

1. **【竟成·模拟三-02】** 下列关于栈的说法错误的是（）。

<div className="choice-options">
<div className="choice-option-row">A. 可以使用两个队列实现一个后入先出的栈 &emsp;&emsp; B. 可以使用一个队列实现一个后入先出的栈</div>
<div className="choice-option-row">C. 可以使用两个栈实现先入先出队列 &emsp;&emsp; D. 可以使用一个栈实现先入先出队列</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 两个队列可以通过在入栈或出栈时倒换元素实现栈；一个队列也可以在入队后通过循环移动前面的元素，使新元素位于队头，从而模拟后进先出，因此 A、B 正确。两个栈可采用“输入栈 + 输出栈”的方式实现队列，C 正确。

在只允许使用一个普通栈及常数个辅助变量、且不借助递归调用或其他线性存储结构的标准模型下，无法长期保持先入先出的访问次序，因此 D 错误。若借助递归，递归调用栈实际上又提供了额外的栈空间，不能视为仅使用一个栈。

</details>

---

2. **【竟成·模拟四-02】** 若栈的输入序列是：1,2,3,···,n，输出序列是：$\displaystyle p_1$,$\displaystyle p_2$,···,$\displaystyle p_n$。若存在k>1，使得$\displaystyle p_k$=n。则当i>k时，$\displaystyle p_i$是（）。

<div className="choice-options">
<div className="choice-option-row">A. n-i+1 &emsp;&emsp; B. 不定 &emsp;&emsp; C. n-i+k &emsp;&emsp; D. n-i+k-1</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 当 $\displaystyle p_k=n$ 时，元素 $\displaystyle n$ 被弹出前，输入序列中的所有元素都已经入过栈。此后虽然不再有新元素入栈，但栈中剩余哪些元素，取决于前 $\displaystyle k-1$ 次出栈时已经弹出了哪些元素，因此后续各项的具体取值不能由 $\displaystyle n$、$\displaystyle i$、$\displaystyle k$ 唯一确定。

例如取 $\displaystyle n=6$、$\displaystyle k=3$：

```text
3,2,6,5,4,1
```

是合法序列；

```text
2,1,6,5,4,3
```

也是合法序列。两者均有 $\displaystyle p_3=6$，但后续元素并不完全相同。因此只能确定后续元素将按当时栈内的顺序依次弹出，不能确定统一公式，选择 B。

</details>

---

3. **【竟成·模拟五-02】** 一个栈的入栈序列为1,2,3,···,n，其出栈序列为$\displaystyle p_1$,$\displaystyle p_2$,$\displaystyle p_3$,···,$\displaystyle p_n$。若存在某个正整数k，使得$\displaystyle p_2$=k(k>3)，则$\displaystyle p_3$可能取值的个数是（）。

<div className="choice-options">
<div className="choice-option-row">A. n-k+1 &emsp;&emsp; B. n-2 &emsp;&emsp; C. n-1 &emsp;&emsp; D. n-k+2</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 要使第二个出栈元素为 $\displaystyle k$，在弹出 $\displaystyle k$ 前必须已将 1～$\displaystyle k$ 入栈，并且第一个出栈元素只能来自 1～$\displaystyle k-1$。分情况讨论第三个出栈元素：

1. 若第一个出栈元素不是 $\displaystyle k-1$，则弹出 $\displaystyle k$ 后栈顶仍为 $\displaystyle k-1$，所以 $\displaystyle p_3$ 可以是 $\displaystyle k-1$。
2. 若第一个出栈元素正好是 $\displaystyle k-1$，则弹出 $\displaystyle k$ 后栈顶为 $\displaystyle k-2$，所以 $\displaystyle p_3$ 可以是 $\displaystyle k-2$。
3. 弹出 $\displaystyle k$ 后，还可以继续将 $\displaystyle k+1,k+2,\ldots,s$ 入栈，再弹出 $\displaystyle s$。因此 $\displaystyle p_3$ 还可以取 $\displaystyle k+1,k+2,\ldots,n$，共 $\displaystyle n-k$ 个值。

所以 $\displaystyle p_3$ 的可能取值集合为

$$
\displaystyle
\{k-2,\ k-1,\ k+1,\ldots,n\}
$$

可能取值个数为

$$
\displaystyle
2+(n-k)=n-k+2
$$

故选择 D。A 少计了一个较小值；B、C 没有考虑 $\displaystyle p_2=k$ 对可取值范围的限制。

</details>

---

4. **【竟成·模拟六-02】** 下列关于栈的说法错误的是（）。
I. 树的层序遍历必须使用栈
II. 图的深度优先遍历可以使用栈来实现
III. 图的广度优先遍历必须使用栈
IV. 树的后序遍历非递归实现中，栈内的结点元素是当前遍历到的结点的祖先元素集合

<div className="choice-options">
<div className="choice-option-row">A. I、III、IV &emsp;&emsp; B. II、IV &emsp;&emsp; C. I、III &emsp;&emsp; D. III、IV</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 逐项判断如下：

- I 错误。树的层序遍历按照“从上到下、从左到右”的顺序访问结点，需要使用队列保存下一层待访问结点，而不是栈。
- II 正确。图的深度优先遍历本质上沿一条路径不断深入，可以使用递归实现，也可以显式使用栈实现。
- III 错误。图的广度优先遍历需要按发现先后依次扩展顶点，因此应使用队列，而不是栈。
- IV 正确。树的非递归后序遍历在深入子树时，需要用栈保存尚未完成访问的祖先结点，以便左右子树处理结束后回溯访问根结点。

因此错误的是 I、III，选择 C。

</details>

---

### 3.1.2 栈的顺序存储结构

5. **【竟成·模拟四-01】** 数组A[n]被双栈共享空间，其中栈一的初始栈顶指针为top1=0，栈二的初始栈顶指针为top2=n-1。则双栈判满的条件为（）。

<div className="choice-options">
<div className="choice-option-row">A. top1==top2 &emsp;&emsp; B. top1-top2==1 &emsp;&emsp; C. top2-top1==1 &emsp;&emsp; D. top1+top2==n</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 栈一从数组低地址向高地址增长，`top1` 指向其下一个可用单元；栈二从高地址向低地址增长，`top2` 指向其下一个可用单元。两栈之间没有剩余空间时，栈一的下一空位置恰好越过栈二的下一空位置，即

$$
\displaystyle
top1=top2+1
$$

因而判满条件为 $\displaystyle top1-top2=1$，选择 B。A 表示两指针仍指向同一个可用单元，尚可再存入一个元素；C 的方向相反；D 与两栈实际占用情况没有固定对应关系。

</details>

---

## 3.2 队列

### 3.2.1 队列的基本概念

6. **【竟成·模拟二-02】** 下列不属于队列基本操作的是（）。

<div className="choice-options">
<div className="choice-option-row">A. 判断队列是否为空 &emsp;&emsp; B. 删除队尾元素 &emsp;&emsp; C. 队列初始化 &emsp;&emsp; D. 获取队头元素</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 队列是只允许在队尾插入、在队头删除的先进先出线性表。其基本操作通常包括初始化队列、判断队列是否为空、在队尾入队、从队头出队以及读取队头元素。因此 A、C、D 都属于队列的基本操作。

删除队尾元素不符合普通队列“队头删除”的受限规则，属于双端队列等结构才可能提供的操作，故选择 B。

</details>

---

### 3.2.2 队列的顺序存储结构

7. **【竟成·模拟四-03】** 若循环队列使用数组A[m]存放数据元素。已知头指针front指向队首元素，尾指针rear指向队尾元素后的空单元，则当前队列中元素的个数为（）。

<div className="choice-options">
<div className="choice-option-row">A. (rear-front+m)%m &emsp;&emsp; B. rear-front+1 &emsp;&emsp; C. rear-front &emsp;&emsp; D. rear-front-1</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** `front` 指向队首元素，`rear` 指向队尾元素后的空单元。未发生回绕时，队列长度为 $\displaystyle rear-front$；发生回绕时，队列长度为 $\displaystyle m-front+rear$。统一写成

$$
\displaystyle
(rear-front+m)\bmod m
$$

故 A 正确。其余各式均不能同时适用于 `rear` 位于 `front` 前后两种情况。

</details>

---

## 3.3 栈和队列的应用

### 3.3.2 栈在表达式求值中的应用

8. **【竟成·模拟二-03】** 若使用大小为3的操作数栈来求解表达式的数值，则下列表达式中会发生上溢的是（）。

<div className="choice-options">
<div className="choice-option-row">A. A-B*(C-D)+E &emsp;&emsp; B. (A-B)*C-D+E &emsp;&emsp; C. (A-B*C+E)-D &emsp;&emsp; D. (A-B)*(C-D)</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 使用操作数栈求值时，可先考查各表达式对应的后缀表达式在求值过程中的最大栈深。

- A 的后缀表达式为 `A B C D - * - E +`。依次读入前四个操作数 `A、B、C、D` 时，栈中同时存在 4 个操作数，超过容量 3，因此发生上溢。
- B 的后缀表达式为 `A B - C * D - E +`，最大栈深为 2。
- C 的后缀表达式为 `A B C * - E + D -`，最大栈深为 3。
- D 的后缀表达式为 `A B - C D - *`，最大栈深为 3。

因此只有 A 会使大小为 3 的操作数栈上溢。

</details>

---

## 3.4 数组和特殊矩阵

### 3.4.3 特殊矩阵的压缩存储

9. **【竟成·模拟一-03】** 已知大小为10×10×10的三维数组A的前两维构成对称矩阵（即A[i][j][k]=A[j][i][k]），按列优先顺序存储且仅存储下三角部分，每个元素占用4个存储单元。若元素A[2][3][4]的存储地址是3000，则元素A[5][5][5]的存储地址是（）。

<div className="choice-options">
<div className="choice-option-row">A. 3292 &emsp;&emsp; B. 3296 &emsp;&emsp; C. 3300 &emsp;&emsp; D. 3304</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 数组下标按 0 开始计算。每个固定的第三维下标 $\displaystyle k$ 对应一个 $\displaystyle 10\times10$ 对称矩阵，只存储下三角部分，因此每层实际存储的元素个数为

$$
\displaystyle
\dfrac{10\times(10+1)}{2}=55
$$

对下三角矩阵按列优先压缩存储，元素 $\displaystyle A[i][j]$（$\displaystyle i\geqslant j$）在一层内的相对下标为

$$
\displaystyle
f(i,j)=\dfrac{j(2n-j+1)}{2}+i-j
$$

由于前两维对称，$\displaystyle A[2][3][4]=A[3][2][4]$。取 $\displaystyle n=10$，有

$$
\displaystyle
f(3,2)=\dfrac{2(20-2+1)}{2}+3-2=20
$$

而

$$
\displaystyle
f(5,5)=\dfrac{5(20-5+1)}{2}+5-5=40
$$

从第 4 层到第 5 层先跨过 55 个元素，层内又向后移动 $\displaystyle 40-20=20$ 个元素，因此总共相差

$$
\displaystyle
55+20=75
$$

个元素。每个元素占 4 个存储单元，所以目标地址为

$$
\displaystyle
3000+75\times4=3300
$$

故选择 C。

</details>

---

10. **【竟成·模拟三-03】** 下三角矩阵A(n×n)按列优先顺序压缩在数组S[(n+1)×n/2]中，若非零元素$\displaystyle a_{ij}$(0≤i,j&lt;n)存放在S[k]中，则i,j,k的关系为（）。

<div className="choice-options">
<div className="choice-option-row">A. k=i×n+j &emsp;&emsp; B. k=(2n-j+1)×j/2+i-j &emsp;&emsp; C. k=(i+1)×i/2+j &emsp;&emsp; D. k=(2n-j+1)×j/2+i</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 下三角矩阵按列优先存储时，第 $\displaystyle 0$ 列有 $\displaystyle n$ 个非零元素，第 $\displaystyle 1$ 列有 $\displaystyle n-1$ 个，依次类推。

在第 $\displaystyle j$ 列之前共有

$$
\displaystyle
n+(n-1)+\cdots+(n-j+1)
=\dfrac{j(2n-j+1)}{2}
$$

个元素。在第 $\displaystyle j$ 列内部，元素 $\displaystyle a_{ij}$ 前面还有 $\displaystyle i-j$ 个元素，因此

$$
\displaystyle
k=\dfrac{j(2n-j+1)}{2}+i-j
$$

与选项 B 相同。C 是下三角矩阵按行优先存储时的公式；D 少减了列内起始行号 $\displaystyle j$。

</details>
