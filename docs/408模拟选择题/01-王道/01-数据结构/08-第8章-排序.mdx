---
sidebar_position: 8
title: 王道 · 数据结构 · 第8章 排序
---

{/* 此文件由 scripts/convert-408-mock-exercises.mjs 自动生成，请勿直接修改。 */}

# 王道 · 数据结构 · 第8章 排序

## 8.3 交换排序

### 8.3.2 快速排序

1. **【王道·卷一-Q11】** 对8个元素的线性表进行快速排序，在最好情况下，元素之间的比较次数为（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 7 &emsp;&emsp; B. 8 &emsp;&emsp; C. 12 &emsp;&emsp; D. 13</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 快速排序最好情况下，每次划分都尽可能均匀。一次对含有 $\displaystyle n$ 个元素的子表进行划分，需要将枢轴与其余 $\displaystyle n-1$ 个元素进行比较。

设最好情况下的比较次数为 $\displaystyle C(n)$。对 8 个元素，第一次划分后，除枢轴外的 7 个元素只能分成大小为 3 和 4 的两个子表，因此：

$$
\displaystyle
C(8)=7+C(3)+C(4)
$$

其中：

$$
\displaystyle
C(3)=2
$$

4 个元素最好可划分成大小为 1 和 2 的子表，所以：

$$
\displaystyle
C(4)=3+C(1)+C(2)=3+0+1=4
$$

因此：

$$
\displaystyle
C(8)=7+2+4=13
$$

故选 D。A、B 只考虑了首趟或少量比较；C 少计了某一层划分所需的比较次数。

</details>

---

2. **【王道·卷五-Q01】** 在下列数据结构中，不适用于快速排序的是（  ）。
I. 数组 II. 单链表 III. 静态链表 IV. 双链表

<div className="choice-options">
<div className="choice-option-row">A. II、IV &emsp;&emsp; B. II、III、IV &emsp;&emsp; C. I、II、IV &emsp;&emsp; D. 全部适用</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 经典快速排序的划分过程通常需要设置两个指针，从待排序区间两端向中间移动，并频繁访问区间中的任意位置。

- 数组支持按下标随机访问，也容易设置左右边界，因此适用于快速排序。
- 单链表只能沿后继方向顺序访问，无法高效地从右端向左扫描。
- 静态链表虽然用数组保存结点，但逻辑次序由游标链接形成，本质上仍是链式存储，不便于按逻辑位置双向扫描。
- 双链表虽然能双向移动，但不能按序号在 $\displaystyle O(1)$ 时间定位区间端点，交换和递归划分也不如顺序表方便，通常不采用经典快速排序。

因此不适用的是 II、III、IV，故选 B。

</details>

---

## 8.4 选择排序

### 8.4.2 堆排序

3. **【王道·卷二-Q11】** 堆排序分为两个阶段，其中一个阶段将给定的序列建成一个堆，第二个阶段逐次输出堆顶元素。设给定序列为&#123;48,62,35,77,55,14,35,98&#125;，若在堆排序的第一个阶段将该序列建成一个堆（大根堆），则交换元素的次数为（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 5 &emsp;&emsp; B. 6 &emsp;&emsp; C. 7 &emsp;&emsp; D. 8</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 按顺序存储的完全二叉树，从最后一个非叶结点 $\displaystyle \lfloor n/2\rfloor=4$ 开始，依次向前进行向下调整。

初始序列为：

```text
48, 62, 35, 77, 55, 14, 35, 98
```

调整过程如下。

1. 调整下标 4 的结点 77：与孩子 98 交换，交换 1 次。
2. 调整下标 3 的结点 35：其孩子为 14、35，不需交换。
3. 调整下标 2 的结点 62：
- 先与较大的孩子 98 交换；
- 62 下沉后再与孩子 77 交换。

共交换 2 次。
4. 调整根结点 48：
- 与较大的孩子 98 交换；
- 48 下沉后与 77 交换；
- 继续下沉后与 62 交换。

共交换 3 次。

总交换次数为：

$$
\displaystyle
1+0+2+3=6
$$

最终大根堆可表示为：

```text
98, 77, 35, 62, 55, 14, 35, 48
```

故选 B。

</details>

---

4. **【王道·卷三-Q10】** 对关键字序列&#123;23,17,72,60,25,8,68,71,52&#125;进行堆排序，输出两个最小关键字后的剩余堆是（  ）。

<div className="choice-options">
<div className="choice-option-row">A. &#123;23,72,60,25,68,71,52&#125; &emsp;&emsp; B. &#123;23,25,52,60,71,72,68&#125;</div>
<div className="choice-option-row">C. &#123;71,25,23,52,60,72,68&#125; &emsp;&emsp; D. &#123;23,25,68,52,60,72,71&#125;</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 题目要求依次输出最小关键字，因此应先建立小根堆。

初始序列建成小根堆后为：

```text
8, 17, 23, 52, 25, 72, 68, 71, 60
```

第一次输出堆顶 8，将末尾元素 60 移至根并向下调整，得到：

```text
17, 25, 23, 52, 60, 72, 68, 71
```

第二次输出堆顶 17，将末尾元素 71 移至根并向下调整：

- 71 与较小孩子 23 交换；
- 71 继续与较小孩子 68 交换。

得到剩余小根堆：

```text
23, 25, 68, 52, 60, 72, 71
```

与选项 D 一致，故选 D。其他选项均不满足小根堆中父结点关键字不大于孩子结点关键字的性质，或不是两次删除堆顶后的正确调整结果。

</details>

---

5. **【王道·卷五-Q10】** 如果从下图的堆中删除值为11的结点，那么值为70的结点将出现在图中的（  ）位置。
> **题图缺失：** 卷五_Q10_堆删除（原引用：`images/卷五_Q10_堆删除.png`）

<div className="choice-options">
<div className="choice-option-row">A. A &emsp;&emsp; B. B &emsp;&emsp; C. C &emsp;&emsp; D. D</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 图中是一个小根堆，11 为堆顶。删除堆顶时，将堆的最后一个结点 70 移到根结点位置，并将堆的元素个数减 1，然后对 70 进行向下调整。

调整过程如下：

1. 根结点 70 的两个孩子为 25 和 20，应与较小的 20 交换。
2. 70 下沉到原 20 的位置，其孩子为 33 和 37，应与较小的 33 交换。
3. 70 继续下沉，其剩余孩子为 42，应再与 42 交换。

因此 70 最终移动到原来 42 所在的位置，即图中的 C 位置，故选 C。

A 位置最终仍位于左子树上；B 是结点 44 所在位置；D 是结点 37 所在位置，均不是 70 向下调整后的最终位置。

</details>

---

## 8.6 各种内部排序算法的比较及应用

### 8.6.1 内部排序算法的比较

6. **【王道·卷二-Q10】** 假设在快速排序算法中总是选择待排序子序列中的最后一个元素作为基准，则这个算法的最坏情况出现在（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 待排序序列初始有序时 &emsp;&emsp; B. 待排序序列呈现中间小并逐次向两边增大的情况</div>
<div className="choice-option-row">C. 待排序序列呈现中间大并逐次向两边减小的情况 &emsp;&emsp; D. 以上选项都不是</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 快速排序的性能取决于每次划分后两个子表是否均衡。若每次选取待排序子序列的最后一个元素作为枢轴，而原序列已经递增有序，则枢轴始终是当前子序列中的最大元素；若原序列递减有序，则枢轴始终是最小元素。

此时每次划分只能确定一个元素的位置，两个子表的规模分别为 $\displaystyle n-1$ 和 $\displaystyle 0$，递归深度达到 $\displaystyle n$，比较次数为：

$$
\displaystyle
(n-1)+(n-2)+\cdots+1=\dfrac{n(n-1)}{2}
$$

时间复杂度退化为 $\displaystyle O(n^2)$。选项 A 所述“初始有序”包含这种极端不均衡情形，故选 A。

</details>

---

7. **【王道·卷四-Q10】** 对一组数据(84,47,15,21,25)排序，数据在排序过程中的变化如下：
1) 84 47 15 21 25; 2) 25 47 15 21 84; 3) 21 25 15 47 84; 4) 15 21 25 47 84
则所采用的排序方法是（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 堆排序 &emsp;&emsp; B. 冒泡排序 &emsp;&emsp; C. 快速排序 &emsp;&emsp; D. 插入排序</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 初始序列本身已经满足大根堆性质：

```text
84
/    \
47      15
/  \
21   25
```

堆排序每趟将堆顶最大元素与当前无序区的最后一个元素交换，再对剩余无序区进行向下调整。

- 第 1 趟先交换 84 和 25，得到 `25 47 15 21 84`；调整后无序区重新成为大根堆。
- 第 2 趟交换当前堆顶 47 与无序区末尾元素，得到题中第 3 个状态。
- 后续继续交换和调整，最终得到 `15 21 25 47 84`。

题目列出的状态是各趟将堆顶元素放到最终位置后的关键状态，符合堆排序过程，故选 A。

</details>

---

8. **【王道·卷六-Q11】** 已知待排序的 $\displaystyle n$ 个元素可分为 $\displaystyle n / k$ 组，每组包含 $\displaystyle k$ 个元素，任一组内的各元素均分别大于前一组内的所有元素且小于后一组内的所有元素，若采用基于比较的排序，其时间下界为（  ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle O(n\log_{2}n)$ &emsp;&emsp; B. $\displaystyle O(n\log_{2}k)$ &emsp;&emsp; C. $\displaystyle O(k\log_{2}n)$ &emsp;&emsp; D. $\displaystyle O(k\log_{2}k)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 各组之间的相对大小关系已经确定，因此无须比较不同组中的元素，只需分别将每组内部的 $\displaystyle k$ 个元素排好序。

对一组 $\displaystyle k$ 个元素采用基于比较的排序，其时间下界为：

$$
\displaystyle
\Omega(k\log_2 k)
$$

一共有 $\displaystyle n/k$ 组，所以总时间下界为：

$$
\displaystyle
\dfrac{n}{k}\cdot\Omega(k\log_2 k)
=\Omega(n\log_2 k)
$$

因此选 B。选项 A 忽略了组间次序已经确定这一条件；C、D 均未正确计入全部 $\displaystyle n/k$ 组的排序代价。

</details>

---

9. **【王道·卷七-Q10】** 在下列排序方法中，时间性能与待排序记录的初始状态无关的是（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 插入排序和快速排序 &emsp;&emsp; B. 归并排序和快速排序</div>
<div className="choice-option-row">C. 选择排序和归并排序 &emsp;&emsp; D. 插入排序和归并排序</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** - 简单选择排序无论初始序列如何，都要在剩余无序区中寻找最小元素，关键字比较次数恒为：

$$
\displaystyle
\dfrac{n(n-1)}{2}
$$

- 归并排序按固定层次不断划分并归并，整体时间复杂度始终为 $\displaystyle O(n\log_2 n)$，受初始有序程度影响很小。
- 插入排序在序列基本有序时接近 $\displaystyle O(n)$，逆序时为 $\displaystyle O(n^2)$，明显依赖初始状态。
- 快速排序在划分均衡时为 $\displaystyle O(n\log_2 n)$，在枢轴反复落在一端时退化为 $\displaystyle O(n^2)$，也依赖初始状态和枢轴选择。

因此选 C。

</details>

---

10. **【王道·卷八-Q11】** 在最好情况下，时间复杂度可以达到线性时间的排序算法是（  ）。
$\displaystyle ①$ 冒泡排序 $\displaystyle ②$ 堆排序 $\displaystyle ③$ 快速排序 $\displaystyle ④$ 归并排序 $\displaystyle ⑤$ 直接插入排序

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle ①$ &emsp;&emsp; B. $\displaystyle ①⑤$ &emsp;&emsp; C. $\displaystyle ④⑤$ &emsp;&emsp; D. $\displaystyle ②③$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 当序列已经有序时：

- 使用“本趟是否发生交换”标志的冒泡排序只需扫描一趟即可结束，最好时间复杂度为 $\displaystyle O(n)$。
- 直接插入排序中，每个元素只需与其前驱比较一次，无须移动大量元素，最好时间复杂度为 $\displaystyle O(n)$。
- 堆排序的建堆和反复调整仍需 $\displaystyle O(n\log_2 n)$ 量级的工作。
- 经典快速排序即使划分较均衡，最好时间复杂度也是 $\displaystyle O(n\log_2 n)$。
- 归并排序无论初始状态如何，都要完成各层归并，时间复杂度为 $\displaystyle O(n\log_2 n)$。

因此能在最好情况下达到线性时间的是 $\displaystyle ①⑤$，故选 B。

</details>

---

## 8.7 外部排序

### 8.7.3 多路平衡归并与败者树

11. **【王道·卷三-Q11】** 6路归并的败者树的深度为（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 3 &emsp;&emsp; B. 5 &emsp;&emsp; C. 6 &emsp;&emsp; D. 4</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 败者树本质上是一棵用于从 $\displaystyle k$ 个候选记录中反复选出最小记录的完全二叉比较树。对于 $\displaystyle k$ 路归并，从叶结点到根结点所需的比较层数为：

$$
\displaystyle
\left\lceil \log_2 k \right\rceil
$$

当 $\displaystyle k=6$ 时：

$$
\displaystyle
\left\lceil \log_2 6 \right\rceil=3
$$

因此 6 路归并的败者树深度为 3，选择 A。B、C、D 均未按二叉比较树的层数计算。

</details>

---

12. **【王道·卷四-Q11】** 18个初始归并段进行5路平衡归并，需要增加（  ）个虚拟归并段。

<div className="choice-options">
<div className="choice-option-row">A. 1 &emsp;&emsp; B. 2 &emsp;&emsp; C. 3 &emsp;&emsp; D. 4</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 为了构造严格的 $\displaystyle k$ 路最佳归并树，叶结点数 $\displaystyle n'$ 应满足：

$$
\displaystyle
(n'-1)\bmod(k-1)=0
$$

本题中 $\displaystyle n=18$，$\displaystyle k=5$。设需增加 $\displaystyle d$ 个虚拟归并段，则：

$$
\displaystyle
\begin{aligned}
(18+d-1)\bmod 4&=0\\
(17+d)\bmod 4&=0
\end{aligned}
$$

因为 $\displaystyle 17\bmod 4=1$，所以最小的非负整数 $\displaystyle d=3$。增加 3 个虚拟归并段后，共有 21 个叶结点，满足：

$$
\displaystyle
(21-1)\bmod 4=0
$$

因此选 C。

</details>

---

13. **【王道·卷五-Q11】** 外部排序过程中的主要开销是I/O操作，因此可以采取一些措施来缩短I/O时间，则下列说法中正确的是（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 使用置换-选择排序是为了通过减小初始归并段的长度来减少元素之间的比较次数</div>
<div className="choice-option-row">B. 使用败者树可以优化置换-选择排序来减少元素之间的比较次数</div>
<div className="choice-option-row">C. 败者树是为了增大归并路数，败者树的路数越多，排序时间就越短</div>
<div className="choice-option-row">D. 构建的最佳归并树和哈夫曼树一样，只有度为0和2的结点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 逐项判断如下。

- A 错误。置换-选择排序的主要目的恰恰是**增大**初始归并段的平均长度，从而减少初始归并段的数量和后续归并趟数，降低磁盘 I/O 次数。
- B 正确。在置换-选择排序中，需要反复从内存工作区中选出满足条件的最小记录。若直接顺序查找，每次选择代价较大；利用败者树后，每输出一个记录，只需沿一条从叶到根的路径重新比较，比较次数约为 $\displaystyle O(\log_2 m)$，其中 $\displaystyle m$ 为工作区中的记录数。
- C 错误。败者树可降低多路归并时选取最小记录的比较代价，从而为增加归并路数提供支持，但归并路数并非越多越好。路数增大还会增加输入缓冲区数量、内存需求及管理开销，不能保证总排序时间一定缩短。
- D 错误。二路哈夫曼树只有度为 0 和 2 的结点；而 $\displaystyle k$ 路最佳归并树经过补充虚拟归并段后，非叶结点通常为度 $\displaystyle k$，并非仅有度 0 和 2 的结点。

因此选 B。

</details>

---

14. **【王道·卷七-Q11】** 若对29条记录只进行3趟多路平衡归并，则 $\displaystyle x$ 选取的归并路数至少是（  ）。

<div className="choice-options">
<div className="choice-option-row">A. 2 &emsp;&emsp; B. 3 &emsp;&emsp; C. 4 &emsp;&emsp; D. 5</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 若将每条记录视为一个初始归并段，进行 $\displaystyle x$ 路平衡归并，每完成一趟，归并段数量最多缩小为原来的 $\displaystyle 1/x$。经过 3 趟后能够归并的初始段数最多为：

$$
\displaystyle
x^3
$$

要使 29 个初始段在 3 趟内归并为一个有序段，需要：

$$
\displaystyle
x^3\ge 29
$$

检验各整数：

$$
\displaystyle
3^3=27<29,\qquad 4^3=64\ge 29
$$

因此归并路数至少为 4，选择 C。

</details>
