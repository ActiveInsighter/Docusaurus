---
title: "六、经济学应用(数三专项)"
sidebar_position: 6
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 六、经济学应用(数三专项)

## 小题

1. **【1989-5-3 分】** 某商品的需求量 $Q$ 与价格 $P$ 的函数关系为 $Q=ap^b$，其中 $a$ 和 $b$ 为常数，且 $a\neq 0$，则需求量对价格 $P$ 的弹性是___。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle b$。

**解析：** 按点弹性的定义，需求量 $Q$ 关于价格 $p$ 的弹性为

$$
\displaystyle E_p=\dfrac{dQ}{dp}\cdot\dfrac{p}{Q}.
$$

由

$$
\displaystyle Q=ap^b
$$

得

$$
\displaystyle \dfrac{dQ}{dp}=abp^{b-1}.
$$

所以

$$
\displaystyle E_p=abp^{b-1}\cdot\dfrac{p}{ap^b}=b.
$$

若采用弹性绝对值的记法，则其绝对值为 $\displaystyle |b|$。

</details>

---

2. **【1992-45-3 分】** 设商品的需求函数为 $Q=100-5P$，其中 $Q$，$P$ 分别表示需求量和价格，如果商品需求弹性的绝对值大于1，则商品价格的取值范围是（元）。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 10<P<20$。

**解析：** 需求函数为

$$
\displaystyle Q=100-5P.
$$

在经济意义下要求 $\displaystyle Q>0$，故

$$
\displaystyle 0<P<20.
$$

需求价格弹性的绝对值为

$$
\displaystyle \eta=-\dfrac{dQ}{dP}\cdot\dfrac{P}{Q}
=5\cdot\dfrac{P}{100-5P}
=\dfrac{P}{20-P}.
$$

由 $\displaystyle \eta>1$ 得

$$
\displaystyle \dfrac{P}{20-P}>1.
$$

由于 $\displaystyle 0<P<20$，分母为正，因此

$$
\displaystyle P>20-P,
$$

即

$$
\displaystyle P>10.
$$

结合 $\displaystyle P<20$，得

$$
\displaystyle 10<P<20.
$$

</details>

---

3. **【2001-34-3 分】** 设生产函数为 $Q=AL^{\alpha}K^{\beta}$，其中 $Q$ 是产出量，$L$ 是劳动投入量，$K$ 是资本投入量，而 $A$，$\alpha$，$\beta$ 均为大于零的参数，则当 $Q=1$ 时 $K$ 关于 $L$ 的弹性为___。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle -\dfrac{\alpha}{\beta}$。

**解析：** 当 $\displaystyle Q=1$ 时，$L$ 与 $K$ 满足

$$
\displaystyle AL^{\alpha}K^{\beta}=1.
$$

两边取对数，得

$$
\displaystyle \ln A+\alpha\ln L+\beta\ln K=0.
$$

对 $L$ 求导，有

$$
\displaystyle \dfrac{\alpha}{L}+\beta\dfrac{1}{K}\dfrac{dK}{dL}=0.
$$

因此

$$
\displaystyle \dfrac{dK}{dL}=-\dfrac{\alpha K}{\beta L}.
$$

故 $K$ 关于 $L$ 的弹性为

$$
\displaystyle E_L^K=\dfrac{dK}{dL}\cdot\dfrac{L}{K}
=-\dfrac{\alpha}{\beta}.
$$

</details>

---

4. **【2007-34-4 分】** 设某商品的需求函数为 $Q=160-2p$，其中 $Q$，$p$ 分别表示需求量和价格，如果该商品需求弹性的绝对值等于1，则商品的价格是( )

<div className="choice-options">
<div className="choice-option-row">A. 10 &emsp;&emsp; B. 20 &emsp;&emsp; C. 30 &emsp;&emsp; D. 40</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 需求价格弹性的绝对值为

$$
\displaystyle \eta=-\dfrac{dQ}{dp}\cdot\dfrac{p}{Q}.
$$

由 $\displaystyle Q=160-2p$，有

$$
\displaystyle \dfrac{dQ}{dp}=-2,
$$

故

$$
\displaystyle \eta=2\cdot\dfrac{p}{160-2p}
=\dfrac{p}{80-p}.
$$

令 $\displaystyle \eta=1$，得

$$
\displaystyle \dfrac{p}{80-p}=1,
$$

从而

$$
\displaystyle p=80-p,
\qquad
2p=80,
$$

即

$$
\displaystyle p=40.
$$

故选 D。

</details>

---

5. **【2009-3-4 分】** 设某产品的需求函数为 $Q=Q(P)$，其对应价格 $P$ 的弹性 $\xi_P=0.2$，则当需求量为10000件时，价格增加1元会使产品收益增加_________元。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 8000$ 元。

**解析：** 总收益为

$$
\displaystyle R=PQ.
$$

需求价格弹性取正值形式为

$$
\displaystyle \xi_P=-\dfrac{dQ}{dP}\cdot\dfrac{P}{Q}.
$$

于是

$$
\displaystyle P\dfrac{dQ}{dP}=-\xi_P Q.
$$

因此收益对价格的导数为

$$
\begin{aligned}
\displaystyle \dfrac{dR}{dP}
&=Q+P\dfrac{dQ}{dP}\\
&=Q(1-\xi_P).
\end{aligned}
$$

当 $\displaystyle Q=10000$，$\displaystyle \xi_P=0.2$ 时，

$$
\displaystyle \dfrac{dR}{dP}=10000(1-0.2)=8000.
$$

故价格增加1元时，收益约增加 $\displaystyle 8000$ 元。

</details>

---

6. **【2010-3-4 分】** 设某商品的收益函数为 $R(p)$，收益弹性为 $1+p^3$，其中 $p$ 为价格，且 $R(1)=1$，则 $R(p)=$___。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle R(p)=p\exp\left(\dfrac{p^3-1}{3}\right)$。

**解析：** 收益函数关于价格的弹性为

$$
\displaystyle \dfrac{pR'(p)}{R(p)}=1+p^3.
$$

因此

$$
\displaystyle \dfrac{R'(p)}{R(p)}=\dfrac{1}{p}+p^2.
$$

两边对 $p$ 积分，得

$$
\displaystyle \ln R(p)=\ln p+\dfrac{p^3}{3}+C.
$$

于是

$$
\displaystyle R(p)=C_1p e^{p^3/3}.
$$

由 $\displaystyle R(1)=1$，得

$$
\displaystyle 1=C_1e^{1/3},
$$

从而

$$
\displaystyle C_1=e^{-1/3}.
$$

故

$$
\displaystyle R(p)=p e^{(p^3-1)/3}.
$$

</details>

---

7. **【2014-3-4 分】** 设某商品的需求函数为 $Q=40-2P$（$P$ 为商品的价格），则该商品的边际收益为___。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle MR=20-Q$，等价地，$\displaystyle MR=2P-20$。

**解析：** 由需求函数

$$
\displaystyle Q=40-2P
$$

解得反需求函数

$$
\displaystyle P=20-\dfrac{Q}{2}.
$$

总收益为

$$
\begin{aligned}
\displaystyle R(Q)
&=PQ\\
&=\left(20-\dfrac{Q}{2}\right)Q\\
&=20Q-\dfrac{Q^2}{2}.
\end{aligned}
$$

故边际收益为

$$
\displaystyle MR=R'(Q)=20-Q.
$$

由 $\displaystyle Q=40-2P$，也可写为

$$
\displaystyle MR=20-(40-2P)=2P-20.
$$

</details>

---

8. **【2017-3-4 分】** 设生产某产品的平均成本 $\bar{C}(Q)=1+e^{-Q}$，其中 $Q$ 为产量，则边际成本为___。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle MC=1+(1-Q)e^{-Q}$。

**解析：** 平均成本满足

$$
\displaystyle \bar{C}(Q)=\dfrac{C(Q)}{Q}.
$$

因此总成本为

$$
\displaystyle C(Q)=Q\bar{C}(Q)=Q(1+e^{-Q}).
$$

边际成本为总成本对产量的导数：

$$
\begin{aligned}
\displaystyle MC
&=C'(Q)\\
&=\dfrac{d}{dQ}\left[Q(1+e^{-Q})\right]\\
&=1+e^{-Q}-Qe^{-Q}\\
&=1+(1-Q)e^{-Q}.
\end{aligned}
$$

</details>

---

9. **【2018-3-4 分】** 设某产品的成本函数 $C(Q)$ 可导，其中 $Q$ 为产量，若产量为 $Q_0$ 时平均成本最小，则( )

<div className="choice-options">
<div className="choice-option-row">A. $C'(Q_0)=0$ &emsp;&emsp; B. $C'(Q_0)=C(Q_0)$ &emsp;&emsp; C. $C'(Q_0)=Q_0C(Q_0)$ &emsp;&emsp; D. $Q_0C'(Q_0)=C(Q_0)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 平均成本为

$$
\displaystyle \bar{C}(Q)=\dfrac{C(Q)}{Q}.
$$

当 $\displaystyle Q=Q_0$ 时平均成本取得极小值，必要条件为

$$
\displaystyle \bar{C}'(Q_0)=0.
$$

而

$$
\displaystyle \bar{C}'(Q)
=\dfrac{QC'(Q)-C(Q)}{Q^2}.
$$

因此

$$
\displaystyle Q_0C'(Q_0)-C(Q_0)=0,
$$

即

$$
\displaystyle Q_0C'(Q_0)=C(Q_0).
$$

故选 D。

</details>

---

10. **【2019-3-4 分】** 以 $p_A$，$p_B$ 分别表示 A，B 两种商品的价格，设商品 A 的需求函数为 $Q_A=500-p_A^2-p_Ap_B+2p_B^2$，则当 $p_A=10$，$p_B=20$ 时，商品 A 的需求量对自身价格的弹性 $\eta_{AA}(\eta_{AA}>0)$ 为___。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 0.4$。

**解析：** 商品 A 的自身价格弹性取正值形式为

$$
\displaystyle \eta_{AA}
=-\dfrac{\partial Q_A}{\partial p_A}\cdot\dfrac{p_A}{Q_A}.
$$

先计算需求量：

$$
\begin{aligned}
\displaystyle Q_A
&=500-10^2-10\times 20+2\times 20^2\\
&=500-100-200+800\\
&=1000.
\end{aligned}
$$

又

$$
\displaystyle \dfrac{\partial Q_A}{\partial p_A}=-2p_A-p_B.
$$

在 $\displaystyle p_A=10$，$\displaystyle p_B=20$ 时，

$$
\displaystyle \dfrac{\partial Q_A}{\partial p_A}=-40.
$$

因此

$$
\displaystyle \eta_{AA}
=-(-40)\cdot\dfrac{10}{1000}
=0.4.
$$

</details>

---

11. **【2020-3-4 分】** 设 $Q$ 表示产量，成本为 $C(Q)=100+13Q$，单价为 $P$，需求量 $Q(P)=\dfrac{800}{P+3}-2$。则工厂取得利润最大时的产量为

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 8$。

**解析：** 由需求函数

$$
\displaystyle Q=\dfrac{800}{P+3}-2
$$

解得反需求函数

$$
\displaystyle P=\dfrac{800}{Q+2}-3.
$$

因此收益函数为

$$
\begin{aligned}
\displaystyle R(Q)
&=PQ\\
&=\left(\dfrac{800}{Q+2}-3\right)Q.
\end{aligned}
$$

利润函数为

$$
\begin{aligned}
\displaystyle L(Q)
&=R(Q)-C(Q)\\
&=\dfrac{800Q}{Q+2}-3Q-(100+13Q)\\
&=\dfrac{800Q}{Q+2}-16Q-100.
\end{aligned}
$$

求导得

$$
\displaystyle L'(Q)=\dfrac{1600}{(Q+2)^2}-16.
$$

令 $\displaystyle L'(Q)=0$，得

$$
\displaystyle (Q+2)^2=100.
$$

由于产量 $\displaystyle Q\geq 0$，故

$$
\displaystyle Q+2=10,
$$

从而

$$
\displaystyle Q=8.
$$

又

$$
\displaystyle L''(Q)=-\dfrac{3200}{(Q+2)^3}<0,
$$

所以当产量为 $\displaystyle 8$ 时，利润取得最大值。

</details>

---

12. **【2023-3-5 分】** 设某公司在 $t$ 时刻的资产为 $f(t)$，从 0 时刻到 $t$ 时刻的平均资产等于 $\dfrac{f(t)}{t}-t$，假设 $f(t)$ 连续且 $f(0)=0$，则 $f(t)=$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle f(t)=2e^t-2t-2$。

**解析：** 从 0 时刻到 $t$ 时刻的平均资产为

$$
\displaystyle \dfrac{1}{t}\int_0^t f(x)\,dx.
$$

根据题意，

$$
\displaystyle \dfrac{1}{t}\int_0^t f(x)\,dx=\dfrac{f(t)}{t}-t.
$$

两边同乘 $t$，得

$$
\displaystyle \int_0^t f(x)\,dx=f(t)-t^2.
$$

两边对 $t$ 求导，得

$$
\displaystyle f(t)=f'(t)-2t,
$$

即

$$
\displaystyle f'(t)-f(t)=2t.
$$

这是一个一阶线性微分方程。乘以积分因子 $\displaystyle e^{-t}$，得

$$
\displaystyle \left[e^{-t}f(t)\right]'=2te^{-t}.
$$

由 $\displaystyle f(0)=0$，从 0 到 $t$ 积分得

$$
\begin{aligned}
\displaystyle e^{-t}f(t)
&=\int_0^t 2xe^{-x}\,dx\\
&=\left[-2(x+1)e^{-x}\right]_0^t\\
&=2-2(t+1)e^{-t}.
\end{aligned}
$$

因此

$$
\displaystyle f(t)=2e^t-2t-2.
$$

</details>

---

13. **【2024-3-5 分】** 某产品的价格函数为 $p=\begin{cases}25-0.25Q, & Q\leq 20,\\35-0.75Q, & Q>20,\end{cases}$（$p$ 为单价，单位：万元；$Q$ 为产量，单位：件），总成本函数为 $C=150+5Q+0.25Q^2$（万元），则经营该产品可获得的最大利润为______万元。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 50$ 万元。

**解析：** 利润等于收益减去成本，即

$$
\displaystyle L(Q)=p(Q)Q-C(Q).
$$

当 $\displaystyle 0\leq Q\leq 20$ 时，

$$
\begin{aligned}
\displaystyle L_1(Q)
&=(25-0.25Q)Q-(150+5Q+0.25Q^2)\\
&=20Q-0.5Q^2-150.
\end{aligned}
$$

求导得

$$
\displaystyle L_1'(Q)=20-Q.
$$

故 $\displaystyle L_1(Q)$ 在 $\displaystyle [0,20]$ 上单调增加，在 $\displaystyle Q=20$ 时取得最大值

$$
\displaystyle L_1(20)=20\times 20-0.5\times 20^2-150=50.
$$

当 $\displaystyle Q>20$ 时，

$$
\begin{aligned}
\displaystyle L_2(Q)
&=(35-0.75Q)Q-(150+5Q+0.25Q^2)\\
&=30Q-Q^2-150.
\end{aligned}
$$

此时

$$
\displaystyle L_2'(Q)=30-2Q<0\qquad(Q>20),
$$

所以 $\displaystyle L_2(Q)$ 在 $\displaystyle (20,+\infty)$ 上单调减少，并且

$$
\displaystyle L_2(Q)<\lim_{Q\to 20^+}L_2(Q)=50.
$$

综上，当 $\displaystyle Q=20$ 时利润最大，最大利润为

$$
\displaystyle 50\text{ 万元}.
$$

</details>

## 大题

1. **【1987-5-8 分】** 设某产品的总成本函数为 $C(x)=400+3x+\dfrac{1}{2}x^2$，而需求函数为 $p=\dfrac{100}{\sqrt{x}}$，其中 $x$ 为产量（假定等于需求量），$p$ 为价格。试求：

(1)边际成本；(2)边际收益；(3)边际利润；(4)收益的价格弹性。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) $\displaystyle MC=3+x$；

(2) $\displaystyle MR=\dfrac{50}{\sqrt{x}}$；

(3) $\displaystyle ML=\dfrac{50}{\sqrt{x}}-3-x$；

(4) 收益的价格弹性为 $\displaystyle -1$。

**解析：** (1) 总成本函数为

$$
\displaystyle C(x)=400+3x+\dfrac{1}{2}x^2.
$$

边际成本为

$$
\displaystyle MC=C'(x)=3+x.
$$

(2) 由需求函数

$$
\displaystyle p=\dfrac{100}{\sqrt{x}},
$$

收益函数为

$$
\displaystyle R(x)=px=\dfrac{100}{\sqrt{x}}\cdot x=100\sqrt{x}.
$$

因此边际收益为

$$
\displaystyle MR=R'(x)=\dfrac{50}{\sqrt{x}}.
$$

(3) 利润函数为

$$
\displaystyle L(x)=R(x)-C(x).
$$

故边际利润为

$$
\begin{aligned}
\displaystyle ML
&=L'(x)\\
&=R'(x)-C'(x)\\
&=\dfrac{50}{\sqrt{x}}-3-x.
\end{aligned}
$$

(4) 由

$$
\displaystyle p=\dfrac{100}{\sqrt{x}}
$$

解得

$$
\displaystyle x=\dfrac{10000}{p^2}.
$$

于是收益关于价格的函数为

$$
\displaystyle R(p)=px=\dfrac{10000}{p}.
$$

收益的价格弹性为

$$
\begin{aligned}
\displaystyle E_p^R
&=\dfrac{dR}{dp}\cdot\dfrac{p}{R}\\
&=\left(-\dfrac{10000}{p^2}\right)\cdot\dfrac{p}{10000/p}\\
&=-1.
\end{aligned}
$$

</details>

---

2. **【1987-4-6 分】** 已知某商品的需求量 $x$ 对价格 $p$ 的弹性为 $\eta=-3p^3$，而市场对商品的最大需求量为 1（万件），求需求函数。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle x=e^{-p^3}$（万件）。

**解析：** 需求量 $x$ 对价格 $p$ 的弹性定义为

$$
\displaystyle \eta=\dfrac{p}{x}\dfrac{dx}{dp}.
$$

由题意，

$$
\displaystyle \dfrac{p}{x}\dfrac{dx}{dp}=-3p^3.
$$

当 $\displaystyle p>0$ 时，可化为

$$
\displaystyle \dfrac{1}{x}\dfrac{dx}{dp}=-3p^2.
$$

分离变量并积分，得

$$
\displaystyle \int\dfrac{1}{x}\,dx=\int -3p^2\,dp,
$$

即

$$
\displaystyle \ln x=-p^3+C.
$$

因此

$$
\displaystyle x=Ae^{-p^3},\qquad A>0.
$$

由于需求量随价格增加而减少，最大需求量在 $\displaystyle p=0$ 时取得。题设最大需求量为 1 万件，故

$$
\displaystyle x(0)=A=1.
$$

所以需求函数为

$$
\displaystyle x=e^{-p^3}\quad\text{（万件）}.
$$

</details>

---

3. **【1988-4-8 分】** 已知某商品的需求量 $D$ 和供给量都是价格 $p$ 的函数：$D=D(p)=\dfrac{a}{p^2}$，$S=S(p)=bp$，其中 $a>0$ 和 $b>0$ 是常数；价格 $p$ 是时间 $t$ 的函数，且满足方程 $\dfrac{dp}{dt}=k[D(p)-S(p)]$（$k$ 是常数），假设当 $t=0$ 时价格为 1。试求：

(1)需求量等于供给量时的均衡价格 $p_e$；

(2)价格函数 $p(t)$；

(3)极限 $\displaystyle \lim_{t\to\infty}p(t)$。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) $\displaystyle p_e=\sqrt[3]{\dfrac{a}{b}}$；

(2) $\displaystyle p(t)=\sqrt[3]{\dfrac{a}{b}+\left(1-\dfrac{a}{b}\right)e^{-3bkt}}$；

(3) 在通常的价格调节假设 $\displaystyle k>0$ 下，

$$
\displaystyle \lim_{t\to\infty}p(t)=\sqrt[3]{\dfrac{a}{b}}=p_e.
$$

**解析：** (1) 均衡时需求量等于供给量，即

$$
\displaystyle \dfrac{a}{p_e^2}=bp_e.
$$

因此

$$
\displaystyle bp_e^3=a,
$$

从而

$$
\displaystyle p_e=\sqrt[3]{\dfrac{a}{b}}.
$$

(2) 价格满足

$$
\displaystyle \dfrac{dp}{dt}=k\left(\dfrac{a}{p^2}-bp\right).
$$

两边乘以 $\displaystyle p^2$，得

$$
\displaystyle p^2\dfrac{dp}{dt}=k(a-bp^3).
$$

令

$$
\displaystyle y=p^3,
$$

则

$$
\displaystyle \dfrac{dy}{dt}=3p^2\dfrac{dp}{dt}.
$$

故原方程化为一阶线性方程

$$
\displaystyle \dfrac{dy}{dt}+3bky=3ka.
$$

其通解为

$$
\displaystyle y=\dfrac{a}{b}+Ce^{-3bkt}.
$$

由初始条件 $\displaystyle p(0)=1$，即 $\displaystyle y(0)=1$，得

$$
\displaystyle C=1-\dfrac{a}{b}.
$$

因此

$$
\displaystyle p^3(t)=\dfrac{a}{b}+\left(1-\dfrac{a}{b}\right)e^{-3bkt},
$$

所以

$$
\displaystyle p(t)=\sqrt[3]{\dfrac{a}{b}+\left(1-\dfrac{a}{b}\right)e^{-3bkt}}.
$$

(3) 在价格调节模型中，$\displaystyle k$ 为正比例系数，因此 $\displaystyle k>0$。于是

$$
\displaystyle e^{-3bkt}\to0\qquad(t\to\infty),
$$

从而

$$
\displaystyle \lim_{t\to\infty}p(t)
=\sqrt[3]{\dfrac{a}{b}}
=p_e.
$$

</details>

---

4. **【1989-4-9 分】** 设某厂家打算生产一批商品投放市场，已知该商品的需求函数为 $p=p(x)=10e^{-\frac{x}{2}}$，且最大需求量为 6，其中 $x$ 表示需求量，$p$ 表示价格。

(1)求该商品的边际收益函数。

(2)求使收益最大时的产量、最大收益和相应价格。

(3)画出收益函数的图形。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) $\displaystyle MR(x)=10e^{-x/2}\left(1-\dfrac{x}{2}\right)$；

(2) 当 $\displaystyle x=2$ 时收益最大，最大收益为 $\displaystyle \dfrac{20}{e}$，相应价格为 $\displaystyle \dfrac{10}{e}$；

(3) 收益函数为 $\displaystyle R(x)=10xe^{-x/2}$，定义域为 $\displaystyle 0\le x\le6$。其在 $\displaystyle [0,2]$ 上递增，在 $\displaystyle [2,6]$ 上递减；$\displaystyle (4,40e^{-2})$ 为拐点。

**解析：** 收益函数为

$$
\displaystyle R(x)=xp(x)=10xe^{-x/2},\qquad 0\le x\le6.
$$

(1) 边际收益为

$$
\begin{aligned}
\displaystyle MR(x)
&=R'(x)\\
&=10e^{-x/2}-5xe^{-x/2}\\
&=10e^{-x/2}\left(1-\dfrac{x}{2}\right).
\end{aligned}
$$

(2) 由于 $\displaystyle e^{-x/2}>0$，所以

$$
\begin{cases}
\displaystyle R'(x)>0, & 0<x<2,\\
\displaystyle R'(x)=0, & x=2,\\
\displaystyle R'(x)<0, & 2<x\le6.
\end{cases}
$$

因此 $\displaystyle R(x)$ 在 $\displaystyle x=2$ 时取得最大值，且

$$
\displaystyle R_{\max}=R(2)=20e^{-1}=\dfrac{20}{e}.
$$

相应价格为

$$
\displaystyle p(2)=10e^{-1}=\dfrac{10}{e}.
$$

(3) 为确定图形的凹凸性，再求二阶导数：

$$
\begin{aligned}
\displaystyle R''(x)
&=10e^{-x/2}\left(\dfrac{x}{4}-1\right)\\
&=\dfrac{5}{2}(x-4)e^{-x/2}.
\end{aligned}
$$

故

$$
\begin{cases}
\displaystyle R''(x)<0, & 0<x<4,\\
\displaystyle R''(x)>0, & 4<x<6.
\end{cases}
$$

所以曲线在 $\displaystyle (0,4)$ 上为凹弧，在 $\displaystyle (4,6)$ 上为凸弧，拐点为

$$
\displaystyle \left(4,R(4)\right)=\left(4,40e^{-2}\right).
$$

此外，曲线经过

$$
\displaystyle (0,0),\qquad \left(2,\dfrac{20}{e}\right),\qquad \left(6,60e^{-3}\right).
$$

据此即可画出收益函数图形：从原点出发先上升，在 $\displaystyle x=2$ 处达到最高点，随后下降，并在 $\displaystyle x=4$ 处改变凹凸性。

</details>

---

5. **【1989-5-6 分】** 已知某企业的总收入函数为 $R=26x-2x^2-4x^3$，总成本函数为 $C=8x+x^2$，其中 $x$ 表示产品的产量，求利润函数、边际收入函数、边际成本函数，以及企业获得最大利润时的产量和最大利润。

<details>
<summary>查看答案与解析</summary>

**答案：** 利润函数为

$$
\displaystyle L(x)=18x-3x^2-4x^3;
$$

边际收入函数为

$$
\displaystyle MR(x)=26-4x-12x^2;
$$

边际成本函数为

$$
\displaystyle MC(x)=8+2x;
$$

当 $\displaystyle x=1$ 时利润最大，最大利润为 $\displaystyle 11$。

**解析：** 利润等于总收入减去总成本，因此

$$
\begin{aligned}
\displaystyle L(x)
&=R(x)-C(x)\\
&=(26x-2x^2-4x^3)-(8x+x^2)\\
&=18x-3x^2-4x^3.
\end{aligned}
$$

边际收入为

$$
\displaystyle MR(x)=R'(x)=26-4x-12x^2.
$$

边际成本为

$$
\displaystyle MC(x)=C'(x)=8+2x.
$$

利润函数的导数为

$$
\begin{aligned}
\displaystyle L'(x)
&=18-6x-12x^2\\
&=-6(2x^2+x-3)\\
&=-6(2x+3)(x-1).
\end{aligned}
$$

在经济意义下 $\displaystyle x\ge0$。于是

$$
\begin{cases}
\displaystyle L'(x)>0, & 0\le x<1,\\
\displaystyle L'(x)<0, & x>1.
\end{cases}
$$

因此 $\displaystyle x=1$ 为利润最大点。最大利润为

$$
\displaystyle L(1)=18-3-4=11.
$$

也可以由利润最大化条件

$$
\displaystyle MR=MC
$$

得到

$$
\displaystyle 26-4x-12x^2=8+2x,
$$

即

$$
\displaystyle 2x^2+x-3=0,
$$

在 $\displaystyle x\ge0$ 中仍得 $\displaystyle x=1$。

</details>

---

6. **【1991-45-8 分】** 某厂家生产的一种产品同时在两个市场销售，售价分别为 $P_1$ 和 $P_2$，销售量分别为 $q_1$ 和 $q_2$，需求函数分别为 $q_1=24-0.2P_1$ 和 $q_2=10-0.05P_2$，总成本函数为 $C=35+40(q_1+q_2)$。试问：厂家如何确定两个市场的售价，能使其获得的总利润最大？最大总利润为多少？

<details>
<summary>查看答案与解析</summary>

**答案：** 第一市场售价为 $\displaystyle P_1=80$，销售量为 $\displaystyle q_1=8$；

第二市场售价为 $\displaystyle P_2=120$，销售量为 $\displaystyle q_2=4$；

最大总利润为 $\displaystyle 605$。

**解析：** 由需求函数分别解出价格：

$$
\displaystyle P_1=120-5q_1,
$$

$$
\displaystyle P_2=200-20q_2.
$$

两个市场的总收益为

$$
\begin{aligned}
\displaystyle R
&=P_1q_1+P_2q_2\\
&=(120-5q_1)q_1+(200-20q_2)q_2.
\end{aligned}
$$

总利润函数为

$$
\begin{aligned}
\displaystyle L(q_1,q_2)
&=R-C\\
&=(120-5q_1)q_1+(200-20q_2)q_2\\
&\quad-35-40(q_1+q_2)\\
&=80q_1-5q_1^2+160q_2-20q_2^2-35.
\end{aligned}
$$

求一阶偏导数：

$$
\displaystyle L_{q_1}=80-10q_1,
$$

$$
\displaystyle L_{q_2}=160-40q_2.
$$

令其为零，得

$$
\displaystyle q_1=8,\qquad q_2=4.
$$

又因为

$$
\displaystyle L_{q_1q_1}=-10<0,
$$

$$
\displaystyle L_{q_2q_2}=-40<0,
$$

且混合偏导数为零，所以利润函数为严格凹函数，上述驻点即唯一的最大值点。

相应售价为

$$
\displaystyle P_1=120-5\times8=80,
$$

$$
\displaystyle P_2=200-20\times4=120.
$$

最大总利润为

$$
\begin{aligned}
\displaystyle L_{\max}
&=80\times8-5\times8^2+160\times4-20\times4^2-35\\
&=605.
\end{aligned}
$$

</details>

---

7. **【1992-5-6 分】** 设生产某产品的固定成本为 10，而当产量为 $x$ 时的边际成本函数为 $MC=-40-20x+3x^2$，边际收入函数为 $MR=32+10x$。试求：

(1)总利润函数；

(2)使总利润最大的产量。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) 总利润函数为

$$
\displaystyle L(x)=-x^3+15x^2+72x-10;
$$

(2) 使总利润最大的产量为

$$
\displaystyle x=12.
$$

**解析：** 利润的边际函数等于边际收入减去边际成本，因此

$$
\begin{aligned}
\displaystyle L'(x)
&=MR-MC\\
&=(32+10x)-(-40-20x+3x^2)\\
&=72+30x-3x^2.
\end{aligned}
$$

对其积分，得

$$
\displaystyle L(x)=72x+15x^2-x^3+C.
$$

当 $\displaystyle x=0$ 时，尚未生产产品，总收入为 0，而固定成本为 10，所以

$$
\displaystyle L(0)=-10.
$$

因此 $\displaystyle C=-10$，总利润函数为

$$
\displaystyle L(x)=-x^3+15x^2+72x-10.
$$

令

$$
\begin{aligned}
\displaystyle L'(x)
&=-3x^2+30x+72\\
&=-3(x-12)(x+2)=0.
\end{aligned}
$$

得驻点

$$
\displaystyle x=12,\qquad x=-2.
$$

由于产量应满足 $\displaystyle x\ge0$，只需考察 $\displaystyle x=12$。又

$$
\displaystyle L''(x)=30-6x,
$$

所以

$$
\displaystyle L''(12)=-42<0.
$$

故 $\displaystyle x=12$ 时总利润最大。

</details>

---

8. **【1993-5-7 分】** 已知某厂生产$x$件产品的成本为$C=25000+200x+\dfrac{1}{40}x^2$，问：

(1)要使平均成本最小，应生产多少件产品？

(2)若产品以每件500元售出，要使利润最大，应生产多少件产品？

<details>
<summary>查看答案与解析</summary>

**答案：** (1) 应生产 $\displaystyle 1000$ 件产品；

(2) 应生产 $\displaystyle 6000$ 件产品。

**解析：** (1) 当产量为 $\displaystyle x>0$ 时，平均成本为

$$
\displaystyle \overline{C}(x)=\dfrac{C(x)}{x}=\dfrac{25000}{x}+200+\dfrac{x}{40}.
$$

求导得

$$
\displaystyle \overline{C}'(x)=-\dfrac{25000}{x^2}+\dfrac{1}{40}.
$$

令 $\displaystyle \overline{C}'(x)=0$，得

$$
\displaystyle x^2=1000000.
$$

由于 $\displaystyle x>0$，所以

$$
\displaystyle x=1000.
$$

又因为

$$
\displaystyle \overline{C}''(x)=\dfrac{50000}{x^3}>0,
$$

故 $\displaystyle x=1000$ 时平均成本取得最小值。

(2) 产品以每件 $\displaystyle 500$ 元售出时，总收入为

$$
\displaystyle R(x)=500x.
$$

利润函数为

$$
\begin{aligned}
\displaystyle L(x)
&=R(x)-C(x)\\
&=500x-\left(25000+200x+\dfrac{x^2}{40}\right)\\
&=300x-25000-\dfrac{x^2}{40}.
\end{aligned}
$$

于是

$$
\displaystyle L'(x)=300-\dfrac{x}{20}.
$$

令 $\displaystyle L'(x)=0$，得

$$
\displaystyle x=6000.
$$

又

$$
\displaystyle L''(x)=-\dfrac{1}{20}<0,
$$

故 $\displaystyle x=6000$ 时利润最大。

</details>

---

9. **【1993-4-9 分】** 设某产品的成本函数为$C=aq^2+bq+c$，需求函数为$q=\dfrac{1}{e}(d-p)$，其中$C$为成本，$q$为需求量(即产量)，$p$为单价，$a,b,c,d,e$都是正的常数，且$d>b$，求：

(1)利润最大时的产量及最大利润；

(2)需求对价格的弹性；

(3)需求对价格弹性的绝对值为1时的产量。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) 利润最大时的产量为

$$
\displaystyle q^*=\dfrac{d-b}{2(a+e)},
$$

最大利润为

$$
\displaystyle L_{\max}=\dfrac{(d-b)^2}{4(a+e)}-c;
$$

(2) 需求对价格的弹性为

$$
\displaystyle E_p=\dfrac{p}{d-p}=\dfrac{d-eq}{eq};
$$

(3) 当弹性的绝对值为 $\displaystyle 1$ 时，产量为

$$
\displaystyle q=\dfrac{d}{2e}.
$$

**解析：** 由需求函数

$$
\displaystyle q=\dfrac{d-p}{e}
$$

可得反需求函数

$$
\displaystyle p=d-eq.
$$

因此总收入为

$$
\displaystyle R(q)=pq=(d-eq)q=dq-eq^2.
$$

利润函数为

$$
\begin{aligned}
\displaystyle L(q)
&=R(q)-C(q)\\
&=dq-eq^2-(aq^2+bq+c)\\
&=(d-b)q-(a+e)q^2-c.
\end{aligned}
$$

于是

$$
\displaystyle L'(q)=d-b-2(a+e)q.
$$

令 $\displaystyle L'(q)=0$，得

$$
\displaystyle q^*=\dfrac{d-b}{2(a+e)}.
$$

因为 $\displaystyle d>b$，故 $\displaystyle q^*>0$。又

$$
\displaystyle L''(q)=-2(a+e)<0,
$$

所以该点为最大值点。代入得

$$
\begin{aligned}
\displaystyle L_{\max}
&=(d-b)\dfrac{d-b}{2(a+e)}-(a+e)\left(\dfrac{d-b}{2(a+e)}\right)^2-c\\
&=\dfrac{(d-b)^2}{4(a+e)}-c.
\end{aligned}
$$

需求量对价格的导数为

$$
\displaystyle \dfrac{dq}{dp}=-\dfrac{1}{e}.
$$

按需求弹性的通常定义

$$
\displaystyle E_p=-\dfrac{p}{q}\dfrac{dq}{dp},
$$

所以

$$
\displaystyle E_p=\dfrac{p}{eq}=\dfrac{p}{d-p}=\dfrac{d-eq}{eq}.
$$

令 $\displaystyle E_p=1$，则

$$
\displaystyle \dfrac{d-eq}{eq}=1,
$$

从而

$$
\displaystyle d=2eq,
$$

故

$$
\displaystyle q=\dfrac{d}{2e}.
$$

</details>

---

10. **【1995-45-6 分】** 设某产品的需求函数为$Q=Q(P)$，收益函数为$R=PQ$，其中$P$为产品价格，$Q$为需求量(产品的产量)，$Q(P)$是单调减函数，如果当价格为$P_0$，对应的产量为$Q_0$时，边际收益$\dfrac{dR}{dQ}|_{Q=Q_0}=a>0$，收益对价格的边际效应$\dfrac{dR}{dP}|_{P=P_0}=c<0$，需求对价格的弹性为$E_p=b>1$，求$P_0$和$Q_0$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle P_0=\dfrac{ab}{b-1},\qquad Q_0=-\dfrac{c}{b-1}.
$$

</div>
</div>

**解析：** 由于需求函数为单调减函数，需求对价格的弹性取正值时定义为

$$
\displaystyle E_p=-\dfrac{P}{Q}\dfrac{dQ}{dP}.
$$

在 $\displaystyle (P_0,Q_0)$ 处有

$$
\displaystyle -\dfrac{P_0}{Q_0}\left.\dfrac{dQ}{dP}\right|_{P=P_0}=b,
$$

所以

$$
\displaystyle \left.\dfrac{dQ}{dP}\right|_{P=P_0}=-\dfrac{bQ_0}{P_0}.
$$

收益函数为 $\displaystyle R=PQ$，故

$$
\begin{aligned}
\displaystyle \left.\dfrac{dR}{dP}\right|_{P=P_0}
&=Q_0+P_0\left.\dfrac{dQ}{dP}\right|_{P=P_0}\\
&=Q_0-bQ_0\\
&=(1-b)Q_0.
\end{aligned}
$$

由题设该值等于 $\displaystyle c$，因此

$$
\displaystyle Q_0=\dfrac{c}{1-b}=-\dfrac{c}{b-1}.
$$

另一方面，

$$
\displaystyle \left.\dfrac{dP}{dQ}\right|_{Q=Q_0}=-\dfrac{P_0}{bQ_0}.
$$

于是边际收益为

$$
\begin{aligned}
\displaystyle \left.\dfrac{dR}{dQ}\right|_{Q=Q_0}
&=P_0+Q_0\left.\dfrac{dP}{dQ}\right|_{Q=Q_0}\\
&=P_0-\dfrac{P_0}{b}\\
&=P_0\left(1-\dfrac{1}{b}\right).
\end{aligned}
$$

由题设该值等于 $\displaystyle a$，故

$$
\displaystyle P_0\dfrac{b-1}{b}=a,
$$

从而

$$
\displaystyle P_0=\dfrac{ab}{b-1}.
$$

由于 $\displaystyle a>0$、$\displaystyle b>1$、$\displaystyle c<0$，所得 $\displaystyle P_0,Q_0$ 均为正数。

</details>

---

11. **【1996-45-6 分】** 设某种商品的单价为$p$时，售出的商品数量$Q$可以表示成$Q=\dfrac{a}{p+b}-c$，其中$a,b,c$均为正数，且$a>bc$。

(1)求$p$在何范围变化时，使相应销售额增加或减少；

(2)要使销售额最大，商品单价$p$应取何值？最大销售额是多少？

<details>
<summary>查看答案与解析</summary>

**答案：** 在有经济意义的价格范围

$$
\displaystyle 0\le p\le \dfrac{a}{c}-b
$$

内：

(1) 当

$$
\displaystyle 0\le p<\sqrt{\dfrac{ab}{c}}-b
$$

时，销售额随价格增加而增加；当

$$
\displaystyle \sqrt{\dfrac{ab}{c}}-b<p\le \dfrac{a}{c}-b
$$

时，销售额随价格增加而减少；

(2) 最优单价为

$$
\displaystyle p^*=\sqrt{\dfrac{ab}{c}}-b,
$$

最大销售额为

$$
\displaystyle R_{\max}=a+bc-2\sqrt{abc}=\left(\sqrt{a}-\sqrt{bc}\right)^2.
$$

**解析：** 销售额函数为

$$
\begin{aligned}
\displaystyle R(p)
&=pQ\\
&=p\left(\dfrac{a}{p+b}-c\right)\\
&=\dfrac{ap}{p+b}-cp.
\end{aligned}
$$

为了使销售量非负，应有

$$
\displaystyle \dfrac{a}{p+b}-c\ge0,
$$

即

$$
\displaystyle 0\le p\le \dfrac{a}{c}-b.
$$

求导得

$$
\displaystyle R'(p)=\dfrac{ab}{(p+b)^2}-c.
$$

令 $\displaystyle R'(p)=0$，得

$$
\displaystyle (p+b)^2=\dfrac{ab}{c}.
$$

由于 $\displaystyle p+b>0$，故

$$
\displaystyle p^*=\sqrt{\dfrac{ab}{c}}-b.
$$

由 $\displaystyle a>bc$ 可知 $\displaystyle p^*>0$。当

$$
\displaystyle p<p^*
$$

时，$\displaystyle R'(p)>0$；当

$$
\displaystyle p>p^*
$$

时，$\displaystyle R'(p)<0$。因此销售额先增加后减少，且在 $\displaystyle p=p^*$ 时取得最大值。

将 $\displaystyle p^*$ 代入，利用

$$
\displaystyle R(p)=a-\dfrac{ab}{p+b}-cp,
$$

得

$$
\begin{aligned}
\displaystyle R_{\max}
&=a-\dfrac{ab}{\sqrt{ab/c}}-c\left(\sqrt{\dfrac{ab}{c}}-b\right)\\
&=a-\sqrt{abc}-\sqrt{abc}+bc\\
&=a+bc-2\sqrt{abc}\\
&=\left(\sqrt{a}-\sqrt{bc}\right)^2.
\end{aligned}
$$

</details>

---

12. **【1997-4-7 分】** 假设某种商品的需求量$Q$是单价$p$(单位：元)的函数：$Q=12000-80p$，商品的总成本是需求量的函数：$C=25000+50Q$，每单位商品需要纳税2元，试求使销售利润最大的商品单价和最大利润额。

<details>
<summary>查看答案与解析</summary>

**答案：** 使销售利润最大的商品单价为

$$
\displaystyle p=101\text{ 元},
$$

最大利润额为

$$
\displaystyle 167080\text{ 元}.
$$

**解析：** 每单位商品需纳税 $\displaystyle 2$ 元，因此总税额为 $\displaystyle 2Q$。销售利润为

$$
\begin{aligned}
\displaystyle L
&=pQ-C-2Q\\
&=pQ-(25000+50Q)-2Q\\
&=(p-52)Q-25000.
\end{aligned}
$$

代入需求函数 $\displaystyle Q=12000-80p$，得

$$
\begin{aligned}
\displaystyle L(p)
&=(p-52)(12000-80p)-25000\\
&=-80p^2+16160p-649000.
\end{aligned}
$$

于是

$$
\displaystyle L'(p)=-160p+16160.
$$

令 $\displaystyle L'(p)=0$，得

$$
\displaystyle p=101.
$$

又

$$
\displaystyle L''(p)=-160<0,
$$

故 $\displaystyle p=101$ 元时利润最大。

此时需求量为

$$
\displaystyle Q=12000-80\times101=3920.
$$

最大利润为

$$
\begin{aligned}
\displaystyle L_{\max}
&=(101-52)\times3920-25000\\
&=167080\text{ 元}.
\end{aligned}
$$

</details>

---

13. **【1997-3-6 分】** 一商家销售某种商品的价格满足关系$p=7-0.2x$(万元/吨)，$x$为销售量(单位：吨)，商品的成本函数$C=3x+1$(万元)。
(1)若每销售一吨商品，政府要征税$t$(万元)，求该商家获最大利润时的销售量；
(2)$t$为何值时，政府税收总额最大。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) 当 $\displaystyle 0\leq t<4$ 时，商家获最大利润时的销售量为

$$
\displaystyle x=10-\dfrac{5}{2}t.
$$

当 $\displaystyle t\geq4$ 时，最优销售量为 $\displaystyle x=0$。

(2) 当

$$
\displaystyle t=2\text{ 万元/吨}
$$

时，政府税收总额最大，最大税收为 $\displaystyle 10$ 万元。

**解析：** 销售收入为

$$
\displaystyle R(x)=px=(7-0.2x)x.
$$

每销售一吨征税 $\displaystyle t$ 万元，故总税额为 $\displaystyle tx$。商家的利润函数为

$$
\begin{aligned}
\displaystyle L(x)
&=R(x)-C(x)-tx\\
&=(7-0.2x)x-(3x+1)-tx\\
&=-0.2x^2+(4-t)x-1.
\end{aligned}
$$

于是

$$
\displaystyle L'(x)=-0.4x+4-t,
$$

且

$$
\displaystyle L''(x)=-0.4<0.
$$

当 $\displaystyle 0\leq t<4$ 时，令 $\displaystyle L'(x)=0$，得

$$
\displaystyle x^*=\dfrac{4-t}{0.4}=10-\dfrac{5}{2}t.
$$

由于二阶导数恒为负，$\displaystyle x^*$ 为唯一的最大利润点。

当 $\displaystyle t\geq4$ 时，对任意 $\displaystyle x>0$ 都有 $\displaystyle L'(x)<0$，故利润随销售量增加而减少，最优销售量为 $\displaystyle x=0$。

政府税收总额为

$$
\begin{aligned}
\displaystyle T(t)
&=tx^*\\
&=t\left(10-\dfrac{5}{2}t\right)\\
&=10t-\dfrac{5}{2}t^2,
\qquad 0\leq t\leq4.
\end{aligned}
$$

因此

$$
\displaystyle T'(t)=10-5t,
\qquad
T''(t)=-5<0.
$$

令 $\displaystyle T'(t)=0$，得 $\displaystyle t=2$。此时

$$
\displaystyle x^*=10-\dfrac{5}{2}\times2=5,
$$

故最大税收为

$$
\displaystyle T_{\max}=2\times5=10\text{ 万元}.
$$

</details>

---

14. **【1997-3-6 分】** 在经济学中，称函数$Q(x)=A[\delta K^{-x}+(1-\delta) L^{-x}]^{-\dfrac{1}{x}}$为固定替代弹性生产函数，而称函数$\bar{Q}=A K^{\delta} L^{1-\delta}$为C-D生产函数(简称C-D生产函数)。试证明：当$x \to 0$时，固定替代弹性生产函数变为C-D生产函数，即有$\lim_{x \to 0} Q(x)=\bar{Q}$。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \lim_{x\to0}Q(x)=AK^{\delta}L^{1-\delta}=\bar Q.
$$

</div>
</div>

**解析：** 设

$$
\displaystyle S(x)=\delta K^{-x}+(1-\delta)L^{-x}.
$$

则

$$
\displaystyle Q(x)=A[S(x)]^{-1/x}.
$$

由于

$$
\displaystyle S(0)=\delta+(1-\delta)=1,
$$

故

$$
\displaystyle \ln\dfrac{Q(x)}{A}=-\dfrac{\ln S(x)}{x}.
$$

当 $\displaystyle x\to0$ 时，上式右端为 $\displaystyle \dfrac{0}{0}$ 型。由洛必达法则，

$$
\begin{aligned}
\displaystyle \lim_{x\to0}\ln\dfrac{Q(x)}{A}
&=\lim_{x\to0}\left(-\dfrac{S'(x)}{S(x)}\right)\\
&=\lim_{x\to0}
\dfrac{\delta(\ln K)K^{-x}+(1-\delta)(\ln L)L^{-x}}
{\delta K^{-x}+(1-\delta)L^{-x}}\\
&=\delta\ln K+(1-\delta)\ln L.
\end{aligned}
$$

于是

$$
\begin{aligned}
\displaystyle \lim_{x\to0}\dfrac{Q(x)}{A}
&=\exp\left[\delta\ln K+(1-\delta)\ln L\right]\\
&=K^{\delta}L^{1-\delta}.
\end{aligned}
$$

因此

$$
\displaystyle \lim_{x\to0}Q(x)=AK^{\delta}L^{1-\delta}=\bar Q.
$$

</details>

---

15. **【1998-34-6 分】** 设某酒厂有一批新酿的好酒，如果现在(假定$t=0$)就售出，总收入为$R_0$(元)，如果窖藏起来，待来日按陈酒价格出售，$t$年末总收入为$R=R_0 e^{\dfrac{2}{5}\sqrt{t}}$。假定银行的年利率为$r$，并以连续复利计息，试求窖藏多少年售出可使总收入的现值最大，并求$r=0.06$时的$t$值。

<details>
<summary>查看答案与解析</summary>

**答案：** 当

$$
\displaystyle t=\dfrac{1}{25r^2}
$$

时售出，可使总收入的现值最大。

当 $\displaystyle r=0.06$ 时，

$$
\displaystyle t=\dfrac{100}{9}\approx11.11\text{ 年}.
$$

**解析：** 连续复利率为 $\displaystyle r$ 时，$\displaystyle t$ 年末收入的现值为

$$
\begin{aligned}
\displaystyle V(t)
&=R(t)e^{-rt}\\
&=R_0e^{\frac{2}{5}\sqrt t-rt},
\qquad t\geq0.
\end{aligned}
$$

由于指数函数严格递增，只需使

$$
\displaystyle \varphi(t)=\dfrac{2}{5}\sqrt t-rt
$$

取得最大值。

对 $\displaystyle t>0$，有

$$
\displaystyle \varphi'(t)=\dfrac{1}{5\sqrt t}-r.
$$

令 $\displaystyle \varphi'(t)=0$，得

$$
\displaystyle \sqrt t=\dfrac{1}{5r},
$$

即

$$
\displaystyle t^*=\dfrac{1}{25r^2}.
$$

又

$$
\displaystyle \varphi''(t)=-\dfrac{1}{10t^{3/2}}<0,
$$

故 $\displaystyle t^*$ 为极大值点。并且当 $\displaystyle t\to+\infty$ 时，$\displaystyle \varphi(t)\to-\infty$，故该极大值也是全局最大值。

当 $\displaystyle r=0.06$ 时，

$$
\displaystyle t^*=\dfrac{1}{25(0.06)^2}
=\dfrac{1}{0.09}
=\dfrac{100}{9}
\approx11.11.
$$

</details>

---

16. **【2000-34-6 分】** 假设某企业在两个相互分割的市场出售同一种产品，两个市场的需求函数分别是$p_1=18-2Q_1$，$p_2=12-Q_2$，其中$p_1$和$p_2$分别表示该产品在两个市场的销售量(即需求量，单位：吨)，并且该企业生产这种产品的总成本函数是$C=2Q+5$，其中$Q$表示该产品在两个市场的销售总量，即$Q=Q_1+Q_2$。
(1)如果该企业实行价格差别策略，试确定两个市场上该产品的销售量和价格，使该企业获得最大利润；
(2)如果该企业实行价格无差别策略，试确定两个市场上该产品的销售量及其统一的价格，使该企业的总利润最大化；并比较两种价格策略下的总利润大小。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) 实行价格差别策略时，

$$
\displaystyle Q_1=4,\quad p_1=10;
\qquad
Q_2=5,\quad p_2=7,
$$

最大利润为

$$
\displaystyle \pi_d=52.
$$

(2) 实行统一价格策略时，统一价格为

$$
\displaystyle p=8,
$$

两个市场的销售量分别为

$$
\displaystyle Q_1=5,
\qquad
Q_2=4,
$$

最大利润为

$$
\displaystyle \pi_u=49.
$$

因此价格差别策略比统一价格策略多获利润

$$
\displaystyle 52-49=3.
$$

**解析：** (1) 实行价格差别策略时，总利润为

$$
\begin{aligned}
\displaystyle \pi(Q_1,Q_2)
&=p_1Q_1+p_2Q_2-C\\
&=(18-2Q_1)Q_1+(12-Q_2)Q_2\\
&\quad-2(Q_1+Q_2)-5\\
&=16Q_1-2Q_1^2+10Q_2-Q_2^2-5.
\end{aligned}
$$

一阶偏导数为

$$
\displaystyle \pi_{Q_1}=16-4Q_1,
\qquad
\pi_{Q_2}=10-2Q_2.
$$

令其均为零，得

$$
\displaystyle Q_1=4,
\qquad
Q_2=5.
$$

由于

$$
\displaystyle \pi_{Q_1Q_1}=-4<0,
\qquad
\pi_{Q_2Q_2}=-2<0,
\qquad
\pi_{Q_1Q_2}=0,
$$

Hesse 矩阵负定，故该点为唯一最大值点。

相应价格为

$$
\displaystyle p_1=18-2\times4=10,
$$

$$
\displaystyle p_2=12-5=7.
$$

最大利润为

$$
\begin{aligned}
\displaystyle \pi_d
&=10\times4+7\times5-[2(4+5)+5]\\
&=75-23\\
&=52.
\end{aligned}
$$

(2) 实行统一价格策略。若两个市场均有销售，则 $\displaystyle 0\leq p\leq12$，并且

$$
\displaystyle Q_1=\dfrac{18-p}{2},
\qquad
Q_2=12-p.
$$

总销售量为

$$
\displaystyle Q=Q_1+Q_2=21-\dfrac{3}{2}p.
$$

利润函数为

$$
\begin{aligned}
\displaystyle \pi(p)
&=pQ-[2Q+5]\\
&=(p-2)\left(21-\dfrac{3}{2}p\right)-5\\
&=-\dfrac{3}{2}p^2+24p-47.
\end{aligned}
$$

于是

$$
\displaystyle \pi'(p)=-3p+24.
$$

令 $\displaystyle \pi'(p)=0$，得

$$
\displaystyle p=8.
$$

又

$$
\displaystyle \pi''(p)=-3<0,
$$

故 $\displaystyle p=8$ 时利润最大。此时

$$
\displaystyle Q_1=\dfrac{18-8}{2}=5,
\qquad
Q_2=12-8=4.
$$

最大利润为

$$
\displaystyle \pi_u=(8-2)(5+4)-5=49.
$$

还需考察只向第一个市场销售的情形。若 $\displaystyle 12<p\leq18$，则

$$
\displaystyle Q_1=\dfrac{18-p}{2},
\qquad
Q_2=0.
$$

此时

$$
\displaystyle \pi_1(p)=\dfrac{(p-2)(18-p)}{2}-5,
$$

且在 $\displaystyle p\geq12$ 上

$$
\displaystyle \pi_1'(p)=10-p<0.
$$

故该区间最大利润在 $\displaystyle p=12$ 处取得，仅为 $\displaystyle 25<49$。因此统一价格策略的全局最优解确为 $\displaystyle p=8$。

比较得

$$
\displaystyle \pi_d=52>49=\pi_u.
$$

</details>

---

17. **【2001-4-7 分】** 某商品进价为$a$(元/件)，根据以往经验，当销售价为$b$(元/件)时，销售量为$c$件（$a,b,c$均为正常数，且$b \geq \dfrac{4}{3}a$），市场调查表明，销售价每下降10%，销售量可增加40%，现决定一次性降价。试问，当销售价定为多少时，可获得最大利润？并求出最大利润。

<details>
<summary>查看答案与解析</summary>

**答案：** 当销售价定为

$$
\displaystyle p=\dfrac{5}{8}b+\dfrac{1}{2}a
$$

时，可获得最大利润，最大利润为

$$
\displaystyle L_{\max}=\dfrac{c(5b-4a)^2}{16b}\text{ 元}.
$$

**解析：** 设降价后增加的销售量为 $\displaystyle x$ 件。由“销售价每下降 $\displaystyle 10\%$，销售量增加 $\displaystyle 40\%$”可知，价格下降量与销售增加量成线性关系。

当销售量增加 $\displaystyle 0.4c$ 件时，价格下降 $\displaystyle 0.1b$ 元，因此

$$
\displaystyle \dfrac{\text{价格下降量}}{\text{销售增加量}}
=\dfrac{0.1b}{0.4c}
=\dfrac{b}{4c}.
$$

故销售量增加 $\displaystyle x$ 件时，销售价格为

$$
\displaystyle p=b-\dfrac{b}{4c}x.
$$

此时总销售量为 $\displaystyle c+x$，利润函数为

$$
\begin{aligned}
\displaystyle L(x)
&=(p-a)(c+x)\\
&=\left(b-\dfrac{b}{4c}x-a\right)(c+x).
\end{aligned}
$$

求导得

$$
\begin{aligned}
\displaystyle L'(x)
&=b-a-\dfrac{b}{4c}x-\dfrac{b}{4c}(c+x)\\
&=\dfrac{3b}{4}-a-\dfrac{b}{2c}x.
\end{aligned}
$$

令 $\displaystyle L'(x)=0$，得

$$
\displaystyle x^*=\dfrac{c(3b-4a)}{2b}.
$$

由 $\displaystyle b\geq\dfrac{4}{3}a$，知 $\displaystyle x^*\geq0$。又

$$
\displaystyle L''(x)=-\dfrac{b}{2c}<0,
$$

故 $\displaystyle x^*$ 为最大利润点。

相应销售价格为

$$
\begin{aligned}
\displaystyle p^*
&=b-\dfrac{b}{4c}x^*\\
&=b-\dfrac{3b-4a}{8}\\
&=\dfrac{5}{8}b+\dfrac{1}{2}a.
\end{aligned}
$$

此时每件利润为

$$
\begin{aligned}
\displaystyle p^*-a
&=\dfrac{5b+4a}{8}-a\\
&=\dfrac{5b-4a}{8},
\end{aligned}
$$

总销售量为

$$
\begin{aligned}
\displaystyle c+x^*
&=c+\dfrac{c(3b-4a)}{2b}\\
&=\dfrac{c(5b-4a)}{2b}.
\end{aligned}
$$

因此最大利润为

$$
\begin{aligned}
\displaystyle L_{\max}
&=(p^*-a)(c+x^*)\\
&=\dfrac{5b-4a}{8}\cdot\dfrac{c(5b-4a)}{2b}\\
&=\dfrac{c(5b-4a)^2}{16b}\text{ 元}.
\end{aligned}
$$

</details>

---

18. **【2002-4-7 分】** 设某商品的需求量$Q$是价格$p$的单调减少函数：$Q=Q(p)$，其需求弹性$\displaystyle \eta=\dfrac{2p^2}{192-p^2}>0$。

(1)设$R$为总收益函数，证明$\displaystyle \dfrac{dR}{dp}=Q(1-\eta)$；

(2)求$p=6$时，总收益对价格的弹性，并说明其经济意义。

<details>
<summary>查看答案与解析</summary>

**答案：** (1)

$$
\displaystyle \dfrac{dR}{dp}=Q(1-\eta).
$$

(2)当 $\displaystyle p=6$ 时，总收益对价格的弹性为

$$
\displaystyle E_R=\dfrac{7}{13}.
$$

其经济意义是：在 $\displaystyle p=6$ 附近，价格每提高 $\displaystyle 1\%$，总收益约提高 $\displaystyle \dfrac{7}{13}\%$；价格每降低 $\displaystyle 1\%$，总收益约降低 $\displaystyle \dfrac{7}{13}\%$。

**解析：** 由于需求量 $\displaystyle Q=Q(p)$ 随价格单调减少，需求弹性定义为

$$
\displaystyle \eta=-\dfrac{p}{Q}\dfrac{dQ}{dp}.
$$

总收益为

$$
\displaystyle R=pQ(p).
$$

因此

$$
\begin{aligned}
\displaystyle \dfrac{dR}{dp}
&=Q+p\dfrac{dQ}{dp}\\
&=Q\left(1+\dfrac{p}{Q}\dfrac{dQ}{dp}\right)\\
&=Q(1-\eta).
\end{aligned}
$$

故(1)得证。

总收益对价格的弹性为

$$
\displaystyle E_R=\dfrac{p}{R}\dfrac{dR}{dp}.
$$

由 $\displaystyle R=pQ$ 及(1)，得

$$
\begin{aligned}
\displaystyle E_R
&=\dfrac{p}{pQ}\cdot Q(1-\eta)\\
&=1-\eta.
\end{aligned}
$$

当 $\displaystyle p=6$ 时，

$$
\displaystyle \eta(6)=\dfrac{2\times6^2}{192-6^2}
=\dfrac{72}{156}
=\dfrac{6}{13}.
$$

所以

$$
\displaystyle E_R=1-\dfrac{6}{13}=\dfrac{7}{13}.
$$

由于 $\displaystyle E_R>0$，总收益与价格在该点附近同方向变动。

</details>

---

19. **【2003-4-8 分】** 设某商品从时刻0到时刻$t$的销售量为$x(t)=kt$，$t \in [0,T]$，$(k>0)$。欲在$T$时将数量为$A$的该商品销售完，试求：

(1)$t$时的商品剩余量，并确定$k$的值；

(2)在时间段$[0,T]$上的平均剩余量。

<details>
<summary>查看答案与解析</summary>

**答案：** (1)

$$
\displaystyle k=\dfrac{A}{T},\qquad S(t)=A\left(1-\dfrac{t}{T}\right).
$$

(2)平均剩余量为

$$
\displaystyle \overline{S}=\dfrac{A}{2}.
$$

**解析：** 由题意，$\displaystyle x(t)=kt$ 表示从时刻 $\displaystyle 0$ 到时刻 $\displaystyle t$ 的累计销售量。

因此，时刻 $\displaystyle t$ 的剩余量为

$$
\displaystyle S(t)=A-x(t)=A-kt.
$$

在 $\displaystyle T$ 时商品恰好销售完，故

$$
\displaystyle x(T)=kT=A,
$$

从而

$$
\displaystyle k=\dfrac{A}{T}.
$$

因此

$$
\displaystyle S(t)=A-\dfrac{A}{T}t
=A\left(1-\dfrac{t}{T}\right).
$$

时间段 $\displaystyle [0,T]$ 上的平均剩余量为

$$
\begin{aligned}
\displaystyle \overline{S}
&=\dfrac{1}{T}\int_0^T S(t)\,dt\\
&=\dfrac{1}{T}\int_0^T A\left(1-\dfrac{t}{T}\right)dt\\
&=\dfrac{A}{T}\left[t-\dfrac{t^2}{2T}\right]_0^T\\
&=\dfrac{A}{T}\cdot\dfrac{T}{2}\\
&=\dfrac{A}{2}.
\end{aligned}
$$

</details>

---

20. **【2004-34-9 分】** 设某商品的需求函数为$Q=100-5P$，其中价格$P \in (0,20)$，$Q$为需求量。

(1)求需求量对价格的弹性$E_d(E_d>0)$；

(2)推导$\displaystyle \dfrac{dR}{dP}=Q(1-E_d)$（其中$R$为收益），并用弹性$E_d$说明价格在何范围内变化时，降低价格反而使收益增加。

<details>
<summary>查看答案与解析</summary>

**答案：** (1)

$$
\displaystyle E_d=\dfrac{P}{20-P}.
$$

(2)

$$
\displaystyle \dfrac{dR}{dP}=Q(1-E_d).
$$

当

$$
\displaystyle 10<P<20
$$

时，降低价格会使收益增加。

**解析：** 需求量对价格的弹性定义为

$$
\displaystyle E_d=-\dfrac{P}{Q}\dfrac{dQ}{dP}.
$$

由

$$
\displaystyle Q=100-5P
$$

得

$$
\displaystyle \dfrac{dQ}{dP}=-5.
$$

因此

$$
\begin{aligned}
\displaystyle E_d
&=-\dfrac{P}{100-5P}(-5)\\
&=\dfrac{5P}{100-5P}\\
&=\dfrac{P}{20-P}.
\end{aligned}
$$

总收益为

$$
\displaystyle R=PQ(P).
$$

故

$$
\begin{aligned}
\displaystyle \dfrac{dR}{dP}
&=Q+P\dfrac{dQ}{dP}\\
&=Q\left(1+\dfrac{P}{Q}\dfrac{dQ}{dP}\right)\\
&=Q(1-E_d).
\end{aligned}
$$

若降低价格时收益反而增加，则当 $\displaystyle dP<0$ 时应有 $\displaystyle dR>0$，因而需要

$$
\displaystyle \dfrac{dR}{dP}<0.
$$

又因 $\displaystyle Q>0$，故

$$
\displaystyle 1-E_d<0,
$$

即

$$
\displaystyle E_d>1.
$$

代入 $\displaystyle E_d=\dfrac{P}{20-P}$，得

$$
\displaystyle \dfrac{P}{20-P}>1.
$$

由于 $\displaystyle 0<P<20$，分母为正，于是

$$
\displaystyle P>10.
$$

故当 $\displaystyle 10<P<20$ 时，降低价格会使收益增加。

</details>

---

21. **【2012-3-10 分】** 某企业为生产甲、乙两种型号的产品投入的固定成本为10000(万元)，设该企业生产甲、乙两种产品的产量分别为$x$(件)和$y$(件)，且这两种产品的边际成本分别为$20+\dfrac{x}{2}$(万元/件)与$6+y$(万元/件)。

(Ⅰ)求生产甲乙两种产品的总成本函数$C(x,y)$(万元)；

(Ⅱ)当总产量为50件时，甲、乙两种产品的产量各为多少时可使总成本最小，求最小总成本；

(Ⅲ)求总产量为50件且总成本最小时甲产品的边际成本，并解释其经济意义。

<details>
<summary>查看答案与解析</summary>

**答案：** (Ⅰ)

$$
\displaystyle C(x,y)=10000+20x+\dfrac{x^2}{4}+6y+\dfrac{y^2}{2}.
$$

(Ⅱ)当

$$
\displaystyle x=24,\qquad y=26
$$

时，总成本最小，最小总成本为

$$
\displaystyle C_{\min}=11118\text{ 万元}.
$$

(Ⅲ)甲产品的边际成本为

$$
\displaystyle MC_x=32\text{ 万元/件}.
$$

其经济意义是：在该最优产量附近，甲产品产量增加1件时，总成本约增加32万元；同时甲、乙两种产品的边际成本相等，无法再通过调整两种产品的产量结构降低总成本。

**解析：** 由边际成本的定义，

$$
\displaystyle \dfrac{\partial C}{\partial x}=20+\dfrac{x}{2},
\qquad
\dfrac{\partial C}{\partial y}=6+y.
$$

先对 $\displaystyle x$ 积分，得

$$
\displaystyle C(x,y)=20x+\dfrac{x^2}{4}+\varphi(y).
$$

再由

$$
\displaystyle \dfrac{\partial C}{\partial y}=\varphi'(y)=6+y
$$

可得

$$
\displaystyle \varphi(y)=6y+\dfrac{y^2}{2}+C_0.
$$

固定成本为 $\displaystyle 10000$ 万元，即

$$
\displaystyle C(0,0)=10000,
$$

故 $\displaystyle C_0=10000$。于是

$$
\displaystyle C(x,y)=10000+20x+\dfrac{x^2}{4}+6y+\dfrac{y^2}{2}.
$$

当总产量为 $\displaystyle 50$ 件时，约束为

$$
\displaystyle x+y=50.
$$

令 $\displaystyle y=50-x$，则

$$
\begin{aligned}
\displaystyle C(x,50-x)
&=10000+20x+\dfrac{x^2}{4}+6(50-x)+\dfrac{(50-x)^2}{2}.
\end{aligned}
$$

求导得

$$
\begin{aligned}
\displaystyle \dfrac{dC}{dx}
&=20+\dfrac{x}{2}-6-(50-x)\\
&=\dfrac{3x}{2}-36.
\end{aligned}
$$

令 $\displaystyle \dfrac{dC}{dx}=0$，得

$$
\displaystyle x=24,
\qquad
y=50-24=26.
$$

又

$$
\displaystyle \dfrac{d^2C}{dx^2}=\dfrac{3}{2}>0,
$$

故此时总成本最小。

最小总成本为

$$
\begin{aligned}
\displaystyle C_{\min}
&=10000+20\times24+\dfrac{24^2}{4}+6\times26+\dfrac{26^2}{2}\\
&=10000+480+144+156+338\\
&=11118\text{ 万元}.
\end{aligned}
$$

此时甲产品的边际成本为

$$
\displaystyle MC_x=20+\dfrac{24}{2}=32\text{ 万元/件}.
$$

同时乙产品的边际成本为

$$
\displaystyle MC_y=6+26=32\text{ 万元/件}.
$$

二者相等，这也说明在总产量固定时，当前产量分配已达到成本最小状态。

</details>

---

22. **【2013-3-10 分】** 设生产某产品的固定成本为60000元，可变成本为20元/件，价格函数为$P=60-\dfrac{Q}{1000}$，($P$是单价，单位：元，$Q$是销量，单位：件)，已知产销平衡，求：

(1)该商品的边际利润。

(2)当$P=50$时的边际利润，并解释其经济意义。

(3)使得利润最大的定价$P$。

<details>
<summary>查看答案与解析</summary>

**答案：** (1)边际利润为

$$
\displaystyle L'(Q)=40-\dfrac{Q}{500}\text{ 元/件}.
$$

(2)当 $\displaystyle P=50$ 时，

$$
\displaystyle L'(Q)=20\text{ 元/件}.
$$

其经济意义是：在当前产销量附近，每增加销售1件产品，企业利润约增加20元。

(3)使利润最大的定价为

$$
\displaystyle P=40\text{ 元}.
$$

**解析：** 总收益为

$$
\begin{aligned}
\displaystyle R(Q)
&=PQ\\
&=\left(60-\dfrac{Q}{1000}\right)Q\\
&=60Q-\dfrac{Q^2}{1000}.
\end{aligned}
$$

总成本为

$$
\displaystyle C(Q)=60000+20Q.
$$

因此利润函数为

$$
\begin{aligned}
\displaystyle L(Q)
&=R(Q)-C(Q)\\
&=40Q-\dfrac{Q^2}{1000}-60000.
\end{aligned}
$$

故边际利润为

$$
\displaystyle L'(Q)=40-\dfrac{Q}{500}.
$$

当 $\displaystyle P=50$ 时，由价格函数

$$
\displaystyle 50=60-\dfrac{Q}{1000}
$$

得

$$
\displaystyle Q=10000.
$$

于是

$$
\displaystyle L'(10000)=40-\dfrac{10000}{500}=20\text{ 元/件}.
$$

为求利润最大值，令

$$
\displaystyle L'(Q)=0,
$$

即

$$
\displaystyle 40-\dfrac{Q}{500}=0,
$$

解得

$$
\displaystyle Q=20000.
$$

又

$$
\displaystyle L''(Q)=-\dfrac{1}{500}<0,
$$

故此时利润取得最大值。

对应价格为

$$
\displaystyle P=60-\dfrac{20000}{1000}=40\text{ 元}.
$$

</details>

---

23. **【2015-3-10 分】** 为了实现利润最大化，厂商需要对某商品确定其定价模型，设$Q$为该商品的需求量，$p$为价格，$MC$为边际成本，$\eta$为需求弹性$(\eta>0)$。

(1)证明定价模型为$p=\dfrac{MC}{1-\dfrac{1}{\eta}}$；

(2)若该商品的成本函数为$C(Q)=1600+Q^2$，需求函数为$Q=40-p$，试由(1)中的定价模型确定此商品的价格。

<details>
<summary>查看答案与解析</summary>

**答案：** (1)利润最大化定价模型为

$$
\displaystyle p=\dfrac{MC}{1-\dfrac{1}{\eta}}.
$$

(2)该商品的最优价格为

$$
\displaystyle p=30.
$$

**解析：** (1)设利润函数为

$$
\displaystyle L(Q)=p(Q)Q-C(Q).
$$

利润最大时应满足一阶必要条件

$$
\displaystyle L'(Q)=0,
$$

即

$$
\displaystyle p+Q\dfrac{dp}{dQ}-MC=0.
$$

按需求弹性的通常定义，

$$
\displaystyle \eta=-\dfrac{p}{Q}\dfrac{dQ}{dp}>0.
$$

由此得

$$
\displaystyle \dfrac{dQ}{dp}=-\dfrac{\eta Q}{p},
$$

从而

$$
\displaystyle \dfrac{dp}{dQ}=-\dfrac{p}{\eta Q}.
$$

代入利润最大化的一阶条件，得

$$
\begin{aligned}
\displaystyle MC
&=p+Q\dfrac{dp}{dQ}\\
&=p-Q\dfrac{p}{\eta Q}\\
&=p\left(1-\dfrac{1}{\eta}\right).
\end{aligned}
$$

因此

$$
\boxed{\displaystyle p=\dfrac{MC}{1-\dfrac{1}{\eta}}}.
$$

这也表明，要使最优价格为正且高于边际成本，通常需要

$$
\displaystyle \eta>1.
$$

(2)由成本函数

$$
\displaystyle C(Q)=1600+Q^2
$$

得边际成本

$$
\displaystyle MC=C'(Q)=2Q.
$$

由需求函数

$$
\displaystyle Q=40-p
$$

可得

$$
\displaystyle \dfrac{dQ}{dp}=-1,
$$

故需求弹性为

$$
\displaystyle \eta=-\dfrac{p}{Q}\dfrac{dQ}{dp}=\dfrac{p}{Q}.
$$

将其代入定价模型，得

$$
\displaystyle p=\dfrac{2Q}{1-\dfrac{Q}{p}}.
$$

整理得

$$
\begin{aligned}
\displaystyle p\left(1-\dfrac{Q}{p}\right)&=2Q,\\
p-Q&=2Q,\\
p&=3Q.
\end{aligned}
$$

又由

$$
\displaystyle p+Q=40,
$$

解得

$$
\displaystyle Q=10,
\qquad
p=30.
$$

因此该商品的最优价格为

$$
\boxed{\displaystyle p=30}.
$$

</details>

---

24. **【2016-3-10 分】** 设某商品的最大需求量为1200件，该商品的需求函数$Q=Q(p)$，需求弹性$\eta=\dfrac{p}{120-p}(\eta>0)$，$p$为单价(万元)。

(1)求需求函数的表达式；

(2)求$p=100$万元时的边际收益，并说明其经济意义。

<details>
<summary>查看答案与解析</summary>

**答案：** (1)需求函数为

$$
\displaystyle Q=10(120-p)=1200-10p,
\qquad 0\leq p<120.
$$

(2)当 $\displaystyle p=100$ 万元时，边际收益为

$$
\displaystyle MR=80\text{ 万元/件}.
$$

其经济意义是：当单价为100万元、需求量为200件时，在当前产销量附近每增加销售1件商品，总收益约增加80万元。

**解析：** (1)由需求弹性的定义

$$
\displaystyle \eta=-\dfrac{p}{Q}\dfrac{dQ}{dp}
$$

以及题设

$$
\displaystyle \eta=\dfrac{p}{120-p},
$$

得

$$
\displaystyle -\dfrac{p}{Q}\dfrac{dQ}{dp}=\dfrac{p}{120-p}.
$$

当 $\displaystyle p>0$ 时，约去 $\displaystyle p$，得

$$
\displaystyle \dfrac{1}{Q}\dfrac{dQ}{dp}=-\dfrac{1}{120-p}.
$$

分离变量并积分：

$$
\displaystyle \int\dfrac{dQ}{Q}=-\int\dfrac{dp}{120-p},
$$

故

$$
\displaystyle \ln Q=\ln(120-p)+C,
$$

从而

$$
\displaystyle Q=A(120-p),
$$

其中 $\displaystyle A>0$ 为常数。

商品的最大需求量为1200件。价格最低取 $\displaystyle p=0$ 时需求量最大，因此

$$
\displaystyle Q(0)=120A=1200,
$$

解得

$$
\displaystyle A=10.
$$

所以需求函数为

$$
\boxed{\displaystyle Q=10(120-p)=1200-10p}.
$$

(2)当

$$
\displaystyle p=100
$$

时，需求量为

$$
\displaystyle Q=1200-10\times100=200.
$$

由需求函数解出反需求函数

$$
\displaystyle p=120-\dfrac{Q}{10}.
$$

总收益为

$$
\begin{aligned}
\displaystyle R(Q)
&=pQ\\
&=\left(120-\dfrac{Q}{10}\right)Q\\
&=120Q-\dfrac{Q^2}{10}.
\end{aligned}
$$

因此边际收益为

$$
\displaystyle MR=R'(Q)=120-\dfrac{Q}{5}.
$$

在 $\displaystyle Q=200$ 时，

$$
\displaystyle MR=120-\dfrac{200}{5}=80\text{ 万元/件}.
$$

故

$$
\boxed{\displaystyle MR=80\text{ 万元/件}}.
$$

</details>
