---
title: "三、极值和拐点"
sidebar_position: 3
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 三、极值和拐点

## 小题

### （一）对概念的直接考查

1. **【1987-3-5 分】** 判断正误：$\displaystyle g''(c)$存在，$\displaystyle g'(c)=0,g''(c)<0\Rightarrow g(c)$是极大值。

<details>
<summary>查看答案与解析</summary>

**答案：** 正确。

**解析：** 由二阶导数判别法，若函数在点 $\displaystyle c$ 的某邻域内有定义，且

$$
\displaystyle
g'(c)=0,\qquad g''(c)<0,
$$

则 $\displaystyle g'(x)$ 在 $\displaystyle c$ 的左侧为正、右侧为负，即函数在 $\displaystyle c$ 左侧递增、右侧递减。

因此 $\displaystyle g(c)$ 是函数 $\displaystyle g(x)$ 的极大值，故命题正确。

</details>

---

2. **【1987-12-3 分】** $\displaystyle y=x2^x$在$\displaystyle x=$____处极小值。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
x=-\dfrac{1}{\ln2}.
$$

</div>
</div>

**解析：** 设

$$
\displaystyle
f(x)=x2^x.
$$

求导得

$$
\displaystyle
f'(x)=2^x+x2^x\ln2
=2^x(1+x\ln2).
$$

由于 $\displaystyle 2^x>0$，故驻点由

$$
\displaystyle
1+x\ln2=0
$$

确定，即

$$
\displaystyle
x=-\dfrac{1}{\ln2}.
$$

当 $\displaystyle x<-\dfrac{1}{\ln2}$ 时，$\displaystyle f'(x)<0$；当 $\displaystyle x>-\dfrac{1}{\ln2}$ 时，$\displaystyle f'(x)>0$。

因此函数先减后增，在

$$
\displaystyle
x=-\dfrac{1}{\ln2}
$$

处取得极小值。

</details>

---

3. **【1988-45-2 分】** 判断正误：$\displaystyle x_0$是极值点$\displaystyle \Rightarrow f'(x_0)=0$。

<details>
<summary>查看答案与解析</summary>

**答案：** 错误。

**解析：** 极值点不一定是可导点。只有当函数在内点 $\displaystyle x_0$ 处可导且 $\displaystyle x_0$ 为极值点时，才有费马定理

$$
\displaystyle
f'(x_0)=0.
$$

例如

$$
\displaystyle
f(x)=|x|.
$$

函数在 $\displaystyle x_0=0$ 处取得极小值，但

$$
\displaystyle
f'_-(0)=-1,\qquad f'_+(0)=1,
$$

所以 $\displaystyle f'(0)$ 不存在。

故原命题错误。

</details>

---

4. **【1993-123-3 分】** $\displaystyle y=\dfrac{x+1}{x^2}$填表

| 单调减区间 |
| ---------- |
| 单调增区间 |
| 极值点     |
| 极值       |
| 凹区间     |
| 凸区间     |
| 拐点       |
| 渐近线     |

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

| 项目 | 结果 |
| --- | --- |
| 单调减区间 | $\displaystyle (-\infty,-2)$，$\displaystyle (0,+\infty)$ |
| 单调增区间 | $\displaystyle (-2,0)$ |
| 极值点 | $\displaystyle x=-2$ |
| 极值 | 极小值 $\displaystyle -\dfrac14$ |
| 凹区间 | $\displaystyle (-3,0)$，$\displaystyle (0,+\infty)$ |
| 凸区间 | $\displaystyle (-\infty,-3)$ |
| 拐点 | $\displaystyle \left(-3,-\dfrac29\right)$ |
| 渐近线 | $\displaystyle x=0$，$\displaystyle y=0$ |

</div>
</div>

**解析：** 函数的定义域为

$$
\displaystyle
(-\infty,0)\cup(0,+\infty).
$$

将函数写为

$$
\displaystyle
f(x)=\dfrac1x+\dfrac1{x^2}.
$$

求一阶导数：

$$
\displaystyle
f'(x)=-\dfrac1{x^2}-\dfrac2{x^3}
=-\dfrac{x+2}{x^3}.
$$

由 $\displaystyle f'(x)$ 的符号可得：

- 在 $\displaystyle (-\infty,-2)$ 上，$\displaystyle f'(x)<0$；
- 在 $\displaystyle (-2,0)$ 上，$\displaystyle f'(x)>0$；
- 在 $\displaystyle (0,+\infty)$ 上，$\displaystyle f'(x)<0$。

故函数在 $\displaystyle (-\infty,-2)$、$\displaystyle (0,+\infty)$ 上单调减少，在 $\displaystyle (-2,0)$ 上单调增加。

在 $\displaystyle x=-2$ 处，导数由负变正，因此该点为极小值点，且

$$
\displaystyle
f(-2)=\dfrac{-2+1}{(-2)^2}=-\dfrac14.
$$

再求二阶导数：

$$
\displaystyle
f''(x)=\dfrac2{x^3}+\dfrac6{x^4}
=\dfrac{2(x+3)}{x^4}.
$$

因为 $\displaystyle x^4>0$，所以 $\displaystyle f''(x)$ 的符号由 $\displaystyle x+3$ 决定：

$$
\displaystyle
f''(x)<0\quad(x<-3),
$$

$$
\displaystyle
f''(x)>0\quad(-3<x<0\ \text{或}\ x>0).
$$

按通常教材约定，$\displaystyle f''(x)>0$ 的区间为凹区间，$\displaystyle f''(x)<0$ 的区间为凸区间。因此

$$
\displaystyle
\text{凹区间为 }(-3,0),(0,+\infty),
$$

$$
\displaystyle
\text{凸区间为 }(-\infty,-3).
$$

在 $\displaystyle x=-3$ 的两侧二阶导数异号，故有拐点

$$
\displaystyle
\left(-3,f(-3)\right)
=\left(-3,-\dfrac29\right).
$$

最后，

$$
\displaystyle
\lim_{x\to0^\pm}f(x)=+\infty,
$$

故 $\displaystyle x=0$ 为垂直渐近线；又

$$
\displaystyle
\lim_{x\to\pm\infty}f(x)=0,
$$

故 $\displaystyle y=0$ 为水平渐近线。

</details>

---

5. **【1989-3-3 分】** $\displaystyle f(x),g(x)$在$\displaystyle x=a$都取极大，则$\displaystyle F(x)=f(x)g(x)$在$\displaystyle x=a$( )。

<div className="choice-options">
<div className="choice-option-row">A. 必极大 &emsp;&emsp; B. 必极小 &emsp;&emsp; C. 不可能极值 &emsp;&emsp; D. 不能确定</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 两个函数都在 $\displaystyle x=a$ 处取得极大值，并不能确定它们乘积在该点的极值性质，因为还与函数值的正负有关。

例如取 $\displaystyle a=0$。

若

$$
\displaystyle
f(x)=g(x)=1-x^2,
$$

则二者均在 $\displaystyle x=0$ 处取得极大值，而

$$
\displaystyle
F(x)=(1-x^2)^2
$$

在 $\displaystyle x=0$ 处也取得极大值。

另一方面，若

$$
\displaystyle
f(x)=g(x)=-1-x^2,
$$

则二者仍均在 $\displaystyle x=0$ 处取得极大值，但

$$
\displaystyle
F(x)=(-1-x^2)^2=(1+x^2)^2
$$

在 $\displaystyle x=0$ 处取得极小值。

因此 $\displaystyle F(x)$ 在 $\displaystyle x=a$ 处可能取极大值，也可能取极小值，无法确定，故选 D。

</details>

---

6. **【1990-3-3 分】** $\displaystyle y=\dfrac1{1+x^2}(x>0)$拐点____。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
\left(\dfrac1{\sqrt3},\dfrac34\right).
$$

</div>
</div>

**解析：** 设

$$
\displaystyle
f(x)=\dfrac1{1+x^2},\qquad x>0.
$$

求导得

$$
\displaystyle
f'(x)=-\dfrac{2x}{(1+x^2)^2}.
$$

继续求导：

$$
\displaystyle
f''(x)=\dfrac{2(3x^2-1)}{(1+x^2)^3}.
$$

由于分母恒正，令

$$
\displaystyle
3x^2-1=0,
$$

得定义域内的唯一解

$$
\displaystyle
x=\dfrac1{\sqrt3}.
$$

当 $\displaystyle 0<x<\dfrac1{\sqrt3}$ 时，$\displaystyle f''(x)<0$；当 $\displaystyle x>\dfrac1{\sqrt3}$ 时，$\displaystyle f''(x)>0$，故凹凸性在该点发生改变。

又

$$
\displaystyle
f\left(\dfrac1{\sqrt3}\right)
=\dfrac{1}{1+\frac13}
=\dfrac34.
$$

因此拐点为

$$
\displaystyle
\left(\dfrac1{\sqrt3},\dfrac34\right).
$$

</details>

---

7. **【1991-3-3 分】** $\displaystyle x_0\neq0$是$\displaystyle f(x)$极大，则( )。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle x_0$必驻点 &emsp;&emsp; B. $\displaystyle -x_0$是$\displaystyle -f(-x)$极小 &emsp;&emsp; C. $\displaystyle -x_0$是$\displaystyle -f(x)$极小 &emsp;&emsp; D. $\displaystyle \forall x,f(x)\le f(x_0)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B。

**解析：** 设

$$
\displaystyle
h(x)=-f(-x).
$$

由于 $\displaystyle x_0$ 是 $\displaystyle f(x)$ 的极大值点，所以存在 $\displaystyle x_0$ 的某邻域，使得其中任意 $\displaystyle u$ 都满足

$$
\displaystyle
f(u)\le f(x_0).
$$

当 $\displaystyle x$ 在 $\displaystyle -x_0$ 附近时，$\displaystyle -x$ 在 $\displaystyle x_0$ 附近，因此

$$
\displaystyle
f(-x)\le f(x_0).
$$

两边取负号，得

$$
\displaystyle
-f(-x)\ge-f(x_0).
$$

即

$$
\displaystyle
h(x)\ge h(-x_0).
$$

故 $\displaystyle -x_0$ 是函数 $\displaystyle -f(-x)$ 的极小值点，B 正确。

A 错误，因为极值点处函数不一定可导；D 把局部极大误认为全局最大；C 中只改变了函数值的符号而未作自变量反射，无法由已知条件推出。

</details>

---

8. **【1996-5-3 分】** $\displaystyle f'(x_0)=f''(x_0)=0,f'''(x_0)>0$，则( )。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f'(x_0)$是$\displaystyle f'(x)$极大 &emsp;&emsp; B. $\displaystyle f(x_0)$极大</div>
<div className="choice-option-row">C. $\displaystyle f(x_0)$极小 &emsp;&emsp; D. $\displaystyle (x_0,f(x_0))$拐点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 由于

$$
\displaystyle
f''(x_0)=0,\qquad f'''(x_0)>0,
$$

对函数 $\displaystyle f''(x)$ 在 $\displaystyle x_0$ 附近考察。由导数定义，$\displaystyle f'''(x_0)>0$ 表明 $\displaystyle f''(x)$ 在 $\displaystyle x_0$ 附近由负变正，即

$$
\displaystyle
f''(x)<0\quad(x<x_0),
$$

$$
\displaystyle
f''(x)>0\quad(x>x_0)
$$

在充分小的邻域内成立。

因此曲线在 $\displaystyle x_0$ 两侧凹凸性发生改变，故

$$
\displaystyle
(x_0,f(x_0))
$$

是拐点。

同时，由于 $\displaystyle f'(x_0)=0$ 且 $\displaystyle f'(x)$ 在 $\displaystyle x_0$ 两侧均为非负的小量，函数在该点附近并非先增后减或先减后增，所以 $\displaystyle f(x_0)$ 不是极值。

故选 D。

</details>

---

9. **【2001-2-3 分】** $\displaystyle y=(x-1)^2(x-3)^2$拐点个数( )。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle 0$ &emsp;&emsp; B. $\displaystyle 1$ &emsp;&emsp; C. $\displaystyle 2$ &emsp;&emsp; D. $\displaystyle 3$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C。

**解析：** 令

$$
\displaystyle
t=x-2,
$$

则

$$
\displaystyle
x-1=t+1,\qquad x-3=t-1,
$$

从而

$$
\displaystyle
f(x)=(t+1)^2(t-1)^2=(t^2-1)^2=t^4-2t^2+1.
$$

因为 $\displaystyle t=x-2$，所以对 $\displaystyle x$ 求导与对 $\displaystyle t$ 求导相同。于是

$$
\displaystyle
f''(x)=12t^2-4
=4\left(3t^2-1\right).
$$

令 $\displaystyle f''(x)=0$，得

$$
\displaystyle
t=\pm\dfrac1{\sqrt3},
$$

即

$$
\displaystyle
x=2\pm\dfrac1{\sqrt3}.
$$

在这两个点的两侧，$\displaystyle f''(x)$ 均发生符号改变，因此二者都是拐点的横坐标。

故曲线共有 $\displaystyle 2$ 个拐点，选 C。

</details>

---

10. **【2004-234-4 分】** $\displaystyle f(x)=|x(1-x)|$，则( )。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle x=0$极值，$\displaystyle (0,0)$非拐点 &emsp;&emsp; B. $\displaystyle x=0$非极值，$\displaystyle (0,0)$拐点</div>
<div className="choice-option-row">C. $\displaystyle x=0$极值，$\displaystyle (0,0)$拐点 &emsp;&emsp; D. $\displaystyle x=0$非极值，$\displaystyle (0,0)$非拐点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C。

**解析：** 在 $\displaystyle x=0$ 附近分段讨论。

当 $\displaystyle x<0$ 时，$\displaystyle x(1-x)<0$，故

$$
\displaystyle
f(x)=-x(1-x)=x^2-x.
$$

当 $\displaystyle 0<x<1$ 时，$\displaystyle x(1-x)>0$，故

$$
\displaystyle
f(x)=x(1-x)=x-x^2.
$$

又 $\displaystyle f(0)=0$，且在 $\displaystyle x=0$ 的去心邻域内

$$
\displaystyle
f(x)=|x(1-x)|>0=f(0).
$$

所以 $\displaystyle x=0$ 是函数的极小值点。

另一方面，在 $\displaystyle x<0$ 时

$$
\displaystyle
f''(x)=2>0,
$$

而在 $\displaystyle 0<x<1$ 时

$$
\displaystyle
f''(x)=-2<0.
$$

曲线在 $\displaystyle x=0$ 两侧的凹凸性发生改变，且函数在该点连续，因此 $\displaystyle (0,0)$ 是拐点。

故 $\displaystyle x=0$ 既是极值点，$\displaystyle (0,0)$ 又是拐点，选 C。

</details>

---

11. **【2005-34-4 分】** $\displaystyle f(x)=x\sin x+\cos x$，则( )。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f(0)$极大，$\displaystyle f(\dfrac{\pi}{2})$极小 &emsp;&emsp; B. $\displaystyle f(0)$极小，$\displaystyle f(\dfrac{\pi}{2})$极大</div>
<div className="choice-option-row">C. 两者均极大 &emsp;&emsp; D. 两者均极小</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B。

**解析：** 求导得

$$
\displaystyle
f'(x)=\sin x+x\cos x-\sin x=x\cos x.
$$

在 $\displaystyle x=0$ 附近，$\displaystyle \cos x>0$，故 $\displaystyle f'(x)$ 的符号与 $\displaystyle x$ 相同，即

$$
\displaystyle
f'(x)<0\quad(x<0),\qquad f'(x)>0\quad(x>0).
$$

因此函数在 $\displaystyle x=0$ 处先减后增，$\displaystyle f(0)$ 为极小值。

在 $\displaystyle x=\dfrac{\pi}{2}$ 附近，$\displaystyle x>0$，而 $\displaystyle \cos x$ 由正变负，所以 $\displaystyle f'(x)$ 由正变负。因此

$$
\displaystyle
f\left(\dfrac{\pi}{2}\right)
$$

为极大值。

故选 B。

</details>

---

12. **【2008-2-4 分】** $\displaystyle y=(x-5)x^{\dfrac{2}{3}}$拐点坐标____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle (-1,-6)$。

**解析：** 函数可写为

$$
\displaystyle
y=x^{\frac53}-5x^{\frac23}.
$$

当 $\displaystyle x\ne0$ 时，

$$
\displaystyle
y' =\dfrac53x^{\frac23}-\dfrac{10}{3}x^{-\frac13},
$$

$$
\displaystyle
y''=\dfrac{10}{9}x^{-\frac13}+\dfrac{10}{9}x^{-\frac43}
=\dfrac{10}{9}x^{-\frac43}(x+1).
$$

由于对 $\displaystyle x\ne0$ 有 $\displaystyle x^{-\frac43}>0$，故 $\displaystyle y''$ 的符号由 $\displaystyle x+1$ 决定。

当 $\displaystyle x$ 经过 $\displaystyle -1$ 时，$\displaystyle y''$ 由负变正，曲线凹凸性发生改变；而在 $\displaystyle x=0$ 两侧，$\displaystyle y''$ 均为正，凹凸性不变，所以 $\displaystyle x=0$ 不是拐点横坐标。

当 $\displaystyle x=-1$ 时，

$$
\displaystyle
y=(-1-5)(-1)^{\frac23}=-6.
$$

因此拐点为

$$
\displaystyle
(-1,-6).
$$

</details>

---

13. **【2010-3-4 分】** $\displaystyle y=x^3+ax^2+bx+1$拐点$\displaystyle (-1,0)$，则$\displaystyle b=$____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 3$。

**解析：** 由

$$
\displaystyle
y''=6x+2a
$$

及 $\displaystyle x=-1$ 为拐点横坐标，得

$$
\displaystyle
y''(-1)=-6+2a=0,
$$

故

$$
\displaystyle
a=3.
$$

又拐点 $\displaystyle (-1,0)$ 在曲线上，所以

$$
\displaystyle
(-1)^3+a(-1)^2+b(-1)+1=0.
$$

代入 $\displaystyle a=3$，得

$$
\displaystyle
-1+3-b+1=0,
$$

从而

$$
\displaystyle
b=3.
$$

</details>

---

14. **【2010-12-4 分】** $\displaystyle f_1''(x_0)<0,f_2''(x_0)<0$，同切线$\displaystyle y=g(x)$，$\displaystyle y=f_1$曲率更大，则( )。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f_1\le f_2\le g$ &emsp;&emsp; B. $\displaystyle f_2\le f_1\le g$</div>
<div className="choice-option-row">C. $\displaystyle f_1\le g\le f_2$ &emsp;&emsp; D. $\displaystyle f_2\le g\le f_1$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A。

**解析：** 两曲线在 $\displaystyle x=x_0$ 处有同一条切线，故

$$
\displaystyle
f_1(x_0)=f_2(x_0)=g(x_0),
$$

$$
\displaystyle
f_1'(x_0)=f_2'(x_0)=g'(x_0).
$$

曲率公式为

$$
\displaystyle
\kappa=\dfrac{|f''(x_0)|}{\left[1+\left(f'(x_0)\right)^2\right]^{\frac32}}.
$$

由于两曲线在该点斜率相同，而 $\displaystyle f_1$ 的曲率更大，所以

$$
\displaystyle
|f_1''(x_0)|>|f_2''(x_0)|.
$$

又 $\displaystyle f_1''(x_0)<0$，$\displaystyle f_2''(x_0)<0$，故

$$
\displaystyle
f_1''(x_0)<f_2''(x_0)<0.
$$

在 $\displaystyle x_0$ 的充分小邻域内，由二阶泰勒展开，

$$
\displaystyle
f_i(x)-g(x)=\dfrac12f_i''(x_0)(x-x_0)^2+o\left((x-x_0)^2\right),
\quad i=1,2.
$$

因此两条曲线均位于公共切线下方，且曲率更大的 $\displaystyle f_1$ 位于更下方，即

$$
\displaystyle
f_1(x)\le f_2(x)\le g(x).
$$

故选 A。

</details>

---

15. **【2019-3-4 分】** $\displaystyle y=x\sin x+2\cos x\left(-\dfrac{\pi}{2}<x<\dfrac{3}{2}\pi\right)$ 的拐点坐标为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle (\pi,-2)$。

**解析：** 求导得

$$
\displaystyle
y'=\sin x+x\cos x-2\sin x=x\cos x-\sin x,
$$

$$
\displaystyle
y''=\cos x-x\sin x-\cos x=-x\sin x.
$$

在区间 $\displaystyle \left(-\dfrac{\pi}{2},\dfrac{3\pi}{2}\right)$ 内，$\displaystyle y''=0$ 的点为

$$
\displaystyle
x=0,
\qquad x=\pi.
$$

考察符号：

- 当 $\displaystyle -\dfrac{\pi}{2}<x<0$ 时，$\displaystyle x<0$，$\displaystyle \sin x<0$，故 $\displaystyle y''<0$；
- 当 $\displaystyle 0<x<\pi$ 时，$\displaystyle x>0$，$\displaystyle \sin x>0$，故 $\displaystyle y''<0$；
- 当 $\displaystyle \pi<x<\dfrac{3\pi}{2}$ 时，$\displaystyle x>0$，$\displaystyle \sin x<0$，故 $\displaystyle y''>0$。

所以 $\displaystyle x=0$ 两侧凹凸性不变，不是拐点；$\displaystyle x=\pi$ 两侧凹凸性改变，是拐点横坐标。

又

$$
\displaystyle
y(\pi)=\pi\sin\pi+2\cos\pi=-2.
$$

故拐点为

$$
\displaystyle
(\pi,-2).
$$

</details>

---

16. **【2019-2-4 分】** 函数 $\displaystyle y=x\sin x+2\cos x\left(-\dfrac{\pi}{2}<x<\dfrac{3}{2}\pi\right)$ 的拐点为（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle (0,2)$ &emsp;&emsp; B. $\displaystyle (\pi,-2)$ &emsp;&emsp; C. $\displaystyle \left(\dfrac{\pi}{2},\dfrac{\pi}{2}\right)$ &emsp;&emsp; D. $\displaystyle \left(\dfrac{3}{2}\pi,-\dfrac{3}{2}\pi\right)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B。

**解析：** 由上一题的计算，

$$
\displaystyle
y''=-x\sin x.
$$

在给定区间内，可能使 $\displaystyle y''=0$ 的点为

$$
\displaystyle
x=0,
\qquad x=\pi.
$$

在 $\displaystyle x=0$ 左右，$\displaystyle y''$ 均小于零，凹凸性不变；在 $\displaystyle x=\pi$ 左侧 $\displaystyle y''<0$，右侧 $\displaystyle y''>0$，凹凸性发生改变。

又

$$
\displaystyle
y(\pi)=-2.
$$

所以唯一拐点为

$$
\displaystyle
(\pi,-2),
$$

故选 B。

</details>

---

17. **【2019-1-4 分】** $\displaystyle f(x)=\begin{cases}x|x|,x\le0\\ x\ln x,x>0\end{cases}$，$\displaystyle x=0$ (  )。

<div className="choice-options">
<div className="choice-option-row">A. 可导极值 &emsp;&emsp; B. 不可导极值 &emsp;&emsp; C. 可导非极值 &emsp;&emsp; D. 不可导非极值</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B。

**解析：** 当 $\displaystyle x<0$ 时，

$$
\displaystyle
f(x)=x|x|=-x^2<0.
$$

当 $\displaystyle 0<x<1$ 时，$\displaystyle \ln x<0$，故

$$
\displaystyle
f(x)=x\ln x<0.
$$

而

$$
\displaystyle
f(0)=0.
$$

因此在 $\displaystyle x=0$ 的某个去心邻域内，恒有

$$
\displaystyle
f(x)<f(0),
$$

所以 $\displaystyle x=0$ 是极大值点。

再判断可导性。左导数为

$$
\displaystyle
f'_-(0)=\lim_{x\to0^-}\dfrac{-x^2-0}{x}=\lim_{x\to0^-}(-x)=0.
$$

右导数为

$$
\displaystyle
f'_+(0)=\lim_{x\to0^+}\dfrac{x\ln x-0}{x}
=\lim_{x\to0^+}\ln x=-\infty.
$$

右导数不存在，因此函数在 $\displaystyle x=0$ 处不可导。

故 $\displaystyle x=0$ 是不可导的极值点，选 B。

</details>

---

18. **【2023-2-5 分】** 设函数 $\displaystyle f(x)=(x^{2}+a)e^{x}$，若 $\displaystyle f(x)$ 没有极值点，但曲线 $\displaystyle f(x)$ 有拐点，则 $\displaystyle a$ 的取值范围是( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle [0,1)$ &emsp;&emsp; B. $\displaystyle [1,+\infty)$ &emsp;&emsp; C. $\displaystyle [1,2)$ &emsp;&emsp; D. $\displaystyle [2,+\infty)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C。

**解析：** 先考察极值点。求导得

$$
\displaystyle
f'(x)=e^x\left(x^2+2x+a\right)
=e^x\left[(x+1)^2+a-1\right].
$$

由于 $\displaystyle e^x>0$，$\displaystyle f'(x)$ 的符号由

$$
\displaystyle
(x+1)^2+a-1
$$

决定。

当 $\displaystyle a<1$ 时，该二次式有两个不同实根，且导数符号发生两次改变，函数存在极值点。

当 $\displaystyle a=1$ 时，

$$
\displaystyle
f'(x)=e^x(x+1)^2\ge0,
$$

在 $\displaystyle x=-1$ 处导数虽为零，但不变号，因此没有极值点。

当 $\displaystyle a>1$ 时，$\displaystyle f'(x)>0$，也没有极值点。

故“没有极值点”等价于

$$
\displaystyle
a\ge1.
$$

再考察拐点：

$$
\displaystyle
f''(x)=e^x\left(x^2+4x+a+2\right)
=e^x\left[(x+2)^2+a-2\right].
$$

曲线存在拐点，要求二次式有两个不同实根并在根处变号，即

$$
\displaystyle
a-2<0,
$$

所以

$$
\displaystyle
a<2.
$$

综合得

$$
\displaystyle
1\le a<2.
$$

故选 C。

</details>

---

19. **【2023-3-5 分】** 已知可导函数 $\displaystyle y=f(x)$，其满足 $\displaystyle ae^{x}+y^{2}+y-\ln (1+x) \cos y+b=0$，且满足 $\displaystyle y(0)=0$，$\displaystyle y'(0)=0$。

(1)求 $\displaystyle a$，$\displaystyle b$；

(2)判断 $\displaystyle x=0$ 是否为 $\displaystyle y=f(x)$ 的极值点。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) $\displaystyle a=1$，$\displaystyle b=-1$；

(2) $\displaystyle x=0$ 是 $\displaystyle y=f(x)$ 的极大值点。

**解析：** 将 $\displaystyle x=0$，$\displaystyle y(0)=0$ 代入原方程，得

$$
\displaystyle
a+b=0.
$$

即

$$
\displaystyle
b=-a.
$$

对原方程两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle
ae^x+(2y+1)y'
-\dfrac{\cos y}{1+x}
+\ln(1+x)\sin y\,y'=0.
$$

令 $\displaystyle x=0$，利用 $\displaystyle y(0)=0$，$\displaystyle y'(0)=0$，得

$$
\displaystyle
a-1=0.
$$

所以

$$
\displaystyle
a=1,
\qquad b=-1.
$$

下面判断极值。由于原方程左端对 $\displaystyle y$ 的偏导数在 $\displaystyle (0,0)$ 处为

$$
\displaystyle
2y+1+\ln(1+x)\sin y\Big|_{(0,0)}=1\ne0,
$$

故由隐函数定理，$\displaystyle y=f(x)$ 在零点附近具有足够阶的连续导数。

再对一阶导数关系求导：

$$
\displaystyle
ae^x+2(y')^2+(2y+1)y''
+\dfrac{\sin y\,y'}{1+x}
+\dfrac{\cos y}{(1+x)^2}
+\dfrac{\sin y\,y'}{1+x}
+\ln(1+x)\left[\cos y\,(y')^2+\sin y\,y''\right]=0.
$$

令 $\displaystyle x=0$，并代入

$$
\displaystyle
a=1,
\qquad y(0)=0,
\qquad y'(0)=0,
$$

得到

$$
\displaystyle
1+y''(0)+1=0.
$$

因此

$$
\displaystyle
y''(0)=-2<0.
$$

又 $\displaystyle y'(0)=0$，故由极值的第二充分条件，$\displaystyle x=0$ 是函数 $\displaystyle y=f(x)$ 的极大值点。

</details>

---

20. **【2025-123-5 分】** 已知函数 $\displaystyle f(x)=\int_{0}^{x} e^{t^{2}} \sin t\,\mathrm{d}t$，$\displaystyle g(x)=\int_{0}^{x} e^{t^{2}}\,\mathrm{d}t \cdot \sin^{2} x$。则( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle x=0$ 是 $\displaystyle f(x)$ 的极值点，也是 $\displaystyle g(x)$ 的极值点。</div>
<div className="choice-option-row">B. $\displaystyle x=0$ 是 $\displaystyle f(x)$ 的极值点，$\displaystyle (0,0)$ 是 $\displaystyle g(x)$ 的拐点。</div>
<div className="choice-option-row">C. $\displaystyle x=0$ 是 $\displaystyle f(x)$ 的极值点，$\displaystyle (0,0)$ 是 $\displaystyle f(x)$ 的拐点。</div>
<div className="choice-option-row">D. $\displaystyle (0,0)$ 是曲线 $\displaystyle f(x)$ 的拐点，$\displaystyle (0,0)$ 也是 $\displaystyle g(x)$ 的拐点。</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 由变上限积分求导公式，

$$
\displaystyle
f'(x)=e^{x^2}\sin x.
$$

由于 $\displaystyle e^{x^2}>0$，所以在 $\displaystyle x=0$ 的充分小邻域内，$\displaystyle f'(x)$ 与 $\displaystyle x$ 同号，即

$$
\displaystyle
f'(x)<0\quad(x<0),
\qquad
f'(x)>0\quad(x>0).
$$

故 $\displaystyle x=0$ 是 $\displaystyle f(x)$ 的极小值点。

令

$$
\displaystyle
F(x)=\int_0^x e^{t^2}\,\mathrm{d}t,
\qquad
h(x)=\sin^2x,
$$

则 $\displaystyle g(x)=F(x)h(x)$。注意到

$$
\displaystyle
F(0)=0,
\qquad F'(0)=1,
$$

以及

$$
\displaystyle
h(0)=h'(0)=0,

\qquad h''(0)=2.
$$

因此

$$
\displaystyle
g''(0)=0,
$$

且

$$
\displaystyle
g'''(0)
=3F'(0)h''(0)=6>0.
$$

由于 $\displaystyle g'''(x)$ 在零点附近连续，故在零点附近有 $\displaystyle g'''(x)>0$，从而 $\displaystyle g''(x)$ 严格增加。结合 $\displaystyle g''(0)=0$ 可知

$$
\displaystyle
g''(x)<0\quad(x<0),

\qquad
g''(x)>0\quad(x>0).
$$

所以曲线 $\displaystyle y=g(x)$ 在 $\displaystyle (0,0)$ 两侧凹凸性发生改变，$\displaystyle (0,0)$ 是其拐点。

综上，选 B。

</details>

### （二）结合方程

1. **【1988-12-3 分】** 设 $\displaystyle y=f(x)$ 是方程 $\displaystyle y''-2y'+4y=0$ 的一个解，若 $\displaystyle f(x)>0$，且 $\displaystyle f'(x_{0})=0$，则函数 $\displaystyle f(x)$ 在点 $\displaystyle x_{0}$( )

<div className="choice-options">
<div className="choice-option-row">A. 取得极大值 &emsp;&emsp; B. 取得极小值 &emsp;&emsp; C. 某个邻域内单调增加 &emsp;&emsp; D. 某个邻域内单调减少</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A

**解析：** 由微分方程

$$
\displaystyle
f''(x)-2f'(x)+4f(x)=0
$$

得

$$
\displaystyle
f''(x_0)=2f'(x_0)-4f(x_0).
$$

又 $\displaystyle f'(x_0)=0$，且 $\displaystyle f(x_0)>0$，所以

$$
\displaystyle
f''(x_0)=-4f(x_0)<0.
$$

因此由极值的第二充分条件，$\displaystyle x_0$ 是 $\displaystyle f(x)$ 的极大值点，即函数在该点取得极大值。

故选 A。

</details>

---

2. **【1994-3-3 分】** 设 $\displaystyle y=f(x)$ 是满足微分方程 $\displaystyle y''+y'-e^{\sin x}=0$，且 $\displaystyle f'(x_{0})=0$，则 $\displaystyle f(x)$ 在( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle x_{0}$ 的某个邻域内单调增加 &emsp;&emsp; B. $\displaystyle x_{0}$ 的某个邻域内单调减少</div>
<div className="choice-option-row">C. $\displaystyle x_{0}$ 处取得极小值 &emsp;&emsp; D. $\displaystyle x_{0}$ 处取得极大值</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 由微分方程可得

$$
\displaystyle
f''(x)=e^{\sin x}-f'(x).
$$

将 $\displaystyle f'(x_0)=0$ 代入，得

$$
\displaystyle
f''(x_0)=e^{\sin x_0}>0.
$$

因此由极值的第二充分条件，$\displaystyle x_0$ 是 $\displaystyle f(x)$ 的极小值点，函数在 $\displaystyle x_0$ 处取得极小值。

故选 C。

</details>

---

3. **【1997-2-3 分】** 已知函数 $\displaystyle y=f(x)$ 对一切 $\displaystyle x$ 满足 $\displaystyle xf''(x)+3x[f'(x)]^{2}=1-e^{-x}$，若 $\displaystyle f'(x_{0})=0\,(x_{0}\neq0)$，则( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f(x_{0})$ 是 $\displaystyle f(x)$ 的极大值</div>
<div className="choice-option-row">B. $\displaystyle f(x_{0})$ 是 $\displaystyle f(x)$ 的极小值</div>
<div className="choice-option-row">C. $\displaystyle (x_{0},f(x_{0}))$ 是曲线 $\displaystyle y=f(x)$ 的拐点</div>
<div className="choice-option-row">D. $\displaystyle f(x_{0})$ 不是 $\displaystyle f(x)$ 的极值，$\displaystyle (x_{0},f(x_{0}))$ 也不是曲线 $\displaystyle y=f(x)$ 的拐点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 在 $\displaystyle x=x_0$ 处，由 $\displaystyle f'(x_0)=0$ 得

$$
\displaystyle
x_0f''(x_0)=1-e^{-x_0},
$$

即

$$
\displaystyle
f''(x_0)=\dfrac{1-e^{-x_0}}{x_0}.
$$

当 $\displaystyle x_0>0$ 时，$\displaystyle 1-e^{-x_0}>0$；当 $\displaystyle x_0<0$ 时，$\displaystyle 1-e^{-x_0}<0$。因此分子与分母始终同号，从而

$$
\displaystyle
f''(x_0)>0.
$$

由极值的第二充分条件，$\displaystyle x_0$ 是 $\displaystyle f(x)$ 的极小值点，即 $\displaystyle f(x_0)$ 是函数的极小值。

故选 B。

</details>

---

4. **【2000-2-3 分】** 设函数 $\displaystyle f(x)$ 满足关系式 $\displaystyle f''(x)+[f'(x)]^{2}=x$，且 $\displaystyle f'(0)=0$，则( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f(0)$ 是 $\displaystyle f(x)$ 的极大值</div>
<div className="choice-option-row">B. $\displaystyle f(0)$ 是 $\displaystyle f(x)$ 的极小值</div>
<div className="choice-option-row">C. 点 $\displaystyle (0,f(0))$ 是曲线 $\displaystyle y=f(x)$ 的拐点</div>
<div className="choice-option-row">D. $\displaystyle f(0)$ 不是 $\displaystyle f(x)$ 的极值，点 $\displaystyle (0,f(0))$ 也不是曲线 $\displaystyle y=f(x)$ 的拐点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 令 $\displaystyle x=0$，由

$$
\displaystyle
f''(x)+[f'(x)]^2=x
$$

以及 $\displaystyle f'(0)=0$，得

$$
\displaystyle
f''(0)=0.
$$

对原关系式两边求导，得

$$
\displaystyle
f'''(x)+2f'(x)f''(x)=1.
$$

令 $\displaystyle x=0$，可得

$$
\displaystyle
f'''(0)=1>0.
$$

由于 $\displaystyle f'''(x)$ 在零点附近连续，故 $\displaystyle f''(x)$ 在零点附近严格增加。又 $\displaystyle f''(0)=0$，所以

$$
\displaystyle
f''(x)<0\quad(x<0),

\qquad
f''(x)>0\quad(x>0).
$$

因此曲线在 $\displaystyle x=0$ 两侧凹凸性发生改变，点 $\displaystyle (0,f(0))$ 是曲线 $\displaystyle y=f(x)$ 的拐点。

故选 C。

</details>

### （三）结合图像

1. **【2003-12-4 分】** 设函数 $\displaystyle f(x)$ 在 $\displaystyle (-\infty,+\infty)$ 内连续，其导数的图形如图所示，则 $\displaystyle f(x)$ 有( )

A. 一个极小值点和两个极大值点

B. 两个极小值点和一个极大值点

C. 两个极小值点和两个极大值点

D. 三个极小值点和一个极大值点

![2003-12-4 题图](高数真题图片/gs_up_2003_12_4.png)

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 图像表示的是 $\displaystyle f'(x)$。

从左到右观察 $\displaystyle f'(x)$ 与横轴的交点及其符号变化：

- 第一个交点处，$\displaystyle f'(x)$ 由正变负，因此 $\displaystyle f(x)$ 取得极大值；
- 第二个交点处，$\displaystyle f'(x)$ 由负变正，因此 $\displaystyle f(x)$ 取得极小值；
- 右侧交点处，$\displaystyle f'(x)$ 再次由负变正，因此 $\displaystyle f(x)$ 又取得一个极小值。

所以 $\displaystyle f(x)$ 有两个极小值点和一个极大值点。

故选 B。

</details>

---

2. **【2015-123-4 分】** 设函数 $\displaystyle f(x)$ 在 $\displaystyle (-\infty,+\infty)$ 内连续，其二阶导函数 $\displaystyle f''(x)$ 的图形如图所示，则曲线 $\displaystyle y=f(x)$ 的拐点个数为( )

A. $\displaystyle 0$

B. $\displaystyle 1$

C. $\displaystyle 2$

D. $\displaystyle 3$

![2015-123-4 题图](高数真题图片/gs_up_2015_123_4.png)

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 曲线 $\displaystyle y=f(x)$ 的拐点对应 $\displaystyle f''(x)$ 变号的位置。

左侧曲线虽然与横轴相切，但在该点两侧均位于横轴上方，即 $\displaystyle f''(x)$ 均为正，未发生变号，因此该点不是拐点。

右侧曲线穿过横轴，$\displaystyle f''(x)$ 由负变正，凹凸性发生改变，因此产生一个拐点。

所以曲线共有一个拐点。

故选 B。

</details>

---

3. **【2016-23-4 分】** 设函数 $\displaystyle f(x)$ 在 $\displaystyle (-\infty,+\infty)$ 内连续，其导函数的图像如图所示，则( )

A. 函数 $\displaystyle f(x)$ 有 $\displaystyle 2$ 个极值点，曲线 $\displaystyle y=f(x)$ 有 $\displaystyle 2$ 个拐点

B. 函数 $\displaystyle f(x)$ 有 $\displaystyle 2$ 个极值点，曲线 $\displaystyle y=f(x)$ 有 $\displaystyle 3$ 个拐点

C. 函数 $\displaystyle f(x)$ 有 $\displaystyle 3$ 个极值点，曲线 $\displaystyle y=f(x)$ 有 $\displaystyle 1$ 个拐点

D. 函数 $\displaystyle f(x)$ 有 $\displaystyle 3$ 个极值点，曲线 $\displaystyle y=f(x)$ 有 $\displaystyle 2$ 个拐点

![2016-23-4 题图](高数真题图片/gs_up_2016_23_4.png)

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 图像为 $\displaystyle f'(x)$ 的图像。

先判断极值点。$\displaystyle f'(x)$ 与横轴有两个穿越交点：

- 左侧交点处，$\displaystyle f'(x)$ 由正变负，故 $\displaystyle f(x)$ 有一个极大值点；
- 虚线右侧的第一个交点处，$\displaystyle f'(x)$ 由负变正，故 $\displaystyle f(x)$ 有一个极小值点。

右侧曲线在横轴处相切，但 $\displaystyle f'(x)$ 在该点两侧均为正，符号不变，故该点不是极值点。因此 $\displaystyle f(x)$ 共有两个极值点。

再判断拐点。曲线 $\displaystyle y=f(x)$ 的凹凸性由 $\displaystyle f'(x)$ 的增减性决定：

- 在虚线所示点的左、右两侧，$\displaystyle f'(x)$ 由递减转为递增，凹凸性发生改变；
- 右侧波峰处，$\displaystyle f'(x)$ 由递增转为递减，产生一个拐点；
- 右侧与横轴相切的谷点处，$\displaystyle f'(x)$ 由递减转为递增，再产生一个拐点。

所以曲线 $\displaystyle y=f(x)$ 共有三个拐点。

故选 B。

</details>

### （四）结合极限

1. **【1987-12-3 分】** 设 $\displaystyle \lim_{x\to a}\dfrac{f(x)-f(a)}{(x-a)^2}=-1$，则在点 $\displaystyle x=a$ 处( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f(x)$ 导数存在，$\displaystyle f'(a)\neq0$ &emsp;&emsp; B. $\displaystyle f(x)$ 取得极大值</div>
<div className="choice-option-row">C. $\displaystyle f(x)$ 取得极小值 &emsp;&emsp; D. $\displaystyle f(x)$ 的导数不存在</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 由极限为 $\displaystyle -1$ 可知，当 $\displaystyle x$ 充分接近 $\displaystyle a$ 且 $\displaystyle x\neq a$ 时，

$$
\displaystyle
\dfrac{f(x)-f(a)}{(x-a)^2}<0.
$$

由于 $\displaystyle (x-a)^2>0$，故

$$
\displaystyle
f(x)-f(a)<0,
$$

即

$$
\displaystyle
f(x)<f(a).
$$

因此 $\displaystyle f(a)$ 是函数的极大值。

另外，

$$
\displaystyle
\dfrac{f(x)-f(a)}{x-a}
=(x-a)\dfrac{f(x)-f(a)}{(x-a)^2}\to0,
$$

所以 $\displaystyle f'(a)=0$。故 A、D 均不正确。

故选 B。

</details>

---

2. **【1990-123-3 分】** 已知 $\displaystyle f(x)$ 在 $\displaystyle x=0$ 的某个邻域内连续，且 $\displaystyle f(0)=0$，$\displaystyle \lim_{x\to0}\dfrac{f(x)}{1-\cos x}=2$，则在点 $\displaystyle x=0$ 处 $\displaystyle f(x)$( )

<div className="choice-options">
<div className="choice-option-row">A. 不可导 &emsp;&emsp; B. 可导，且 $\displaystyle f'(0)\neq0$ &emsp;&emsp; C. 取得极大值 &emsp;&emsp; D. 取得极小值</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D

**解析：** 由

$$
\displaystyle
\lim_{x\to0}\dfrac{f(x)}{1-\cos x}=2>0
$$

可知，当 $\displaystyle x$ 充分接近 $\displaystyle 0$ 且 $\displaystyle x\neq0$ 时，$\displaystyle f(x)$ 与 $\displaystyle 1-\cos x$ 同号。

又因为

$$
\displaystyle
1-\cos x>0\qquad(x\neq0),
$$

所以在零点的某个去心邻域内有

$$
\displaystyle
f(x)>0=f(0).
$$

因此 $\displaystyle x=0$ 是 $\displaystyle f(x)$ 的极小值点，函数在该点取得极小值。

事实上，由 $\displaystyle 1-\cos x\sim\dfrac{x^2}{2}$，还有

$$
\displaystyle
f(x)\sim x^2,
$$

从而 $\displaystyle f'(0)=0$，故 B 也不成立。

故选 D。

</details>

---

3. **【1996-12-3 分】** 设 $\displaystyle f(x)$ 具有二阶连续导数，且 $\displaystyle f'(0)=0$，$\displaystyle \lim_{x\to0}\dfrac{f''(x)}{|x|}=1$，则( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f(0)$ 是 $\displaystyle f(x)$ 的极大值</div>
<div className="choice-option-row">B. $\displaystyle f(0)$ 是 $\displaystyle f(x)$ 的极小值</div>
<div className="choice-option-row">C. $\displaystyle (0,f(0))$ 是曲线 $\displaystyle y=f(x)$ 的拐点</div>
<div className="choice-option-row">D. $\displaystyle f(0)$ 不是 $\displaystyle f(x)$ 的极值，$\displaystyle (0,f(0))$ 也不是曲线 $\displaystyle y=f(x)$ 的拐点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 由

$$
\displaystyle
\lim_{x\to0}\dfrac{f''(x)}{|x|}=1>0,
$$

可知当 $\displaystyle x$ 充分接近 $\displaystyle 0$ 且 $\displaystyle x\neq0$ 时，

$$
\displaystyle
f''(x)>0.
$$

因此 $\displaystyle f'(x)$ 在零点附近严格单调增加。又因为 $\displaystyle f'(0)=0$，所以

$$
\displaystyle
f'(x)<0\quad(x<0),\qquad f'(x)>0\quad(x>0).
$$

于是 $\displaystyle f(x)$ 在 $\displaystyle x=0$ 左侧递减、右侧递增，故 $\displaystyle f(0)$ 是 $\displaystyle f(x)$ 的极小值。

另一方面，$\displaystyle f''(x)$ 在零点两侧均为正，凹凸性不发生改变，因此 $\displaystyle (0,f(0))$ 不是拐点。

故选 B。

</details>

---

4. **【1998-2-3 分】** 设函数 $\displaystyle f(x)$ 在 $\displaystyle x=a$ 的某个邻域内连续，且 $\displaystyle f(a)$ 为极大值，则存在 $\displaystyle \delta>0$，当 $\displaystyle x\in(a-\delta,a+\delta)$ 时，必有( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle (x-a)[f(x)-f(a)]\geq0$</div>
<div className="choice-option-row">B. $\displaystyle (x-a)[f(x)-f(a)]\leq0$</div>
<div className="choice-option-row">C. $\displaystyle \lim_{t\to a}\dfrac{f(t)-f(x)}{(t-x)^2}\geq0\quad(x\neq a)$</div>
<div className="choice-option-row">D. $\displaystyle \lim_{t\to a}\dfrac{f(t)-f(x)}{(t-x)^2}\leq0\quad(x\neq a)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C

**解析：** 因为 $\displaystyle f(a)$ 是极大值，所以存在 $\displaystyle \delta>0$，使得当

$$
\displaystyle
x\in(a-\delta,a+\delta)
$$

时，恒有

$$
\displaystyle
f(x)\leq f(a).
$$

固定任意 $\displaystyle x\neq a$，由 $\displaystyle f$ 在 $\displaystyle a$ 处连续，得

$$
\displaystyle
\lim_{t\to a}\dfrac{f(t)-f(x)}{(t-x)^2}
=
\dfrac{f(a)-f(x)}{(a-x)^2}.
$$

其中

$$
\displaystyle
f(a)-f(x)\geq0,
\qquad
(a-x)^2>0,
$$

故

$$
\displaystyle
\lim_{t\to a}\dfrac{f(t)-f(x)}{(t-x)^2}\geq0.
$$

因此 C 必然成立。

对于 A、B，当 $\displaystyle x$ 位于 $\displaystyle a$ 的两侧时，$\displaystyle x-a$ 的符号不同，故均不能在整个邻域内恒成立。

故选 C。

</details>

---

5. **【2001-34-3 分】** 设 $\displaystyle f(x)$ 的导数在 $\displaystyle x=a$ 处连续，又 $\displaystyle \lim_{x\to a}\dfrac{f'(x)}{x-a}=-1$，则( )

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle x=a$ 是 $\displaystyle f(x)$ 的极小值点</div>
<div className="choice-option-row">B. $\displaystyle x=a$ 是 $\displaystyle f(x)$ 的极大值点</div>
<div className="choice-option-row">C. $\displaystyle (a,f(a))$ 是曲线 $\displaystyle y=f(x)$ 的拐点</div>
<div className="choice-option-row">D. $\displaystyle x=a$ 不是 $\displaystyle f(x)$ 的极值点，$\displaystyle (a,f(a))$ 也不是曲线 $\displaystyle y=f(x)$ 的拐点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B

**解析：** 由于 $\displaystyle f'(x)$ 在 $\displaystyle x=a$ 处连续，且

$$
\displaystyle
\lim_{x\to a}\dfrac{f'(x)}{x-a}=-1,
$$

首先有 $\displaystyle f'(a)=0$。否则当 $\displaystyle x\to a$ 时，分子趋于非零常数，而分母趋于零，极限不可能为有限值。

又由极限为负数可知，当 $\displaystyle x$ 充分接近 $\displaystyle a$ 且 $\displaystyle x\neq a$ 时，

$$
\displaystyle
\dfrac{f'(x)}{x-a}<0.
$$

因此：

- 当 $\displaystyle x<a$ 时，$\displaystyle x-a<0$，从而 $\displaystyle f'(x)>0$；
- 当 $\displaystyle x>a$ 时，$\displaystyle x-a>0$，从而 $\displaystyle f'(x)<0$。

所以 $\displaystyle f(x)$ 在 $\displaystyle a$ 的左侧递增、右侧递减，故 $\displaystyle x=a$ 是 $\displaystyle f(x)$ 的极大值点。

故选 B。

</details>

## 大题

### （一）对概念的直接考查

1. **【1988-45-6 分】** $\displaystyle f(x)=\int_0^xe^{-\dfrac{1}{2}t^2}\mathrm{d}t$，求：

(1)$\displaystyle f'(x)$；(2)单调性；(3)奇偶；(4)拐点；(5)凹凸；(6)水平渐近线。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) $\displaystyle f'(x)=e^{-\dfrac{x^2}{2}}$；

(2) $\displaystyle f(x)$ 在 $\displaystyle (-\infty,+\infty)$ 上严格单调增加；

(3) $\displaystyle f(x)$ 为奇函数；

(4) 拐点为 $\displaystyle (0,0)$；

(5) 凹区间为 $\displaystyle (-\infty,0)$，凸区间为 $\displaystyle (0,+\infty)$；

(6) 水平渐近线为

$$
\displaystyle y=\sqrt{\dfrac{\pi}{2}},\qquad y=-\sqrt{\dfrac{\pi}{2}}.
$$

**解析：** 由变上限积分求导公式，得

$$
\displaystyle
f'(x)=e^{-\dfrac{x^2}{2}}>0.
$$

因此 $\displaystyle f(x)$ 在整个实数轴上严格单调增加。

被积函数 $\displaystyle e^{-t^2/2}$ 是偶函数，故

$$
\displaystyle
f(-x)=\int_0^{-x}e^{-\dfrac{t^2}{2}}\mathrm{d}t
=-\int_0^xe^{-\dfrac{u^2}{2}}\mathrm{d}u
=-f(x),
$$

所以 $\displaystyle f(x)$ 为奇函数，且 $\displaystyle f(0)=0$。

再求二阶导数：

$$
\displaystyle
f''(x)=-xe^{-\dfrac{x^2}{2}}.
$$

由于指数因子恒正，故

$$
\displaystyle
f''(x)>0\quad(x<0),
\qquad
f''(x)<0\quad(x>0).
$$

于是曲线在 $\displaystyle (-\infty,0)$ 上为凹的，在 $\displaystyle (0,+\infty)$ 上为凸的；二阶导数在 $\displaystyle x=0$ 两侧变号，因此拐点为

$$
\displaystyle (0,f(0))=(0,0).
$$

最后，由高斯积分

$$
\displaystyle
\int_0^{+\infty}e^{-\dfrac{t^2}{2}}\mathrm{d}t
=\sqrt{\dfrac{\pi}{2}},
$$

得

$$
\displaystyle
\lim_{x\to+\infty}f(x)=\sqrt{\dfrac{\pi}{2}}.
$$

又因 $\displaystyle f$ 为奇函数，

$$
\displaystyle
\lim_{x\to-\infty}f(x)=-\sqrt{\dfrac{\pi}{2}}.
$$

故两条水平渐近线分别为

$$
\displaystyle
y=\sqrt{\dfrac{\pi}{2}},
\qquad
y=-\sqrt{\dfrac{\pi}{2}}.
$$

</details>

---

2. **【1988-3-12 分】** $\displaystyle y=\dfrac6{x^2-2x+4}$，填表：

| 单调增加区间 |
| ------------ |
| 单调减少区间 |
| 极值点       |
| 极值         |
| 凹(∪)区间    |
| 凸(∩)区间    |
| 拐点         |
| 渐近线       |

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

| 项目 | 结果 |
| --- | --- |
| 单调增加区间 | $\displaystyle (-\infty,1)$ |
| 单调减少区间 | $\displaystyle (1,+\infty)$ |
| 极值点 | $\displaystyle x=1$ |
| 极值 | 极大值 $\displaystyle y(1)=2$ |
| 凹(∪)区间 | $\displaystyle (-\infty,0)\cup(2,+\infty)$ |
| 凸(∩)区间 | $\displaystyle (0,2)$ |
| 拐点 | $\displaystyle \left(0,\dfrac32\right)$，$\displaystyle \left(2,\dfrac32\right)$ |
| 渐近线 | 水平渐近线 $\displaystyle y=0$ |

</div>
</div>

**解析：** 将函数写成

$$
\displaystyle
y=\dfrac{6}{(x-1)^2+3}.
$$

定义域为 $\displaystyle \mathbb R$。求导得

$$
\displaystyle
y'=-\dfrac{12(x-1)}{\left[(x-1)^2+3\right]^2}.
$$

分母恒正，因此

$$
\displaystyle
y'>0\quad(x<1),
\qquad
y'<0\quad(x>1).
$$

所以函数在 $\displaystyle (-\infty,1)$ 上增加，在 $\displaystyle (1,+\infty)$ 上减少，并在 $\displaystyle x=1$ 处取得极大值

$$
\displaystyle
y(1)=\dfrac63=2.
$$

二阶导数为

$$
\displaystyle
y''=\dfrac{36\left[(x-1)^2-1\right]}{\left[(x-1)^2+3\right]^3}
=\dfrac{36x(x-2)}{\left[(x-1)^2+3\right]^3}.
$$

故

$$
\displaystyle
y''>0\quad(x<0\text{ 或 }x>2),
$$

$$
\displaystyle
y''<0\quad(0<x<2).
$$

因此凹区间为 $\displaystyle (-\infty,0)\cup(2,+\infty)$，凸区间为 $\displaystyle (0,2)$。

在 $\displaystyle x=0,2$ 处二阶导数变号，且

$$
\displaystyle
y(0)=y(2)=\dfrac64=\dfrac32,
$$

故拐点为

$$
\displaystyle
\left(0,\dfrac32\right),
\qquad
\left(2,\dfrac32\right).
$$

又

$$
\displaystyle
\lim_{x\to\pm\infty}\dfrac6{x^2-2x+4}=0,
$$

所以水平渐近线为 $\displaystyle y=0$。由于分母恒大于零，不存在垂直渐近线。

</details>

---

3. **【1989-5-12 分】** $\displaystyle y=\dfrac{2x^2}{(1-x)^2}$，求单调区间、极值、凹凸、拐点、渐近线并作图。

<details>
<summary>查看答案与解析</summary>

**答案：** - 单调减少区间：$\displaystyle (-\infty,0)$，$\displaystyle (1,+\infty)$；
- 单调增加区间：$\displaystyle (0,1)$；
- 极小值点：$\displaystyle x=0$，极小值为 $\displaystyle 0$；
- 凸区间：$\displaystyle \left(-\infty,-\dfrac12\right)$；
- 凹区间：$\displaystyle \left(-\dfrac12,1\right)\cup(1,+\infty)$；
- 拐点：$\displaystyle \left(-\dfrac12,\dfrac29\right)$；
- 垂直渐近线：$\displaystyle x=1$；
- 水平渐近线：$\displaystyle y=2$。

**解析：** 函数定义域为

$$
\displaystyle
(-\infty,1)\cup(1,+\infty).
$$

求一阶导数：

$$
\displaystyle
y'=\dfrac{-4x}{(x-1)^3}.
$$

因为临界点为 $\displaystyle x=0$，间断点为 $\displaystyle x=1$，考察导数符号可得

$$
\displaystyle
y'<0\quad(x<0),
$$

$$
\displaystyle
y'>0\quad(0<x<1),
$$

$$
\displaystyle
y'<0\quad(x>1).
$$

所以函数在 $\displaystyle (-\infty,0)$ 和 $\displaystyle (1,+\infty)$ 上减少，在 $\displaystyle (0,1)$ 上增加。

在 $\displaystyle x=0$ 处，导数由负变正，故有极小值

$$
\displaystyle
y(0)=0.
$$

再求二阶导数：

$$
\displaystyle
y''=\dfrac{4(2x+1)}{(x-1)^4}.
$$

分母恒正，所以二阶导数的符号由 $\displaystyle 2x+1$ 决定：

$$
\displaystyle
y''<0\quad\left(x<-\dfrac12\right),
$$

$$
\displaystyle
y''>0\quad\left(x>-\dfrac12,\ x\neq1\right).
$$

因此凸区间为

$$
\displaystyle
\left(-\infty,-\dfrac12\right),
$$

凹区间为

$$
\displaystyle
\left(-\dfrac12,1\right)\cup(1,+\infty).
$$

在 $\displaystyle x=-\dfrac12$ 处二阶导数变号，且

$$
\displaystyle
y\left(-\dfrac12\right)
=\dfrac{2\cdot\dfrac14}{\left(1+\dfrac12\right)^2}
=\dfrac29,
$$

故拐点为

$$
\displaystyle
\left(-\dfrac12,\dfrac29\right).
$$

当 $\displaystyle x\to1^-$ 或 $\displaystyle x\to1^+$ 时，

$$
\displaystyle
y\to+\infty,
$$

故 $\displaystyle x=1$ 是垂直渐近线。

又

$$
\displaystyle
\lim_{x\to\pm\infty}\dfrac{2x^2}{(1-x)^2}=2,
$$

故 $\displaystyle y=2$ 是水平渐近线。

作图时还应注意：曲线经过原点，且除原点外函数值均为正；左支从水平渐近线 $\displaystyle y=2$ 附近下降到原点，再上升至 $\displaystyle +\infty$；右支从 $\displaystyle +\infty$ 单调下降并趋近于 $\displaystyle y=2$。

</details>

---

4. **【1993-123-7 分】** 作 $\displaystyle y=(x+6)e^{\dfrac{1}{x}}$ 图像。

<details>
<summary>查看答案与解析</summary>

**答案：** 函数的主要特征如下：

- 定义域：$\displaystyle (-\infty,0)\cup(0,+\infty)$；
- 与 $\displaystyle x$ 轴交点：$\displaystyle (-6,0)$；
- 单调增加区间：$\displaystyle (-\infty,-2)$，$\displaystyle (3,+\infty)$；
- 单调减少区间：$\displaystyle (-2,0)$，$\displaystyle (0,3)$；
- 极大值点：$\displaystyle \left(-2,4e^{-1/2}\right)$；
- 极小值点：$\displaystyle \left(3,9e^{1/3}\right)$；
- 凸区间：$\displaystyle \left(-\infty,-\dfrac6{13}\right)$；
- 凹区间：$\displaystyle \left(-\dfrac6{13},0\right)\cup(0,+\infty)$；
- 拐点：$\displaystyle \left(-\dfrac6{13},\dfrac{72}{13}e^{-13/6}\right)$；
- 垂直渐近线：$\displaystyle x=0$（右侧）；
- 斜渐近线：$\displaystyle y=x+7$。

**解析：** 函数定义域为 $\displaystyle x\neq0$。一阶导数为

$$
\displaystyle
y'=e^{1/x}\left(1-\dfrac{x+6}{x^2}\right)
=e^{1/x}\dfrac{(x-3)(x+2)}{x^2}.
$$

由于 $\displaystyle e^{1/x}>0$ 且 $\displaystyle x^2>0$，导数符号由 $\displaystyle (x-3)(x+2)$ 决定。因此

$$
\displaystyle
y'>0\quad(x<-2\text{ 或 }x>3),
$$

$$
\displaystyle
y'<0\quad(-2<x<0\text{ 或 }0<x<3).
$$

所以函数在 $\displaystyle (-\infty,-2)$、$\displaystyle (3,+\infty)$ 上增加，在 $\displaystyle (-2,0)$、$\displaystyle (0,3)$ 上减少。

于是

$$
\displaystyle
y(-2)=4e^{-1/2}
$$

为极大值，

$$
\displaystyle
y(3)=9e^{1/3}
$$

为极小值。

二阶导数为

$$
\displaystyle
y''=e^{1/x}\dfrac{13x+6}{x^4}.
$$

故

$$
\displaystyle
y''<0\quad\left(x<-\dfrac6{13}\right),
$$

$$
\displaystyle
y''>0\quad\left(x>-\dfrac6{13},\ x\neq0\right).
$$

因此凸区间为

$$
\displaystyle
\left(-\infty,-\dfrac6{13}\right),
$$

凹区间为

$$
\displaystyle
\left(-\dfrac6{13},0\right)\cup(0,+\infty).
$$

在 $\displaystyle x=-\dfrac6{13}$ 处二阶导数变号，故拐点为

$$
\displaystyle
\left(-\dfrac6{13},
\left(6-\dfrac6{13}\right)e^{-13/6}\right)
=
\left(-\dfrac6{13},\dfrac{72}{13}e^{-13/6}\right).
$$

函数与 $\displaystyle x$ 轴的交点由 $\displaystyle x+6=0$ 得

$$
\displaystyle (-6,0).
$$

考察 $\displaystyle x=0$ 附近：

$$
\displaystyle
\lim_{x\to0^+}(x+6)e^{1/x}=+\infty,
$$

$$
\displaystyle
\lim_{x\to0^-}(x+6)e^{1/x}=0.
$$

所以 $\displaystyle x=0$ 是右侧垂直渐近线。

再求无穷远处的斜渐近线。由

$$
\displaystyle
e^{1/x}=1+\dfrac1x+o\left(\dfrac1x\right),
$$

得

$$
\displaystyle
(x+6)e^{1/x}
=x+7+o(1).
$$

因此斜渐近线为

$$
\displaystyle y=x+7.
$$

据以上性质即可准确作出图像：左支经过 $\displaystyle (-6,0)$，在 $\displaystyle x=-2$ 处取得极大值，随后趋于原点上方；右支从 $\displaystyle x=0^+$ 处的正无穷下降至 $\displaystyle x=3$ 处极小值，再上升并趋近斜渐近线。

</details>

---

5. **【1994-3-9 分】** $\displaystyle y=\dfrac{x^3+4}{x^2}$：

(1)增减极值；(2)凹凸拐点；(3)渐近线；(4)作图。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) 单调增加区间为 $\displaystyle (-\infty,0)$、$\displaystyle (2,+\infty)$，单调减少区间为 $\displaystyle (0,2)$；在 $\displaystyle x=2$ 处取得极小值 $\displaystyle 3$。

(2) 凹区间为 $\displaystyle (-\infty,0)\cup(0,+\infty)$，无凸区间，无拐点。

(3) 垂直渐近线为 $\displaystyle x=0$，斜渐近线为 $\displaystyle y=x$。

(4) 曲线与 $\displaystyle x$ 轴交于 $\displaystyle (-\sqrt[3]{4},0)$，据上述性质作图。

**解析：** 先化简为

$$
\displaystyle
y=x+\dfrac4{x^2},
\qquad x\neq0.
$$

求一阶导数：

$$
\displaystyle
y'=1-\dfrac8{x^3}
=\dfrac{x^3-8}{x^3}.
$$

由于

$$
\displaystyle
x^3-8=(x-2)(x^2+2x+4),
$$

且 $\displaystyle x^2+2x+4>0$，故导数符号如下：

$$
\displaystyle
y'>0\quad(x<0),
$$

$$
\displaystyle
y'<0\quad(0<x<2),
$$

$$
\displaystyle
y'>0\quad(x>2).
$$

因此函数在 $\displaystyle (-\infty,0)$ 和 $\displaystyle (2,+\infty)$ 上增加，在 $\displaystyle (0,2)$ 上减少。

在 $\displaystyle x=2$ 处导数由负变正，故取得极小值

$$
\displaystyle
y(2)=2+\dfrac4{2^2}=3.
$$

再求二阶导数：

$$
\displaystyle
y''=\dfrac{24}{x^4}>0\qquad(x\neq0).
$$

所以曲线在定义域的两个区间 $\displaystyle (-\infty,0)$ 与 $\displaystyle (0,+\infty)$ 上均为凹的，不存在凸区间。虽然 $\displaystyle x=0$ 将定义域分开，但函数在该点无定义，且二阶导数并未发生符号改变，因此不存在拐点。

当 $\displaystyle x\to0^\pm$ 时，

$$
\displaystyle
y=x+\dfrac4{x^2}\to+\infty,
$$

所以垂直渐近线为

$$
\displaystyle x=0.
$$

又

$$
\displaystyle
\lim_{x\to\pm\infty}\left(y-x\right)
=\lim_{x\to\pm\infty}\dfrac4{x^2}=0,
$$

所以斜渐近线为

$$
\displaystyle y=x.
$$

横轴交点由

$$
\displaystyle
x^3+4=0
$$

得

$$
\displaystyle x=-\sqrt[3]{4}.
$$

因此曲线经过 $\displaystyle (-\sqrt[3]{4},0)$。左支从负无穷处沿斜渐近线 $\displaystyle y=x$ 上升，穿过横轴后趋于 $\displaystyle +\infty$；右支从 $\displaystyle +\infty$ 下降至极小点 $\displaystyle (2,3)$，再上升并趋近斜渐近线 $\displaystyle y=x$。

</details>

---

6. **【1996-3-8 分】** $2y^3-2y^2+2xy-x^2=1$确定$y(x)$，求驻点并判别极值。

<details>
<summary>查看答案与解析</summary>

**答案：** 曲线所确定的函数 $y=y(x)$ 只有一个驻点 $(1,1)$，且该点为极小值点，极小值为 $1$。

**解析：** 令

$$
\displaystyle
F(x,y)=2y^3-2y^2+2xy-x^2-1.
$$

由隐函数求导法，对方程 $F(x,y)=0$ 两边关于 $x$ 求导，得

$$
\displaystyle
(6y^2-4y+2x)y'+2y-2x=0.
$$

于是

$$
\displaystyle
y'=\dfrac{x-y}{3y^2-2y+x}.
$$

驻点处应有 $y'=0$，故

$$
\displaystyle
x=y.
$$

将 $x=y$ 代入原方程，得

$$
\displaystyle
2y^3-y^2-1=0.
$$

因

$$
\displaystyle
2y^3-y^2-1=(y-1)(2y^2+y+1),
$$

而 $2y^2+y+1>0$，故唯一实根为

$$
\displaystyle
y=1,
$$

从而

$$
\displaystyle
x=1.
$$

在点 $(1,1)$ 处，

$$
\displaystyle
F_y=6y^2-4y+2x=4\neq0,
$$

故该曲线在此点附近确实可以确定可微函数 $y=y(x)$。

再对关系

$$
\displaystyle
F_x+F_y y'=0
$$

求导，得

$$
\displaystyle
F_{xx}+2F_{xy}y'+F_{yy}(y')^2+F_y y''=0.
$$

在驻点 $(1,1)$ 处，$y'=0$，且

$$
\displaystyle
F_{xx}=-2,
\qquad
F_y=4.
$$

所以

$$
\displaystyle
y''(1)=-\dfrac{F_{xx}}{F_y}
=\dfrac12>0.
$$

因此 $x=1$ 为极小值点，极小值为

$$
\displaystyle
y(1)=1.
$$

</details>

---

7. **【1999-2-8 分】** $y=\dfrac{x^3}{(x-1)^2}$：(1)单调极值；(2)凹凸拐点；(3)渐近线。

<details>
<summary>查看答案与解析</summary>

**答案：** (1) 单调增加区间为 $(-\infty,1)$、$(3,+\infty)$，单调减少区间为 $(1,3)$；在 $x=3$ 处取得极小值 $\dfrac{27}{4}$。$x=0$ 为驻点，但不是极值点。

(2) 凸区间为 $(-\infty,0)$，凹区间为 $(0,1)$、$(1,+\infty)$；拐点为 $(0,0)$。

(3) 垂直渐近线为 $x=1$，斜渐近线为 $y=x+2$。

**解析：** 函数的定义域为

$$
\displaystyle
(-\infty,1)\cup(1,+\infty).
$$

求一阶导数：

$$
\displaystyle
y'=\dfrac{3x^2(x-1)^2-2x^3(x-1)}{(x-1)^4}
=\dfrac{x^2(x-3)}{(x-1)^3}.
$$

由于 $x^2\ge0$，根据 $x-3$ 与 $x-1$ 的符号可得：

$$
\displaystyle
y'>0,
\qquad x\in(-\infty,1)\cup(3,+\infty),
$$

$$
\displaystyle
y'<0,
\qquad x\in(1,3).
$$

因此函数在 $(-\infty,1)$ 与 $(3,+\infty)$ 上单调增加，在 $(1,3)$ 上单调减少。

虽然 $y'(0)=0$，但导数在 $x=0$ 两侧均为正，故 $x=0$ 不是极值点。在 $x=3$ 处，导数由负变正，所以函数取得极小值

$$
\displaystyle
y(3)=\dfrac{27}{4}.
$$

再求二阶导数：

$$
\displaystyle
y''=\dfrac{6x}{(x-1)^4}.
$$

因分母恒为正，故 $y''$ 的符号由 $x$ 决定：

$$
\displaystyle
y''<0,
\qquad x<0,
$$

$$
\displaystyle
y''>0,
\qquad x>0,
\quad x\neq1.
$$

所以曲线在 $(-\infty,0)$ 上为凸的，在 $(0,1)$ 和 $(1,+\infty)$ 上为凹的。二阶导数在 $x=0$ 两侧变号，且函数在 $x=0$ 处连续，因此拐点为

$$
\displaystyle
(0,y(0))=(0,0).
$$

当 $x\to1^\pm$ 时，

$$
\displaystyle
y=\dfrac{x^3}{(x-1)^2}\to+\infty,
$$

故垂直渐近线为

$$
\displaystyle
x=1.
$$

作多项式除法：

$$
\displaystyle
\dfrac{x^3}{(x-1)^2}
=x+2+\dfrac{3x-2}{(x-1)^2}.
$$

于是

$$
\displaystyle
\lim_{x\to\pm\infty}\left[y-(x+2)\right]
=\lim_{x\to\pm\infty}\dfrac{3x-2}{(x-1)^2}=0.
$$

故斜渐近线为

$$
\displaystyle
y=x+2.
$$

</details>

---

8. **【2000-34-7 分】** $y=(x-1)e^{\dfrac{\pi}{2}+\arctan x}$，求单调极值、渐近线。

<details>
<summary>查看答案与解析</summary>

**答案：** 函数在 $(-\infty,-1)$ 和 $(0,+\infty)$ 上单调增加，在 $(-1,0)$ 上单调减少。

在 $x=-1$ 处取得极大值

$$
\displaystyle
y(-1)=-2e^{\frac{\pi}{4}},
$$

在 $x=0$ 处取得极小值

$$
\displaystyle
y(0)=-e^{\frac{\pi}{2}}.
$$

当 $x\to-\infty$ 时，斜渐近线为

$$
\displaystyle
y=x-2;
$$

当 $x\to+\infty$ 时，斜渐近线为

$$
\displaystyle
y=e^\pi x-2e^\pi.
$$

**解析：** 求导得

$$
\displaystyle
y'
=e^{\frac{\pi}{2}+\arctan x}
\left(1+\dfrac{x-1}{1+x^2}\right).
$$

整理得

$$
\displaystyle
y'
=e^{\frac{\pi}{2}+\arctan x}
\dfrac{x(x+1)}{1+x^2}.
$$

由于指数因子及分母均为正，故 $y'$ 的符号由 $x(x+1)$ 决定。因此

$$
\displaystyle
y'>0,
\qquad x\in(-\infty,-1)\cup(0,+\infty),
$$

$$
\displaystyle
y'<0,
\qquad x\in(-1,0).
$$

所以 $x=-1$ 为极大值点，且

$$
\displaystyle
y(-1)=(-2)e^{\frac{\pi}{2}-\frac{\pi}{4}}
=-2e^{\frac{\pi}{4}}.
$$

$x=0$ 为极小值点，且

$$
\displaystyle
y(0)=-e^{\frac{\pi}{2}}.
$$

下面求渐近线。

当 $x\to+\infty$ 时，

$$
\displaystyle
\arctan x
=\dfrac{\pi}{2}-\dfrac1x+o\left(\dfrac1x\right).
$$

故

$$
\displaystyle
e^{\frac{\pi}{2}+\arctan x}
=e^\pi\left[1-\dfrac1x+o\left(\dfrac1x\right)\right].
$$

于是

$$
\displaystyle
y
=(x-1)e^\pi\left[1-\dfrac1x+o\left(\dfrac1x\right)\right]
=e^\pi x-2e^\pi+o(1).
$$

因此当 $x\to+\infty$ 时的斜渐近线为

$$
\displaystyle
y=e^\pi x-2e^\pi.
$$

当 $x\to-\infty$ 时，

$$
\displaystyle
\arctan x
=-\dfrac{\pi}{2}-\dfrac1x+o\left(\dfrac1x\right),
$$

从而

$$
\displaystyle
e^{\frac{\pi}{2}+\arctan x}
=1-\dfrac1x+o\left(\dfrac1x\right).
$$

因此

$$
\displaystyle
y
=(x-1)\left[1-\dfrac1x+o\left(\dfrac1x\right)\right]
=x-2+o(1).
$$

故当 $x\to-\infty$ 时的斜渐近线为

$$
\displaystyle
y=x-2.
$$

</details>

---

9. **【2008-4-10 分】** $f(x)=\int_0^1|t^2-x^2|\mathrm{d}t(x>0)$，求$f'(x)$与最值。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
f'(x)=
\begin{cases}
2x(2x-1),&0<x<1,\\
2x,&x\ge1.
\end{cases}
$$

</div>
</div>

函数在 $\left(0,\dfrac12\right)$ 上单调减少，在 $\left(\dfrac12,+\infty\right)$ 上单调增加。

函数在 $x=\dfrac12$ 处取得最小值

$$
\displaystyle
f_{\min}=\dfrac14,
$$

无最大值。

**解析：** 由于 $x>0$，需根据 $x$ 与积分区间端点 $1$ 的大小分类讨论。

当 $0<x<1$ 时，$t^2-x^2$ 在 $t=x$ 处变号，因此

$$
\displaystyle
f(x)
=\int_0^x(x^2-t^2)\,\mathrm dt
+\int_x^1(t^2-x^2)\,\mathrm dt.
$$

计算得

$$
\displaystyle
f(x)=\dfrac13-x^2+\dfrac43x^3,
\qquad 0<x<1.
$$

当 $x\ge1$ 时，对所有 $t\in[0,1]$ 都有 $t^2-x^2\le0$，故

$$
\displaystyle
f(x)=\int_0^1(x^2-t^2)\,\mathrm dt
=x^2-\dfrac13.
$$

因此

$$
\displaystyle
f(x)=
\begin{cases}
\dfrac13-x^2+\dfrac43x^3,&0<x<1,\\[4pt]
x^2-\dfrac13,&x\ge1.
\end{cases}
$$

求导得

$$
\displaystyle
f'(x)=
\begin{cases}
-2x+4x^2=2x(2x-1),&0<x<1,\\[4pt]
2x,&x>1.
\end{cases}
$$

在 $x=1$ 处，左、右导数均为 $2$，故

$$
\displaystyle
f'(1)=2.
$$

于是可写成

$$
\displaystyle
f'(x)=
\begin{cases}
2x(2x-1),&0<x<1,\\
2x,&x\ge1.
\end{cases}
$$

当 $0<x<\dfrac12$ 时，$f'(x)<0$；当 $x>\dfrac12$ 时，$f'(x)>0$。故 $x=\dfrac12$ 为唯一的极小值点，也是全局最小值点。

最小值为

$$
\displaystyle
f\left(\dfrac12\right)
=\dfrac13-\dfrac14+\dfrac43\cdot\dfrac18
=\dfrac14.
$$

又因

$$
\displaystyle
\lim_{x\to+\infty}f(x)=+\infty,
$$

所以函数没有最大值。

</details>

---

10. **【2010-2-10 分】** 设 $f(x)=\int_1^{x^2}(x^2-t)e^{-t^2}\mathrm{d}t$，求 $f(x)$ 的单调区间与极值。

<details>
<summary>查看答案与解析</summary>

**答案：** 函数的单调减少区间为

$$
\displaystyle
(-\infty,-1),\qquad(0,1),
$$

单调增加区间为

$$
\displaystyle
(-1,0),\qquad(1,+\infty).
$$

在 $x=0$ 处取得极大值

$$
\displaystyle
f(0)=\dfrac{1-e^{-1}}2;
$$

在 $x=\pm1$ 处取得极小值

$$
\displaystyle
f(\pm1)=0.
$$

其中 $f(0)=\dfrac{1-e^{-1}}2$ 是局部极大值，$0$ 是函数的全局最小值。

**解析：** 设

$$
\displaystyle
\varphi(x,t)=(x^2-t)e^{-t^2}.
$$

由莱布尼茨公式，

$$
\displaystyle
f'(x)
=\varphi(x,x^2)\cdot2x
+\int_1^{x^2}\dfrac{\partial\varphi}{\partial x}(x,t)\,\mathrm dt.
$$

由于

$$
\displaystyle
\varphi(x,x^2)=0,
\qquad
\dfrac{\partial\varphi}{\partial x}=2xe^{-t^2},
$$

故

$$
\displaystyle
f'(x)=2x\int_1^{x^2}e^{-t^2}\,\mathrm dt.
$$

因被积函数 $e^{-t^2}>0$，所以积分的符号只取决于上下限的大小：

$$
\displaystyle
\int_1^{x^2}e^{-t^2}\,\mathrm dt
\begin{cases}
>0,&|x|>1,\\
=0,&|x|=1,\\
<0,&|x|<1.
\end{cases}
$$

再结合因子 $2x$ 的符号，得到

$$
\displaystyle
f'(x)<0,
\qquad x\in(-\infty,-1)\cup(0,1),
$$

$$
\displaystyle
f'(x)>0,
\qquad x\in(-1,0)\cup(1,+\infty).
$$

因此函数在 $(-\infty,-1)$ 和 $(0,1)$ 上单调减少，在 $(-1,0)$ 和 $(1,+\infty)$ 上单调增加。

在 $x=-1$ 与 $x=1$ 处，导数均由负变正，因此均为极小值点，且

$$
\displaystyle
f(\pm1)=\int_1^1(1-t)e^{-t^2}\,\mathrm dt=0.
$$

在 $x=0$ 处，导数由正变负，因此为极大值点，且

$$
\displaystyle
f(0)
=\int_1^0(-t)e^{-t^2}\,\mathrm dt
=\dfrac{1-e^{-1}}2.
$$

当 $|x|>1$ 时，积分区间为 $[1,x^2]$，且在其内部有 $x^2-t\ge0$，故 $f(x)\ge0$；当 $|x|<1$ 时，将积分反向后同样可得 $f(x)>0$。因此

$$
\displaystyle
f(x)\ge0,
$$

且仅在 $x=\pm1$ 时取等号，所以极小值 $0$ 同时也是全局最小值。函数随 $|x|\to\infty$ 趋于 $+\infty$，故不存在全局最大值。

</details>

---

11. **【2011-2-10 分】** 设函数 $y=y(x)$ 由参数方程

$$
\displaystyle
\begin{cases}
x=\dfrac13t^3+t+\dfrac13,\\[4pt]
y=\dfrac13t^3-t+\dfrac13
\end{cases}
$$

确定，求函数 $y=y(x)$ 的极值和曲线 $y=y(x)$ 的凹凸区间及拐点。

<details>
<summary>查看答案与解析</summary>

**答案：** 极大值为 $1$，在 $x=-1$ 处取得；极小值为 $-\dfrac13$，在 $x=\dfrac53$ 处取得。

曲线的凸（$\cap$）区间为

$$
\displaystyle
\left(-\infty,\dfrac13\right),
$$

凹（$\cup$）区间为

$$
\displaystyle
\left(\dfrac13,+\infty\right),
$$

拐点为

$$
\displaystyle
\left(\dfrac13,\dfrac13\right).
$$

**解析：** 由

$$
\displaystyle
\dfrac{\mathrm dx}{\mathrm dt}=t^2+1>0,
$$

可知 $x=x(t)$ 在 $(-\infty,+\infty)$ 上严格增加，因此参数 $t$ 与自变量 $x$ 的增减方向一致。

参数方程所确定函数的一阶导数为

$$
\displaystyle
\dfrac{\mathrm dy}{\mathrm dx}
=\dfrac{\dfrac{\mathrm dy}{\mathrm dt}}{\dfrac{\mathrm dx}{\mathrm dt}}
=\dfrac{t^2-1}{t^2+1}.
$$

令 $\dfrac{\mathrm dy}{\mathrm dx}=0$，得

$$
\displaystyle
t=\pm1.
$$

当 $t<-1$ 或 $t>1$ 时，$\dfrac{\mathrm dy}{\mathrm dx}>0$；当 $-1<t<1$ 时，$\dfrac{\mathrm dy}{\mathrm dx}<0$。因此：

- 当 $t=-1$ 时，导数由正变负，函数取得极大值；
- 当 $t=1$ 时，导数由负变正，函数取得极小值。

将 $t=-1$ 代入参数方程，得

$$
\displaystyle
x=-1,\qquad y=1;
$$

将 $t=1$ 代入参数方程，得

$$
\displaystyle
x=\dfrac53,\qquad y=-\dfrac13.
$$

再求二阶导数：

$$
\displaystyle
\dfrac{\mathrm d}{\mathrm dt}
\left(\dfrac{\mathrm dy}{\mathrm dx}\right)
=\dfrac{4t}{(t^2+1)^2},
$$

故

$$
\displaystyle
\dfrac{\mathrm d^2y}{\mathrm dx^2}
=\dfrac{\dfrac{\mathrm d}{\mathrm dt}
\left(\dfrac{\mathrm dy}{\mathrm dx}\right)}
{\dfrac{\mathrm dx}{\mathrm dt}}
=\dfrac{4t}{(t^2+1)^3}.
$$

当 $t<0$ 时，$y''<0$；当 $t>0$ 时，$y''>0$。又因为 $x(t)$ 严格增加，且

$$
\displaystyle
x(0)=\dfrac13,
$$

所以曲线在 $\left(-\infty,\dfrac13\right)$ 上为凸（$\cap$），在 $\left(\dfrac13,+\infty\right)$ 上为凹（$\cup$）。当 $t=0$ 时，

$$
\displaystyle
x=y=\dfrac13,
$$

且二阶导数在该点两侧变号，故拐点为

$$
\displaystyle
\left(\dfrac13,\dfrac13\right).
$$

</details>

---

12. **【2014-2-10 分】** 已知函数 $y=y(x)$ 满足微分方程

$$
\displaystyle
x^2+y^2y'=1-y',
$$

且 $y(2)=0$，求 $y(x)$ 的极大值与极小值。

<details>
<summary>查看答案与解析</summary>

**答案：** 函数在 $x=-1$ 处取得极小值 $0$，在 $x=1$ 处取得极大值 $1$。

**解析：** 原方程可化为

$$
\displaystyle
(1+y^2)y'=1-x^2.
$$

分离变量并积分，得

$$
\displaystyle
(1+y^2)\,\mathrm dy=(1-x^2)\,\mathrm dx,
$$

从而

$$
\displaystyle
y+\dfrac13y^3=x-\dfrac13x^3+C.
$$

利用初始条件 $y(2)=0$，有

$$
\displaystyle
0=2-\dfrac83+C,
$$

故

$$
\displaystyle
C=\dfrac23.
$$

于是函数由隐式方程

$$
\displaystyle
y+\dfrac13y^3
=x-\dfrac13x^3+\dfrac23
$$

确定。

由

$$
\displaystyle
y'=\dfrac{1-x^2}{1+y^2},
$$

且 $1+y^2>0$，可知 $y'$ 的符号完全由 $1-x^2$ 决定：

$$
\displaystyle
y'<0,
\qquad x\in(-\infty,-1)\cup(1,+\infty),
$$

$$
\displaystyle
y'>0,
\qquad x\in(-1,1).
$$

因此，$x=-1$ 为极小值点，$x=1$ 为极大值点。

当 $x=-1$ 时，

$$
\displaystyle
y+\dfrac13y^3=0.
$$

由于函数 $u+\dfrac13u^3$ 严格增加，故唯一解为 $y=0$。

当 $x=1$ 时，

$$
\displaystyle
y+\dfrac13y^3=\dfrac43,
$$

显然 $y=1$，且由同样的单调性知它是唯一解。

故极小值为 $0$，极大值为 $1$。

</details>

---

13. **【2017-12-10 分】** 已知函数 $y=y(x)$ 由方程

$$
\displaystyle
x^3+y^3-3x+3y-2=0
$$

确定，求 $y(x)$ 的极值。

<details>
<summary>查看答案与解析</summary>

**答案：** 函数在 $x=-1$ 处取得极小值 $0$，在 $x=1$ 处取得极大值 $1$。

**解析：** 对隐函数方程两边关于 $x$ 求导，得

$$
\displaystyle
3x^2+3y^2y'-3+3y'=0.
$$

整理得

$$
\displaystyle
(1+y^2)y'=1-x^2,
$$

即

$$
\displaystyle
y'=\dfrac{1-x^2}{1+y^2}.
$$

因为 $1+y^2>0$，所以：

$$
\displaystyle
y'<0,
\qquad x\in(-\infty,-1)\cup(1,+\infty),
$$

$$
\displaystyle
y'>0,
\qquad x\in(-1,1).
$$

因此 $x=-1$ 为极小值点，$x=1$ 为极大值点。

当 $x=-1$ 时，代入原方程得

$$
\displaystyle
-1+y^3+3+3y-2=0,
$$

即

$$
\displaystyle
y^3+3y=0.
$$

故 $y=0$。

当 $x=1$ 时，代入原方程得

$$
\displaystyle
1+y^3-3+3y-2=0,
$$

即

$$
\displaystyle
y^3+3y-4=0.
$$

因 $y=1$ 是其根，且 $u^3+3u-4$ 严格增加，故唯一实根为 $y=1$。

所以函数的极小值为 $0$，极大值为 $1$。

</details>

---

14. **【2019-23-10 分】** 设

$$
\displaystyle
f(x)=
\begin{cases}
xe^x+1,&x\le0,\\[4pt]
x^{2x},&x>0.
\end{cases}
$$

(1)求 $f'(x)$；

(2)求 $f(x)$ 的极值。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
f'(x)=
\begin{cases}
(x+1)e^x,&x<0,\\[4pt]
\text{不存在},&x=0,\\[4pt]
2x^{2x}(\ln x+1),&x>0.
\end{cases}
$$

</div>
</div>

函数在 $x=-1$ 处取得极小值

$$
\displaystyle
1-\dfrac1e,
$$

在 $x=0$ 处取得极大值 $1$，在 $x=\dfrac1e$ 处取得极小值

$$
\displaystyle
e^{-2/e}.
$$

**解析：** 当 $x<0$ 时，

$$
\displaystyle
f'(x)=(xe^x+1)'=(x+1)e^x.
$$

当 $x>0$ 时，写成

$$
\displaystyle
x^{2x}=e^{2x\ln x},
$$

故

$$
\displaystyle
f'(x)=2x^{2x}(\ln x+1).
$$

下面考查 $x=0$ 处的导数。左导数为

$$
\displaystyle
f'_-(0)=\lim_{x\to0^-}(x+1)e^x=1.
$$

右侧有

$$
\displaystyle
\lim_{x\to0^+}\dfrac{x^{2x}-1}{x}.
$$

由于 $2x\ln x\to0$，且 $e^u-1\sim u$，故

$$
\displaystyle
\dfrac{x^{2x}-1}{x}
=\dfrac{e^{2x\ln x}-1}{x}
\sim2\ln x\to-\infty.
$$

所以 $f'_+(0)$ 不存在，进而 $f'(0)$ 不存在。

当 $x<0$ 时，$e^x>0$，故 $f'(x)$ 的符号由 $x+1$ 决定。因此函数在 $(-\infty,-1)$ 上减少，在 $(-1,0)$ 上增加，故 $x=-1$ 为极小值点，

$$
\displaystyle
f(-1)=1-\dfrac1e.
$$

当 $x>0$ 时，$2x^{2x}>0$，故 $f'(x)$ 的符号由 $\ln x+1$ 决定。因此函数在 $\left(0,\dfrac1e\right)$ 上减少，在 $\left(\dfrac1e,+\infty\right)$ 上增加，故 $x=\dfrac1e$ 为极小值点，

$$
\displaystyle
f\left(\dfrac1e\right)
=\left(\dfrac1e\right)^{2/e}
=e^{-2/e}.
$$

又因为

$$
\displaystyle
\lim_{x\to0^-}f(x)=1,
\qquad
\lim_{x\to0^+}f(x)=1=f(0),
$$

且在 $x=0$ 左侧函数增加、右侧函数减少，所以即使 $f'(0)$ 不存在，$x=0$ 仍为极大值点，极大值为 $1$。

</details>

---

15. **【2019-1-10 分】** 设函数 $y=y(x)$ 是微分方程

$$
\displaystyle
y'+xy=e^{-\dfrac{x^2}{2}}
$$

满足条件 $y(0)=0$ 的特解。

(1)求 $y(x)$；

(2)求曲线 $y=y(x)$ 的凹凸区间及拐点。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle
y(x)=xe^{-\dfrac{x^2}{2}}.
$$

</div>
</div>

曲线的凹（$\cup$）区间为

$$
\displaystyle
(-\sqrt3,0)\cup(\sqrt3,+\infty),
$$

凸（$\cap$）区间为

$$
\displaystyle
(-\infty,-\sqrt3)\cup(0,\sqrt3).
$$

拐点为

$$
\displaystyle
\left(-\sqrt3,-\sqrt3e^{-3/2}\right),
\qquad
(0,0),
\qquad
\left(\sqrt3,\sqrt3e^{-3/2}\right).
$$

**解析：** 原方程是一阶线性微分方程，其积分因子为

$$
\displaystyle
\mu(x)=e^{\int x\,\mathrm dx}=e^{x^2/2}.
$$

方程两边同乘 $e^{x^2/2}$，得

$$
\displaystyle
\left(e^{x^2/2}y\right)'=1.
$$

积分得

$$
\displaystyle
e^{x^2/2}y=x+C.
$$

利用 $y(0)=0$，得 $C=0$，故

$$
\displaystyle
y=xe^{-x^2/2}.
$$

求导：

$$
\displaystyle
y'=(1-x^2)e^{-x^2/2},
$$

$$
\displaystyle
y''
=\left(x^3-3x\right)e^{-x^2/2}
=x(x^2-3)e^{-x^2/2}.
$$

因为 $e^{-x^2/2}>0$，所以 $y''$ 的符号由 $x(x^2-3)$ 决定。其零点为

$$
\displaystyle
x=-\sqrt3,\quad0,\quad\sqrt3.
$$

作符号分析可得：

$$
\displaystyle
y''>0,
\qquad x\in(-\sqrt3,0)\cup(\sqrt3,+\infty),
$$

$$
\displaystyle
y''<0,
\qquad x\in(-\infty,-\sqrt3)\cup(0,\sqrt3).
$$

因此上述区间分别为凹（$\cup$）区间和凸（$\cap$）区间。由于 $y''$ 在 $x=-\sqrt3,0,\sqrt3$ 两侧均变号，故三点均为拐点。

代入函数表达式，得对应纵坐标：

$$
\displaystyle
y(-\sqrt3)=-\sqrt3e^{-3/2},
\qquad y(0)=0,
\qquad y(\sqrt3)=\sqrt3e^{-3/2}.
$$

</details>
