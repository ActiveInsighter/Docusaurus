---
title: "一、导数的几何意义和物理意义"
sidebar_position: 1
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 一、导数的几何意义和物理意义

## 小题

### （一）求某点的切线和法线

1. **【1987-3-3 分】** 曲线 $\displaystyle y=\arctan x$ 在横坐标为 $1$ 点处的切线方程为____，法线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** 切线方程为 $\displaystyle y-\dfrac{\pi}{4}=\dfrac{1}{2}(x-1)$；法线方程为 $\displaystyle y-\dfrac{\pi}{4}=-2(x-1)$。

**解析：** 曲线在 $\displaystyle x=1$ 处对应的点为

$$
\displaystyle \left(1,\arctan 1\right)=\left(1,\dfrac{\pi}{4}\right).
$$

由

$$
\displaystyle y'=\dfrac{1}{1+x^2},
$$

得切线斜率

$$
\displaystyle k_{\text{切}}=y'(1)=\dfrac{1}{2}.
$$

因此切线方程为

$$
\displaystyle y-\dfrac{\pi}{4}=\dfrac{1}{2}(x-1).
$$

法线斜率为切线斜率的负倒数，即

$$
\displaystyle k_{\text{法}}=-\dfrac{1}{k_{\text{切}}}=-2,
$$

故法线方程为

$$
\displaystyle y-\dfrac{\pi}{4}=-2(x-1).
$$

</details>

---

2. **【1988-3-4 分】** $\displaystyle f(x)=\dfrac{1}{3}x^3+\dfrac{1}{2}x^2+6x+1$ 的图形在点 $(0,1)$ 处切线与 $x$ 轴交点的坐标是（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle \left(-\dfrac{1}{6},0\right)$ &emsp;&emsp; B. $\displaystyle (-1,0)$ &emsp;&emsp; C. $\displaystyle \left(\dfrac{1}{6},0\right)$ &emsp;&emsp; D. $\displaystyle (1,0)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A。

**解析：** 先求导：

$$
\displaystyle f'(x)=x^2+x+6.
$$

所以曲线在 $\displaystyle x=0$ 处的切线斜率为

$$
\displaystyle f'(0)=6.
$$

切线经过点 $(0,1)$，故切线方程为

$$
\displaystyle y-1=6x,
$$

即

$$
\displaystyle y=6x+1.
$$

令 $\displaystyle y=0$，得

$$
\displaystyle 6x+1=0\quad\Longrightarrow\quad x=-\dfrac{1}{6}.
$$

因此切线与 $x$ 轴的交点为 $\displaystyle \left(-\dfrac{1}{6},0\right)$，故选 A。其余选项均不满足切线方程。

</details>

---

3. **【1989-3-3 分】** 曲线 $\displaystyle y=\int_0^x(t-1)(t-2)\,dt$ 在点 $(0,0)$ 处的切线方程是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=2x$。

**解析：** 由变上限积分求导公式，

$$
\displaystyle y'=(x-1)(x-2).
$$

在 $\displaystyle x=0$ 处，切线斜率为

$$
\displaystyle y'(0)=(-1)(-2)=2.
$$

曲线经过点 $(0,0)$，故切线方程为

$$
\displaystyle y-0=2(x-0),
$$

即

$$
\displaystyle y=2x.
$$

</details>

---

4. **【1990-3-3 分】** 曲线

$$
\displaystyle
\begin{cases}
 x=\cos^3 t,\\
 y=\sin^3 t
\end{cases}
$$

上对应 $\displaystyle t=\dfrac{\pi}{6}$ 点处的法线方程是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y-\dfrac{1}{8}=\sqrt{3}\left(x-\dfrac{3\sqrt{3}}{8}\right)$，即 $\displaystyle y=\sqrt{3}x-1$。

**解析：** 当 $\displaystyle t=\dfrac{\pi}{6}$ 时，曲线上对应点为

$$
\displaystyle x=\cos^3\dfrac{\pi}{6}=\left(\dfrac{\sqrt{3}}{2}\right)^3=\dfrac{3\sqrt{3}}{8},
$$

$$
\displaystyle y=\sin^3\dfrac{\pi}{6}=\left(\dfrac{1}{2}\right)^3=\dfrac{1}{8}.
$$

参数方程所确定曲线的切线斜率为

$$
\displaystyle \dfrac{dy}{dx}=\dfrac{\dfrac{dy}{dt}}{\dfrac{dx}{dt}}
=\dfrac{3\sin^2t\cos t}{-3\cos^2t\sin t}
=-\tan t.
$$

因此在 $\displaystyle t=\dfrac{\pi}{6}$ 处，

$$
\displaystyle k_{\text{切}}=-\tan\dfrac{\pi}{6}=-\dfrac{1}{\sqrt{3}}.
$$

法线斜率为

$$
\displaystyle k_{\text{法}}=-\dfrac{1}{k_{\text{切}}}=\sqrt{3}.
$$

故法线方程为

$$
\displaystyle y-\dfrac{1}{8}=\sqrt{3}\left(x-\dfrac{3\sqrt{3}}{8}\right),
$$

化简得

$$
\displaystyle y=\sqrt{3}x-1.
$$

</details>

---

5. **【1995-45-3 分】** 设 $\displaystyle f(x)$ 为可导函数，且满足条件 $\displaystyle \lim_{x\to0}\dfrac{f(1)-f(1-x)}{2x}=-1$，则曲线 $\displaystyle y=f(x)$ 在点 $\displaystyle (1,f(1))$ 处的切线斜率为（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle 2$ &emsp;&emsp; B. $\displaystyle -1$ &emsp;&emsp; C. $\displaystyle \dfrac{1}{2}$ &emsp;&emsp; D. $\displaystyle -2$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 由导数定义，

$$
\displaystyle f'(1)=\lim_{x\to0}\dfrac{f(1)-f(1-x)}{x}.
$$

又题设给出

$$
\displaystyle \lim_{x\to0}\dfrac{f(1)-f(1-x)}{2x}=-1,
$$

故

$$
\displaystyle \dfrac{1}{2}f'(1)=-1,
$$

从而

$$
\displaystyle f'(1)=-2.
$$

曲线在点 $\displaystyle (1,f(1))$ 处的切线斜率就是 $\displaystyle f'(1)$，故选 D。

</details>

---

6. **【1995-3-3 分】** 曲线

$$
\displaystyle
\begin{cases}
 x=1+t^2,\\
 y=t^3
\end{cases}
$$

在 $\displaystyle t=2$ 处的切线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y-8=3(x-5)$，即 $\displaystyle 3x-y-7=0$。

**解析：** 当 $\displaystyle t=2$ 时，曲线上对应点为

$$
\displaystyle x=1+2^2=5,\qquad y=2^3=8.
$$

又

$$
\displaystyle \dfrac{dx}{dt}=2t,\qquad \dfrac{dy}{dt}=3t^2,
$$

所以

$$
\displaystyle \dfrac{dy}{dx}=\dfrac{\dfrac{dy}{dt}}{\dfrac{dx}{dt}}
=\dfrac{3t^2}{2t}=\dfrac{3t}{2}.
$$

当 $\displaystyle t=2$ 时，切线斜率为

$$
\displaystyle k=\dfrac{3\times2}{2}=3.
$$

因此切线方程为

$$
\displaystyle y-8=3(x-5),
$$

即

$$
\displaystyle 3x-y-7=0.
$$

</details>

---

7. **【1996-4-3 分】** 设 $\displaystyle (x_0,y_0)$ 是抛物线 $\displaystyle y=ax^2+bx+c$ 上的一点，若在该点的切线过原点，则系数 $\displaystyle a,b,c$ 应满足的关系是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle ax_0^2=c$。

**解析：** 由于 $\displaystyle (x_0,y_0)$ 在抛物线上，故

$$
\displaystyle y_0=ax_0^2+bx_0+c.
$$

抛物线的导数为

$$
\displaystyle y'=2ax+b,
$$

所以在 $\displaystyle (x_0,y_0)$ 处的切线斜率为

$$
\displaystyle 2ax_0+b.
$$

切线方程为

$$
\displaystyle y-y_0=(2ax_0+b)(x-x_0).
$$

该切线经过原点 $(0,0)$，代入得

$$
\displaystyle -y_0=-(2ax_0+b)x_0,
$$

即

$$
\displaystyle y_0=(2ax_0+b)x_0.
$$

与 $\displaystyle y_0=ax_0^2+bx_0+c$ 比较，得

$$
\displaystyle ax_0^2+bx_0+c=2ax_0^2+bx_0,
$$

从而

$$
\displaystyle ax_0^2=c.
$$

</details>

---

8. **【1997-1-3 分】** 对数螺线 $\displaystyle \rho=e^\theta$ 在点 $\displaystyle (\rho,\theta)=\left(e^{\frac{\pi}{2}},\dfrac{\pi}{2}\right)$ 处切线的直角坐标方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle x+y=e^{\frac{\pi}{2}}$。

**解析：** 由极坐标与直角坐标的关系，

$$
\displaystyle x=\rho\cos\theta=e^\theta\cos\theta,
$$

$$
\displaystyle y=\rho\sin\theta=e^\theta\sin\theta.
$$

当 $\displaystyle \theta=\dfrac{\pi}{2}$ 时，曲线上对应点为

$$
\displaystyle x=0,\qquad y=e^{\frac{\pi}{2}}.
$$

分别对 $\displaystyle \theta$ 求导：

$$
\displaystyle \dfrac{dx}{d\theta}=e^\theta(\cos\theta-\sin\theta),
$$

$$
\displaystyle \dfrac{dy}{d\theta}=e^\theta(\sin\theta+\cos\theta).
$$

故切线斜率为

$$
\displaystyle \dfrac{dy}{dx}
=\dfrac{\sin\theta+\cos\theta}{\cos\theta-\sin\theta}.
$$

在 $\displaystyle \theta=\dfrac{\pi}{2}$ 处，

$$
\displaystyle k=\dfrac{1+0}{0-1}=-1.
$$

因此切线方程为

$$
\displaystyle y-e^{\frac{\pi}{2}}=-x,
$$

即

$$
\displaystyle x+y=e^{\frac{\pi}{2}}.
$$

</details>

---

9. **【1998-34-3 分】** 设曲线 $\displaystyle f(x)=x^n$ 在点 $(1,1)$ 处的切线与 $x$ 轴的交点为 $\displaystyle (\xi_n,0)$，则 $\displaystyle \lim_{n\to\infty}f(\xi_n)=$____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{1}{e}$。

**解析：** 由

$$
\displaystyle f'(x)=nx^{n-1},
$$

得曲线在点 $(1,1)$ 处的切线斜率为

$$
\displaystyle f'(1)=n.
$$

切线方程为

$$
\displaystyle y-1=n(x-1).
$$

令 $\displaystyle y=0$，得切线与 $x$ 轴交点的横坐标

$$
\displaystyle -1=n(\xi_n-1),
$$

所以

$$
\displaystyle \xi_n=1-\dfrac{1}{n}.
$$

于是

$$
\displaystyle f(\xi_n)=\xi_n^n=\left(1-\dfrac{1}{n}\right)^n.
$$

利用重要极限，

$$
\displaystyle \lim_{n\to\infty}\left(1-\dfrac{1}{n}\right)^n=e^{-1}.
$$

故

$$
\displaystyle \lim_{n\to\infty}f(\xi_n)=\dfrac{1}{e}.
$$

</details>

---

10. **【1999-2-3 分】** 曲线

$$
\displaystyle
\begin{cases}
 x=e^t\sin 2t,\\
 y=e^t\cos t
\end{cases}
$$

在点 $(0,1)$ 处的法线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y-1=-2x$，即 $\displaystyle 2x+y-1=0$。

**解析：** 当 $\displaystyle t=0$ 时，

$$
\displaystyle x=e^0\sin0=0,\qquad y=e^0\cos0=1,
$$

故点 $(0,1)$ 对应参数 $\displaystyle t=0$。

分别求导：

$$
\displaystyle \dfrac{dx}{dt}=e^t\sin2t+2e^t\cos2t
=e^t(\sin2t+2\cos2t),
$$

$$
\displaystyle \dfrac{dy}{dt}=e^t\cos t-e^t\sin t
=e^t(\cos t-\sin t).
$$

因此切线斜率为

$$
\displaystyle \dfrac{dy}{dx}
=\dfrac{\cos t-\sin t}{\sin2t+2\cos2t}.
$$

在 $\displaystyle t=0$ 处，

$$
\displaystyle k_{\text{切}}=\dfrac{1}{2}.
$$

所以法线斜率为

$$
\displaystyle k_{\text{法}}=-\dfrac{1}{k_{\text{切}}}=-2.
$$

法线经过点 $(0,1)$，故其方程为

$$
\displaystyle y-1=-2x,
$$

即

$$
\displaystyle 2x+y-1=0.
$$

</details>

---

11. **【2001-2-3 分】** 设函数 $\displaystyle y=f(x)$ 由方程

$$
\displaystyle e^{2x+y}-\cos(xy)=e-1
$$

所确定，则曲线 $\displaystyle y=f(x)$ 在点 $(0,1)$ 处的法线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y-1=\dfrac{1}{2}x$，即 $\displaystyle x-2y+2=0$。

**解析：** 对方程两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle e^{2x+y}(2+y')+\sin(xy)(y+xy')=0.
$$

在点 $(0,1)$ 处，$\displaystyle e^{2x+y}=e$，且 $\displaystyle \sin(xy)=0$，所以

$$
\displaystyle e(2+y')=0,
$$

从而切线斜率为

$$
\displaystyle y'(0)=-2.
$$

因此法线斜率为

$$
\displaystyle k_{\text{法}}=-\dfrac{1}{y'(0)}=\dfrac{1}{2}.
$$

法线经过点 $(0,1)$，故法线方程为

$$
\displaystyle y-1=\dfrac{1}{2}x,
$$

即

$$
\displaystyle x-2y+2=0.
$$

</details>

---

12. **【2003-2-4 分】** 设函数 $\displaystyle y=f(x)$ 由方程

$$
\displaystyle xy+2\ln x=y^4
$$

所确定，则曲线 $\displaystyle y=f(x)$ 在点 $(1,1)$ 处的切线方程是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=x$。

**解析：** 对方程两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle y+xy'+\dfrac{2}{x}=4y^3y'.
$$

代入点 $(1,1)$，有

$$
\displaystyle 1+y'+2=4y',
$$

所以

$$
\displaystyle y'=1.
$$

因此曲线在点 $(1,1)$ 处的切线方程为

$$
\displaystyle y-1=x-1,
$$

即

$$
\displaystyle y=x.
$$

</details>

---

13. **【2004-1-4 分】** 曲线 $\displaystyle y=\ln x$ 上与直线 $\displaystyle x+y=1$ 垂直的切线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=x-1$。

**解析：** 直线 $\displaystyle x+y=1$ 的斜率为 $\displaystyle -1$，所以与其垂直的直线斜率为 $\displaystyle 1$。

曲线 $\displaystyle y=\ln x$ 的导数为

$$
\displaystyle y'=\dfrac{1}{x}.
$$

令切线斜率等于 $\displaystyle 1$，得

$$
\displaystyle \dfrac{1}{x}=1,
$$

所以 $\displaystyle x=1$，对应点为

$$
\displaystyle (1,\ln1)=(1,0).
$$

因此所求切线方程为

$$
\displaystyle y-0=1(x-1),
$$

即

$$
\displaystyle y=x-1.
$$

</details>

---

14. **【2005-2-4 分】** 设函数由参数方程

$$
\displaystyle
\begin{cases}
 x=t^2+2t,\\
 y=\ln(1+t)
\end{cases}
$$

确定，则曲线 $\displaystyle y=y(x)$ 在 $\displaystyle x=3$ 处的法线与 $\displaystyle x$ 轴交点的横坐标是（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle \dfrac{1}{8}\ln2+3$ &emsp;&emsp; B. $\displaystyle -\dfrac{1}{8}\ln2+3$ &emsp;&emsp; C. $\displaystyle -8\ln2+3$ &emsp;&emsp; D. $\displaystyle 8\ln2+3$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A。

**解析：** 由 $\displaystyle x=3$，得

$$
\displaystyle t^2+2t=3,
$$

即

$$
\displaystyle (t-1)(t+3)=0.
$$

由于 $\displaystyle y=\ln(1+t)$ 要求 $\displaystyle t>-1$，故只能取 $\displaystyle t=1$。此时曲线上的点为

$$
\displaystyle (x,y)=(3,\ln2).
$$

又

$$
\displaystyle \dfrac{dx}{dt}=2t+2,
\qquad
\dfrac{dy}{dt}=\dfrac{1}{1+t}.
$$

所以切线斜率为

$$
\displaystyle \dfrac{dy}{dx}
=\dfrac{\dfrac{dy}{dt}}{\dfrac{dx}{dt}}
=\dfrac{1}{2(1+t)^2}.
$$

当 $\displaystyle t=1$ 时，

$$
\displaystyle k_{\text{切}}=\dfrac{1}{8},
$$

故法线斜率为

$$
\displaystyle k_{\text{法}}=-8.
$$

法线方程为

$$
\displaystyle y-\ln2=-8(x-3).
$$

令 $\displaystyle y=0$，得

$$
\displaystyle -\ln2=-8(x-3),
$$

所以法线与 $\displaystyle x$ 轴交点的横坐标为

$$
\displaystyle x=3+\dfrac{1}{8}\ln2.
$$

故选 A。B 的符号错误，C、D 则把斜率 $\displaystyle 8$ 与其倒数混淆。

</details>

---

15. **【2007-2-4 分】** 曲线

$$
\displaystyle
\begin{cases}
 x=\cos t+\cos^2t,\\
 y=1+\sin t
\end{cases}
$$

上对应于 $\displaystyle t=\dfrac{\pi}{4}$ 点处的法线斜率为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 1+\sqrt2$。

**解析：** 分别对参数 $\displaystyle t$ 求导：

$$
\displaystyle \dfrac{dx}{dt}
=-\sin t-2\cos t\sin t
=-\sin t(1+2\cos t),
$$

$$
\displaystyle \dfrac{dy}{dt}=\cos t.
$$

因此切线斜率为

$$
\displaystyle k_{\text{切}}
=\dfrac{dy}{dx}
=-\dfrac{\cos t}{\sin t(1+2\cos t)}.
$$

在 $\displaystyle t=\dfrac{\pi}{4}$ 处，$\displaystyle \sin t=\cos t=\dfrac{\sqrt2}{2}$，所以

$$
\displaystyle k_{\text{切}}
=-\dfrac{1}{1+\sqrt2}.
$$

法线斜率为切线斜率的负倒数，故

$$
\displaystyle k_{\text{法}}
=-\dfrac{1}{k_{\text{切}}}
=1+\sqrt2.
$$

</details>

---

16. **【2008-4-4 分】** 已知

$$
\displaystyle \lim_{x\to0}\dfrac{f(x)}{x}=2,
$$

则 $\displaystyle y=f(x)$ 上对应 $\displaystyle x=0$ 处的切线方程____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=2x$。

**解析：** 由

$$
\displaystyle \lim_{x\to0}\dfrac{f(x)}{x}=2
$$

可知 $\displaystyle \lim_{x\to0}f(x)=0$。题目既指出曲线在 $\displaystyle x=0$ 处有对应点并要求其切线，按题意该点为 $(0,0)$，即 $\displaystyle f(0)=0$。

于是

$$
\displaystyle f'(0)
=\lim_{x\to0}\dfrac{f(x)-f(0)}{x-0}
=\lim_{x\to0}\dfrac{f(x)}{x}
=2.
$$

因此曲线在原点处的切线斜率为 $\displaystyle 2$，切线方程为

$$
\displaystyle y=2x.
$$

</details>

---

17. **【2008-12-4 分】** 曲线

$$
\displaystyle \sin(xy)+\ln(y-x)=x
$$

在点 $(0,1)$ 处的切线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=x+1$。

**解析：** 对方程两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle \cos(xy)(y+xy')+\dfrac{y'-1}{y-x}=1.
$$

代入点 $(0,1)$，有

$$
\displaystyle 1+(y'-1)=1,
$$

所以

$$
\displaystyle y'=1.
$$

因此曲线在点 $(0,1)$ 处的切线方程为

$$
\displaystyle y-1=x,
$$

即

$$
\displaystyle y=x+1.
$$

</details>

---

18. **【2009-2-4 分】** 曲线

$$
\displaystyle
\begin{cases}
 x=\displaystyle\int_0^{1-t}e^{-u^2}\,du,\\
 y=t^2\ln(2-t^2)
\end{cases}
$$

在 $(0,0)$ 处的切线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=2x$。

**解析：** 由

$$
\displaystyle x=\int_0^{1-t}e^{-u^2}\,du=0
$$

以及被积函数恒为正，得 $\displaystyle 1-t=0$，即 $\displaystyle t=1$。此时

$$
\displaystyle y=1^2\ln(2-1)=0,
$$

故点 $(0,0)$ 对应参数 $\displaystyle t=1$。

由变上限积分求导公式，

$$
\displaystyle \dfrac{dx}{dt}
=-e^{-(1-t)^2}.
$$

同时，

$$
\displaystyle \dfrac{dy}{dt}
=2t\ln(2-t^2)-\dfrac{2t^3}{2-t^2}.
$$

在 $\displaystyle t=1$ 处，

$$
\displaystyle \left.\dfrac{dx}{dt}\right|_{t=1}=-1,
\qquad
\left.\dfrac{dy}{dt}\right|_{t=1}=-2.
$$

所以切线斜率为

$$
\displaystyle \left.\dfrac{dy}{dx}\right|_{t=1}
=\dfrac{-2}{-1}=2.
$$

切线经过原点，故切线方程为

$$
\displaystyle y=2x.
$$

</details>

---

19. **【2011-3-4 分】** 曲线

$$
\displaystyle \tan\left(x+y+\dfrac{\pi}{4}\right)=e^y
$$

在点 $(0,0)$ 处的切线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=-2x$。

**解析：** 对方程两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle \sec^2\left(x+y+\dfrac{\pi}{4}\right)(1+y')=e^y y'.
$$

在点 $(0,0)$ 处，

$$
\displaystyle \sec^2\dfrac{\pi}{4}=2,
\qquad e^0=1,
$$

故

$$
\displaystyle 2(1+y')=y'.
$$

解得

$$
\displaystyle y'=-2.
$$

因此曲线在原点处的切线方程为

$$
\displaystyle y=-2x.
$$

</details>

---

20. **【2013-2-4 分】** 曲线

$$
\displaystyle
\begin{cases}
 x=\arctan t,\\
 y=\ln\sqrt{1+t^2}
\end{cases}
$$

上对应于 $\displaystyle t=1$ 的点处的法线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y-\dfrac{1}{2}\ln2=-\left(x-\dfrac{\pi}{4}\right)$。

**解析：** 当 $\displaystyle t=1$ 时，曲线上的点为

$$
\displaystyle x=\arctan1=\dfrac{\pi}{4},
\qquad
 y=\ln\sqrt2=\dfrac{1}{2}\ln2.
$$

分别求导：

$$
\displaystyle \dfrac{dx}{dt}=\dfrac{1}{1+t^2},
$$

$$
\displaystyle \dfrac{dy}{dt}
=\dfrac{d}{dt}\left[\dfrac{1}{2}\ln(1+t^2)\right]
=\dfrac{t}{1+t^2}.
$$

因此切线斜率为

$$
\displaystyle k_{\text{切}}
=\dfrac{dy}{dx}
=\dfrac{t}{1+t^2}\div\dfrac{1}{1+t^2}
=t.
$$

在 $\displaystyle t=1$ 处，$\displaystyle k_{\text{切}}=1$，所以法线斜率为 $\displaystyle -1$。

故法线方程为

$$
\displaystyle y-\dfrac{1}{2}\ln2
=-\left(x-\dfrac{\pi}{4}\right).
$$

</details>

---

21. **【2014-2-4 分】** 曲线 $\displaystyle L$ 的极坐标方程是 $\displaystyle r=\theta$，则 $\displaystyle L$ 在点 $\displaystyle (r,\theta)=\left(\dfrac{\pi}{2},\dfrac{\pi}{2}\right)$ 的切线的直角坐标方程是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2x+\pi y-\dfrac{\pi^2}{2}=0$。

**解析：** 由极坐标与直角坐标的关系，曲线可写成参数方程

$$
\displaystyle
\begin{cases}
 x=\theta\cos\theta,\\
 y=\theta\sin\theta.
\end{cases}
$$

因此

$$
\displaystyle \dfrac{dx}{d\theta}=\cos\theta-\theta\sin\theta,
\qquad
\displaystyle \dfrac{dy}{d\theta}=\sin\theta+\theta\cos\theta.
$$

在 $\displaystyle \theta=\dfrac{\pi}{2}$ 时，

$$
\displaystyle x=0,
\qquad
 y=\dfrac{\pi}{2},
$$

且

$$
\displaystyle \left.\dfrac{dx}{d\theta}\right|_{\theta=\frac{\pi}{2}}
=-\dfrac{\pi}{2},
\qquad
\left.\dfrac{dy}{d\theta}\right|_{\theta=\frac{\pi}{2}}=1.
$$

故切线斜率为

$$
\displaystyle k=\left.\dfrac{dy}{dx}\right|_{\theta=\frac{\pi}{2}}
=\dfrac{1}{-\frac{\pi}{2}}
=-\dfrac{2}{\pi}.
$$

于是切线方程为

$$
\displaystyle y-\dfrac{\pi}{2}=-\dfrac{2}{\pi}x,
$$

即

$$
\displaystyle 2x+\pi y-\dfrac{\pi^2}{2}=0.
$$

</details>

---

22. **【2018-23-4 分】** 曲线 $\displaystyle y=x^2+2\ln x$ 在其拐点处的切线方程是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=4x-3$。

**解析：** 函数的定义域为 $\displaystyle x>0$。求导得

$$
\displaystyle y'=2x+\dfrac{2}{x},
$$

$$
\displaystyle y''=2-\dfrac{2}{x^2}
=\dfrac{2(x^2-1)}{x^2}.
$$

令 $\displaystyle y''=0$，得 $\displaystyle x=1$。当 $\displaystyle 0<x<1$ 时，$\displaystyle y''<0$；当 $\displaystyle x>1$ 时，$\displaystyle y''>0$，故曲线在 $\displaystyle x=1$ 处凹凸性发生改变，拐点为

$$
\displaystyle (1,1).
$$

在 $\displaystyle x=1$ 处的切线斜率为

$$
\displaystyle y'(1)=2+2=4.
$$

因此切线方程为

$$
\displaystyle y-1=4(x-1),
$$

即

$$
\displaystyle y=4x-3.
$$

</details>

---

23. **【2019-2-4 分】** 曲线

$$
\displaystyle
\begin{cases}
 x=t-\sin t,\\
 y=1-\cos t
\end{cases}
$$

在 $\displaystyle t=\dfrac{3}{2}\pi$ 对应点处的切线在 $\displaystyle y$ 轴上的截距为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2+\dfrac{3\pi}{2}$。

**解析：** 当 $\displaystyle t=\dfrac{3\pi}{2}$ 时，

$$
\displaystyle x=\dfrac{3\pi}{2}-\sin\dfrac{3\pi}{2}
=\dfrac{3\pi}{2}+1,
$$

$$
\displaystyle y=1-\cos\dfrac{3\pi}{2}=1.
$$

又

$$
\displaystyle \dfrac{dx}{dt}=1-\cos t,
\qquad
\dfrac{dy}{dt}=\sin t.
$$

在 $\displaystyle t=\dfrac{3\pi}{2}$ 处，

$$
\displaystyle \dfrac{dx}{dt}=1,
\qquad
\dfrac{dy}{dt}=-1,
$$

故切线斜率为

$$
\displaystyle \dfrac{dy}{dx}=-1.
$$

切线方程为

$$
\displaystyle y-1=-\left(x-\dfrac{3\pi}{2}-1\right).
$$

令 $\displaystyle x=0$，得切线与 $\displaystyle y$ 轴交点的纵坐标为

$$
\displaystyle y=1+\dfrac{3\pi}{2}+1
=2+\dfrac{3\pi}{2}.
$$

所以切线在 $\displaystyle y$ 轴上的截距为 $\displaystyle 2+\dfrac{3\pi}{2}$。

</details>

---

24. **【2020-3-4 分】** 曲线 $\displaystyle x+y+e^{2xy}=0$ 在点 $\displaystyle (0,-1)$ 处的切线方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=x-1$。

**解析：** 对方程两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle 1+y'+e^{2xy}\cdot2(y+xy')=0.
$$

将 $\displaystyle x=0,y=-1$ 代入，得

$$
\displaystyle 1+y'+2(-1)=0,
$$

即

$$
\displaystyle y'=1.
$$

因此曲线在点 $\displaystyle (0,-1)$ 处的切线方程为

$$
\displaystyle y+1=x,
$$

即

$$
\displaystyle y=x-1.
$$

</details>

---

25. **【2023-2-5 分】** 曲线 $\displaystyle 3x^3=y^5+2y^3$ 在 $\displaystyle x=1$ 对应点处的法线斜率为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle -\dfrac{11}{9}$。

**解析：** 当 $\displaystyle x=1$ 时，方程化为

$$
\displaystyle y^5+2y^3=3.
$$

函数 $\displaystyle h(y)=y^5+2y^3$ 严格单调增加，且 $\displaystyle h(1)=3$，故对应点的纵坐标为 $\displaystyle y=1$。

对原方程两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle 9x^2=(5y^4+6y^2)y'.
$$

在点 $\displaystyle (1,1)$ 处，切线斜率为

$$
\displaystyle y'=\dfrac{9}{5+6}=\dfrac{9}{11}.
$$

因此法线斜率为切线斜率的负倒数，即

$$
\displaystyle k_{\text{法}}=-\dfrac{11}{9}.
$$

</details>

### （二）相切的充要条件

1. **【1991-45-3 分】** 设曲线 $\displaystyle f(x)=x^3+ax$ 与 $\displaystyle g(x)=bx^2+c$ 都通过点 $\displaystyle (-1,0)$，且在点 $\displaystyle (-1,0)$ 有公共切线，则（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle a=0,b=-2$ &emsp;&emsp; B. $\displaystyle a=1,b=-3$ &emsp;&emsp; C. $\displaystyle a=-3,b=1$ &emsp;&emsp; D. $\displaystyle a=-1,b=-1$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 两条曲线都通过点 $\displaystyle (-1,0)$，因此

$$
\displaystyle f(-1)=-1-a=0,
$$

从而

$$
\displaystyle a=-1.
$$

又

$$
\displaystyle g(-1)=b+c=0,
$$

即 $\displaystyle c=-b$。

两曲线在该点有公共切线，故它们在 $\displaystyle x=-1$ 处的导数相等。由

$$
\displaystyle f'(x)=3x^2+a,
\qquad
 g'(x)=2bx,
$$

得

$$
\displaystyle f'(-1)=3+a=2,
\qquad
 g'(-1)=-2b.
$$

于是

$$
\displaystyle -2b=2,
$$

解得

$$
\displaystyle b=-1.
$$

因此 $\displaystyle a=-1,b=-1$，故选 D。其余选项不能同时满足“过点”和“切线斜率相等”两个条件。

</details>

---

2. **【1991-3-3 分】** 若曲线 $\displaystyle y=x^2+ax+b$ 和 $\displaystyle 2y=-1+xy^3$ 在 $\displaystyle (1,-1)$ 点相切，其中 $\displaystyle a,b$ 是常数，则（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle a=0,b=-2$ &emsp;&emsp; B. $\displaystyle a=1,b=-3$ &emsp;&emsp; C. $\displaystyle a=-3,b=1$ &emsp;&emsp; D. $\displaystyle a=-1,b=-1$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 第一条曲线通过点 $\displaystyle (1,-1)$，故

$$
\displaystyle -1=1+a+b,
$$

即

$$
\displaystyle a+b=-2.
$$

第一条曲线的导数为

$$
\displaystyle y'=2x+a,
$$

所以在 $\displaystyle x=1$ 处的切线斜率为

$$
\displaystyle k_1=2+a.
$$

对第二条曲线

$$
\displaystyle 2y=-1+xy^3
$$

两边关于 $\displaystyle x$ 求导，得

$$
\displaystyle 2y'=y^3+3xy^2y'.
$$

整理得

$$
\displaystyle (2-3xy^2)y'=y^3.
$$

在点 $\displaystyle (1,-1)$ 处，

$$
\displaystyle (2-3)y'=(-1)^3,
$$

因此

$$
\displaystyle y'=1.
$$

两曲线相切，故斜率相等，即

$$
\displaystyle 2+a=1,
$$

从而

$$
\displaystyle a=-1.
$$

再由 $\displaystyle a+b=-2$，得

$$
\displaystyle b=-1.
$$

故选 D。

</details>

---

3. **【2003-3-4 分】** 已知曲线 $\displaystyle y=x^3-3a^2x+b$ 与 $\displaystyle x$ 轴相切，则 $\displaystyle b^2=$____（用 $\displaystyle a$ 表示）。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 4a^6$。

**解析：** 曲线与 $\displaystyle x$ 轴相切，说明存在切点 $\displaystyle (x_0,0)$，使得

$$
\displaystyle
\begin{cases}
 x_0^3-3a^2x_0+b=0,\\
 3x_0^2-3a^2=0.
\end{cases}
$$

由第二个方程得

$$
\displaystyle x_0^2=a^2,
$$

即

$$
\displaystyle x_0=a
\quad\text{或}\quad
x_0=-a.
$$

当 $\displaystyle x_0=a$ 时，

$$
\displaystyle a^3-3a^3+b=0,
$$

所以

$$
\displaystyle b=2a^3.
$$

当 $\displaystyle x_0=-a$ 时，

$$
\displaystyle -a^3+3a^3+b=0,
$$

所以

$$
\displaystyle b=-2a^3.
$$

两种情形均有

$$
\displaystyle b^2=4a^6.
$$

</details>

---

4. **【2010-2-4 分】** 曲线 $\displaystyle y=x^2$ 与曲线 $\displaystyle y=a\ln x\ (a\ne0)$ 相切，则 $\displaystyle a=$（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle 4e$ &emsp;&emsp; B. $\displaystyle 3e$ &emsp;&emsp; C. $\displaystyle 2e$ &emsp;&emsp; D. $\displaystyle e$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C。

**解析：** 设两曲线在 $\displaystyle x=x_0$ 处相切。由于对数函数要求 $\displaystyle x_0>0$，相切条件为函数值相等且导数相等，即

$$
\displaystyle
\begin{cases}
 x_0^2=a\ln x_0,\\[1mm]
 2x_0=\dfrac{a}{x_0}.
\end{cases}
$$

由第二个方程得

$$
\displaystyle a=2x_0^2.
$$

代入第一个方程，得

$$
\displaystyle x_0^2=2x_0^2\ln x_0.
$$

因 $\displaystyle x_0>0$，且相切点不可能为 $\displaystyle x_0=0$，故

$$
\displaystyle 1=2\ln x_0.
$$

于是

$$
\displaystyle \ln x_0=\dfrac{1}{2},
\qquad
x_0=\sqrt e.
$$

所以

$$
\displaystyle a=2x_0^2=2e.
$$

故选 C。

</details>

---

5. **【2013-3-4 分】** 设曲线 $\displaystyle y=f(x)$ 与 $\displaystyle y=x^2-x$ 在点 $\displaystyle (1,0)$ 处有公共切线，则

$$
\displaystyle \lim_{n\to\infty}n f\left(\dfrac{n}{n+2}\right)=
$$

____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle -2$。

**解析：** 两曲线在点 $\displaystyle (1,0)$ 处有公共切线，因此

$$
\displaystyle f(1)=0,
$$

且

$$
\displaystyle f'(1)=\left.(2x-1)\right|_{x=1}=1.
$$

令

$$
\displaystyle x_n=\dfrac{n}{n+2},
$$

则 $\displaystyle x_n\to1$，并且

$$
\displaystyle x_n-1=-\dfrac{2}{n+2}.
$$

由导数定义，

$$
\displaystyle
\lim_{x\to1}\dfrac{f(x)-f(1)}{x-1}=f'(1)=1.
$$

于是

$$
\displaystyle
\begin{aligned}
\lim_{n\to\infty}n f\left(\dfrac{n}{n+2}\right)
&=\lim_{n\to\infty}
 n\left(x_n-1\right)
 \dfrac{f(x_n)-f(1)}{x_n-1}\\
&=\lim_{n\to\infty}
 \left(-\dfrac{2n}{n+2}\right)\cdot1\\
&=-2.
\end{aligned}
$$

</details>

### （三）导数的物理应用（数一、数二）

1. **【1991-3-3 分】** 质点以速度 $\displaystyle t\sin(t^2)$ 米/秒作直线运动，则从时刻 $\displaystyle t_1=\sqrt{\dfrac{\pi}{2}}$ 秒到 $\displaystyle t_2=\sqrt{\pi}$ 秒内质点所经过的路程等于____米。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{1}{2}$。

**解析：** 质点的速度为

$$
\displaystyle v(t)=t\sin(t^2).
$$

当 $\displaystyle t\in\left[\sqrt{\dfrac{\pi}{2}},\sqrt\pi\right]$ 时，

$$
\displaystyle t^2\in\left[\dfrac{\pi}{2},\pi\right],
$$

故 $\displaystyle \sin(t^2)\ge0$，从而 $\displaystyle v(t)\ge0$。因此该时间段内的路程等于位移：

$$
\displaystyle
\begin{aligned}
s
&=\int_{\sqrt{\frac{\pi}{2}}}^{\sqrt\pi}t\sin(t^2)\,dt\\
&=-\dfrac{1}{2}\cos(t^2)\Bigg|_{\sqrt{\frac{\pi}{2}}}^{\sqrt\pi}\\
&=-\dfrac{1}{2}\left(\cos\pi-\cos\dfrac{\pi}{2}\right)\\
&=\dfrac{1}{2}.
\end{aligned}
$$

相关知识点：求路程时应积分速度的绝对值；只有在速度不变号时，路程才等于位移的绝对值。本题中速度在给定区间内非负，故可直接积分。

</details>

---

2. **【2010-2-4 分】** 已知一个长方形的长 $\displaystyle l$ 以 $\displaystyle 2\mathrm{cm/s}$ 的速率增加，宽 $\displaystyle w$ 以 $\displaystyle 3\mathrm{cm/s}$ 的速率增加。则当 $\displaystyle l=12\mathrm{cm},w=5\mathrm{cm}$ 时，它的对角线增加的速率为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 3\mathrm{cm/s}$。

**解析：** 设长方形对角线长度为 $\displaystyle d$，则

$$
\displaystyle d^2=l^2+w^2.
$$

对时间 $\displaystyle t$ 求导，得

$$
\displaystyle 2d\dfrac{dd}{dt}=2l\dfrac{dl}{dt}+2w\dfrac{dw}{dt},
$$

所以

$$
\displaystyle \dfrac{dd}{dt}
=\dfrac{l\frac{dl}{dt}+w\frac{dw}{dt}}{d}.
$$

当 $\displaystyle l=12,w=5$ 时，

$$
\displaystyle d=\sqrt{12^2+5^2}=13.
$$

又 $\displaystyle \dfrac{dl}{dt}=2$，$\displaystyle \dfrac{dw}{dt}=3$，故

$$
\displaystyle
\dfrac{dd}{dt}
=\dfrac{12\times2+5\times3}{13}
=\dfrac{39}{13}
=3\mathrm{cm/s}.
$$

相关知识点：相关变化率问题应先建立各变量之间的几何关系，再对时间求导，最后代入给定时刻的数据。

</details>

---

3. **【2016-2-4 分】** 已知动点 $\displaystyle P$ 在曲线 $\displaystyle y=x^3$ 上运动，记坐标原点与点 $\displaystyle P$ 间的距离为 $\displaystyle l$。若点 $\displaystyle P$ 的横坐标对时间的变化率为常数 $\displaystyle v_0$，则当点 $\displaystyle P$ 运动到点 $\displaystyle (1,1)$ 时，$\displaystyle l$ 对时间的变化率是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2\sqrt2\,v_0$。

**解析：** 点 $\displaystyle P$ 的坐标为 $\displaystyle (x,x^3)$，故它到原点的距离满足

$$
\displaystyle l^2=x^2+x^6.
$$

对时间 $\displaystyle t$ 求导，得

$$
\displaystyle 2l\dfrac{dl}{dt}
=\left(2x+6x^5\right)\dfrac{dx}{dt}.
$$

于是

$$
\displaystyle
\dfrac{dl}{dt}
=\dfrac{x+3x^5}{l}\dfrac{dx}{dt}.
$$

在点 $\displaystyle (1,1)$ 处，$\displaystyle x=1$，且

$$
\displaystyle l=\sqrt{1^2+1^2}=\sqrt2,
\qquad
\dfrac{dx}{dt}=v_0.
$$

因此

$$
\displaystyle
\dfrac{dl}{dt}
=\dfrac{1+3}{\sqrt2}v_0
=2\sqrt2\,v_0.
$$

相关知识点：距离是坐标的复合函数，应先利用距离公式建立关系，再通过隐函数求导计算变化率。

</details>

---

4. **【2021-2-5 分】** 有一圆柱体底面半径与高随时间变化的速率分别为 $\displaystyle 2\mathrm{cm/s},-3\mathrm{cm/s}$，当底面半径为 $\displaystyle 10\mathrm{cm}$，高为 $\displaystyle 5\mathrm{cm}$ 时，圆柱体的体积与表面积随时间变化的速率分别为（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle 125\pi\mathrm{cm^3/s},40\pi\mathrm{cm^2/s}$ &emsp;&emsp; B. $\displaystyle 125\pi\mathrm{cm^3/s},-40\pi\mathrm{cm^2/s}$</div>
<div className="choice-option-row">C. $\displaystyle -100\pi\mathrm{cm^3/s},40\pi\mathrm{cm^2/s}$ &emsp;&emsp; D. $\displaystyle -100\pi\mathrm{cm^3/s},-40\pi\mathrm{cm^2/s}$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C。

**解析：** 设圆柱体底面半径为 $\displaystyle r$，高为 $\displaystyle h$。已知

$$
\displaystyle \dfrac{dr}{dt}=2,
\qquad
\dfrac{dh}{dt}=-3.
$$

圆柱体体积为

$$
\displaystyle V=\pi r^2h.
$$

对时间求导，得

$$
\displaystyle
\dfrac{dV}{dt}
=\pi\left(2rh\dfrac{dr}{dt}+r^2\dfrac{dh}{dt}\right).
$$

代入 $\displaystyle r=10,h=5$，得

$$
\displaystyle
\dfrac{dV}{dt}
=\pi\left(2\times10\times5\times2+10^2\times(-3)\right)
=-100\pi\mathrm{cm^3/s}.
$$

圆柱体的表面积包括两个底面与侧面，故

$$
\displaystyle S=2\pi r^2+2\pi rh.
$$

对时间求导，得

$$
\displaystyle
\dfrac{dS}{dt}
=4\pi r\dfrac{dr}{dt}
+2\pi\left(h\dfrac{dr}{dt}+r\dfrac{dh}{dt}\right).
$$

代入数据，得

$$
\displaystyle
\begin{aligned}
\dfrac{dS}{dt}
&=4\pi\times10\times2
+2\pi\left(5\times2+10\times(-3)\right)\\
&=80\pi-40\pi\\
&=40\pi\mathrm{cm^2/s}.
\end{aligned}
$$

故选 C。

错误选项分析：A、B 的体积变化率计算错误；D 的表面积变化率符号错误。虽然圆柱的高在减小，但半径增加对表面积的影响更大，所以此时总表面积仍在增加。

</details>

### （四）曲率（数一、数二）

1. **【2009-2-4 分】** 若 $\displaystyle f''(x)$ 不变号，且曲线 $\displaystyle y=f(x)$ 在点 $\displaystyle (1,1)$ 处的曲率圆为 $\displaystyle x^2+y^2=2$，则函数 $\displaystyle f(x)$ 在区间 $\displaystyle (1,2)$ 内（ ）。

<div className="choice-options">
<div className="choice-option-row">A. 有极值点，无零点 &emsp;&emsp; B. 无极值点，有零点 &emsp;&emsp; C. 有极值点，有零点 &emsp;&emsp; D. 无极值点，无零点</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** B。

**解析：** 曲率圆 $\displaystyle x^2+y^2=2$ 的圆心为 $\displaystyle (0,0)$，半径为 $\displaystyle \sqrt2$。在点 $\displaystyle (1,1)$ 处，圆的切线满足

$$
\displaystyle x+y=2,
$$

故曲线 $\displaystyle y=f(x)$ 在该点的切线斜率为

$$
\displaystyle f'(1)=-1.
$$

曲率圆的圆心也就是曲线在该点的曲率中心。对函数曲线 $\displaystyle y=f(x)$，曲率中心的横坐标为

$$
\displaystyle x_c=x-\dfrac{f'(x)\left[1+(f'(x))^2\right]}{f''(x)}.
$$

代入 $\displaystyle x=1$、$\displaystyle x_c=0$、$\displaystyle f'(1)=-1$，得

$$
\displaystyle
0=1-\dfrac{(-1)(1+1)}{f''(1)}
=1+\dfrac{2}{f''(1)},
$$

所以

$$
\displaystyle f''(1)=-2<0.
$$

由于 $\displaystyle f''(x)$ 不变号，故在 $\displaystyle (1,2)$ 内曲线保持凹性，$\displaystyle f'(x)$ 单调不增。于是对 $\displaystyle x>1$，有

$$
\displaystyle f'(x)\le f'(1)=-1<0.
$$

所以 $\displaystyle f(x)$ 在 $\displaystyle (1,2)$ 内严格单调减少，不存在极值点。

又曲线在 $\displaystyle x=1$ 处的切线为 $\displaystyle y=2-x$。由于曲线在该处向下弯曲，且 $\displaystyle f''(1)<0$，在 $\displaystyle x=1$ 的右侧曲线位于切线下方；特别地，结合 $\displaystyle f(1)=1$ 及 $\displaystyle f'(x)\le-1$ 可得

$$
\displaystyle f(2)<f(1)-1=0.
$$

而 $\displaystyle f(1)=1>0$，由连续性知在 $\displaystyle (1,2)$ 内至少有一个零点。故选 B。

错误选项分析：A、C 错在函数在该区间内严格递减，不会出现极值点；D 错在函数从正值下降到负值，必有零点。

</details>

---

2. **【2012-2-4 分】** 曲线 $\displaystyle y=x^2+x\ (x<0)$ 上曲率为 $\displaystyle \dfrac{\sqrt2}{2}$ 的点的坐标是____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle (-1,0)$。

**解析：** 对曲线 $\displaystyle y=x^2+x$，有

$$
\displaystyle y'=2x+1,
\qquad
 y''=2.
$$

函数曲线的曲率公式为

$$
\displaystyle
\kappa=\dfrac{|y''|}{\left[1+(y')^2\right]^{\frac32}}.
$$

由题意，

$$
\displaystyle
\dfrac{2}{\left[1+(2x+1)^2\right]^{\frac32}}
=\dfrac{\sqrt2}{2}.
$$

整理得

$$
\displaystyle
\left[1+(2x+1)^2\right]^{\frac32}=2\sqrt2=2^{\frac32}.
$$

两边取 $\displaystyle \dfrac{2}{3}$ 次方，得

$$
\displaystyle 1+(2x+1)^2=2,
$$

所以

$$
\displaystyle (2x+1)^2=1.
$$

于是

$$
\displaystyle x=0
\quad\text{或}\quad
x=-1.
$$

由于题设限定 $\displaystyle x<0$，故只取 $\displaystyle x=-1$。此时

$$
\displaystyle y=(-1)^2+(-1)=0.
$$

故所求点为 $\displaystyle (-1,0)$。

</details>

---

3. **【2014-2-4 分】** 曲线

$$
\displaystyle
\begin{cases}
x=t^2+7,\\
y=t^2+4t+1
\end{cases}
$$

上对应于 $\displaystyle t=1$ 的点处的曲率半径是（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle \dfrac{\sqrt{10}}{50}$ &emsp;&emsp; B. $\displaystyle \dfrac{\sqrt{10}}{100}$ &emsp;&emsp; C. $\displaystyle 10\sqrt{10}$ &emsp;&emsp; D. $\displaystyle 5\sqrt{10}$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** C。

**解析：** 参数曲线的曲率为

$$
\displaystyle
\kappa
=\dfrac{|x'y''-y'x''|}{\left[(x')^2+(y')^2\right]^{\frac32}}.
$$

由

$$
\displaystyle x=t^2+7,
\qquad
 y=t^2+4t+1,
$$

得

$$
\displaystyle x'=2t,
\quad x''=2,
\quad y'=2t+4,
\quad y''=2.
$$

当 $\displaystyle t=1$ 时，

$$
\displaystyle x'=2,
\quad y'=6,
\quad x''=2,
\quad y''=2.
$$

因此

$$
\displaystyle
\kappa
=\dfrac{|2\times2-6\times2|}{(2^2+6^2)^{\frac32}}
=\dfrac{8}{40^{\frac32}}
=\dfrac{1}{10\sqrt{10}}.
$$

曲率半径为

$$
\displaystyle \rho=\dfrac{1}{\kappa}=10\sqrt{10}.
$$

故选 C。

错误选项分析：A、B 给出的是与曲率同量纲的表达式而非曲率半径；D 少乘了一个系数 $\displaystyle 2$。

</details>

---

4. **【2016-2-4 分】** 设函数 $\displaystyle f_i(x)\ (i=1,2)$ 具有二阶连续导数，且 $\displaystyle f_i''(x_0)<0$。若两条曲线 $\displaystyle y=f_i(x)$ 在点 $\displaystyle (x_0,y_0)$ 处具有公切线 $\displaystyle y=g(x)$，且在该点处曲线 $\displaystyle y=f_1(x)$ 的曲率大于曲线 $\displaystyle y=f_2(x)$ 的曲率，则在 $\displaystyle x_0$ 的某个邻域内，有（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle f_1(x)\le f_2(x)\le g(x)$ &emsp;&emsp; B. $\displaystyle f_2(x)\le f_1(x)\le g(x)$</div>
<div className="choice-option-row">C. $\displaystyle f_1(x)\le g(x)\le f_2(x)$ &emsp;&emsp; D. $\displaystyle f_2(x)\le g(x)\le f_1(x)$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A。

**解析：** 两曲线在 $\displaystyle (x_0,y_0)$ 处具有公切线，因此

$$
\displaystyle f_1(x_0)=f_2(x_0)=g(x_0),
$$

且

$$
\displaystyle f_1'(x_0)=f_2'(x_0)=g'(x_0).
$$

函数曲线的曲率为

$$
\displaystyle
\kappa_i
=\dfrac{|f_i''(x_0)|}{\left[1+(f_i'(x_0))^2\right]^{\frac32}}.
$$

两曲线在该点斜率相同，故分母相同。又 $\displaystyle f_i''(x_0)<0$，且 $\displaystyle \kappa_1>\kappa_2$，所以

$$
\displaystyle |f_1''(x_0)|>|f_2''(x_0)|,
$$

即

$$
\displaystyle f_1''(x_0)<f_2''(x_0)<0.
$$

由二阶导数的连续性，在 $\displaystyle x_0$ 的某个邻域内仍有

$$
\displaystyle f_1''(x)<f_2''(x)<0.
$$

令 $\displaystyle h(x)=f_1(x)-f_2(x)$，则

$$
\displaystyle h(x_0)=h'(x_0)=0,
\qquad
h''(x)<0.
$$

故 $\displaystyle x_0$ 是 $\displaystyle h(x)$ 的局部极大值点，从而

$$
\displaystyle f_1(x)-f_2(x)=h(x)\le0,
$$

即

$$
\displaystyle f_1(x)\le f_2(x).
$$

另一方面，两曲线在该邻域内均向下凹，因而曲线位于其切线下方，即

$$
\displaystyle f_2(x)\le g(x).
$$

综上，

$$
\displaystyle f_1(x)\le f_2(x)\le g(x).
$$

故选 A。

错误选项分析：B 颠倒了两条曲线因曲率大小产生的上下位置；C、D 均错误地把向下凹曲线置于切线上方。

</details>

---

5. **【2018-2-4 分】** 曲线

$$
\displaystyle
\begin{cases}
x=\cos^3t,\\
y=\sin^3t
\end{cases}
$$

在 $\displaystyle t=\dfrac{\pi}{4}$ 对应点处的曲率为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{2}{3}$。

**解析：** 参数曲线的曲率公式为

$$
\displaystyle
\kappa
=\dfrac{|x'y''-y'x''|}{\left[(x')^2+(y')^2\right]^{\frac32}}.
$$

先求导：

$$
\displaystyle x'=-3\cos^2t\sin t,
\qquad
 y'=3\sin^2t\cos t.
$$

再求二阶导数：

$$
\displaystyle
x''=3\cos t\left(2\sin^2t-\cos^2t\right),
$$

$$
\displaystyle
y''=3\sin t\left(2\cos^2t-\sin^2t\right).
$$

当 $\displaystyle t=\dfrac{\pi}{4}$ 时，$\displaystyle \sin t=\cos t=\dfrac{\sqrt2}{2}$，于是

$$
\displaystyle
x'=-\dfrac{3\sqrt2}{4},
\quad
y'=\dfrac{3\sqrt2}{4},
\quad
x''=\dfrac{3\sqrt2}{4},
\quad
y''=\dfrac{3\sqrt2}{4}.
$$

因此

$$
\displaystyle
|x'y''-y'x''|
=\left|-\dfrac{9}{8}-\dfrac{9}{8}\right|
=\dfrac{9}{4},
$$

且

$$
\displaystyle
\left[(x')^2+(y')^2\right]^{\frac32}
=\left(\dfrac{9}{8}+\dfrac{9}{8}\right)^{\frac32}
=\left(\dfrac{9}{4}\right)^{\frac32}
=\dfrac{27}{8}.
$$

故

$$
\displaystyle
\kappa
=\dfrac{\frac94}{\frac{27}{8}}
=\dfrac{2}{3}.
$$

</details>

---

6. **【2019-2-4 分】** 设 $\displaystyle f(x),g(x)$ 的二阶导函数在 $\displaystyle x=a$ 处连续，则

$$
\displaystyle
\lim_{x\to a}\dfrac{f(x)-g(x)}{(x-a)^2}=0
$$

是两条曲线在 $\displaystyle x=a$ 处相切且曲率相等的（ ）。

<div className="choice-options">
<div className="choice-option-row">A. 充分不必要 &emsp;&emsp; B. 充分必要 &emsp;&emsp; C. 必要不充分 &emsp;&emsp; D. 既不充分也不必要</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A。

**解析：** 令

$$
\displaystyle h(x)=f(x)-g(x).
$$

由于 $\displaystyle f,g$ 的二阶导函数在 $\displaystyle x=a$ 处连续，可在 $\displaystyle x=a$ 处作二阶泰勒展开：

$$
\displaystyle
h(x)
=h(a)+h'(a)(x-a)+\dfrac{1}{2}h''(a)(x-a)^2+o\left((x-a)^2\right).
$$

若

$$
\displaystyle
\lim_{x\to a}\dfrac{h(x)}{(x-a)^2}=0,
$$

则必须有

$$
\displaystyle h(a)=0,
\qquad
h'(a)=0,
\qquad
h''(a)=0.
$$

因此

$$
\displaystyle f(a)=g(a),
\qquad
f'(a)=g'(a),
\qquad
f''(a)=g''(a).
$$

前两个等式说明两条曲线在 $\displaystyle x=a$ 处相切；又函数曲线的曲率为

$$
\displaystyle
\kappa=\dfrac{|y''|}{\left[1+(y')^2\right]^{\frac32}},
$$

由 $\displaystyle f'(a)=g'(a)$、$\displaystyle f''(a)=g''(a)$ 可知两曲率相等。因此题设极限条件是充分条件。

但它不是必要条件。例如取

$$
\displaystyle f(x)=(x-a)^2,
\qquad
g(x)=-(x-a)^2.
$$

两曲线在 $\displaystyle x=a$ 处均经过点 $\displaystyle (a,0)$，且

$$
\displaystyle f'(a)=g'(a)=0.
$$

所以它们在该点相切。又

$$
\displaystyle |f''(a)|=|g''(a)|=2,
$$

故曲率相等。然而

$$
\displaystyle
\lim_{x\to a}\dfrac{f(x)-g(x)}{(x-a)^2}
=\lim_{x\to a}\dfrac{2(x-a)^2}{(x-a)^2}
=2\ne0.
$$

因此该条件充分但不必要，故选 A。

错误选项分析：B 错在曲率只与二阶导数的绝对值有关，曲率相等不必推出二阶导数相等；C、D 则忽略了题设极限能够推出函数值、一阶导数和二阶导数均相等。

</details>

---

7. **【2024-2-5 分】** 曲线 $\displaystyle y^2=x$ 在点 $(0,0)$ 处的曲率圆方程为____。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \left(x-\dfrac{1}{2}\right)^2+y^2=\dfrac{1}{4}$。

**解析：** 将曲线参数化为

$$
\displaystyle
\begin{cases}
x=t^2,\\
y=t.
\end{cases}
$$

则

$$
\displaystyle x'=2t,\qquad y'=1,\qquad x''=2,\qquad y''=0.
$$

参数曲线的曲率公式为

$$
\displaystyle
\kappa=\dfrac{|x'y''-y'x''|}{\left[(x')^2+(y')^2\right]^{\frac32}}.
$$

在 $\displaystyle t=0$ 时，

$$
\displaystyle
\kappa
=\dfrac{|0\cdot0-1\cdot2|}{(0^2+1^2)^{\frac32}}
=2.
$$

所以曲率半径为

$$
\displaystyle \rho=\dfrac{1}{\kappa}=\dfrac{1}{2}.
$$

曲线 $\displaystyle x=y^2$ 在原点处的切线为 $\displaystyle x=0$，法线为 $\displaystyle y=0$。由于曲线开口向右，曲率圆圆心位于原点右侧的法线上，故圆心为

$$
\displaystyle \left(\dfrac{1}{2},0\right).
$$

因此曲率圆方程为

$$
\displaystyle \left(x-\dfrac{1}{2}\right)^2+y^2=\left(\dfrac{1}{2}\right)^2
=\dfrac{1}{4}.
$$

</details>

## 大题

### （一）求某点的切线和法线

1. **【1992-5-7 分】** 给定曲线 $\displaystyle y=\dfrac{1}{x^2}$。

（1）求曲线在横坐标为 $\displaystyle x_0$ 的点处的切线方程；

（2）求曲线的切线被两坐标轴所截线段的最短长度。

<details>
<summary>查看答案与解析</summary>

**答案：** （1）$\displaystyle y-\dfrac{1}{x_0^2}=-\dfrac{2}{x_0^3}(x-x_0)$，其中 $\displaystyle x_0\ne0$；

（2）最短长度为 $\displaystyle \dfrac{3\sqrt3}{2}$。

**解析：** （1）由

$$
\displaystyle y=x^{-2},
$$

得

$$
\displaystyle y'=-2x^{-3}=-\dfrac{2}{x^3}.
$$

曲线上横坐标为 $\displaystyle x_0$ 的点为

$$
\displaystyle \left(x_0,\dfrac{1}{x_0^2}\right),
$$

该点处的切线斜率为

$$
\displaystyle y'(x_0)=-\dfrac{2}{x_0^3}.
$$

故切线方程为

$$
\displaystyle y-\dfrac{1}{x_0^2}=-\dfrac{2}{x_0^3}(x-x_0).
$$

整理得

$$
\displaystyle y=-\dfrac{2}{x_0^3}x+\dfrac{3}{x_0^2}.
$$

（2）令 $\displaystyle y=0$，得切线与 $\displaystyle x$ 轴的交点为

$$
\displaystyle A\left(\dfrac{3x_0}{2},0\right).
$$

令 $\displaystyle x=0$，得切线与 $\displaystyle y$ 轴的交点为

$$
\displaystyle B\left(0,\dfrac{3}{x_0^2}\right).
$$

因此截线段长度为

$$
\displaystyle
AB=\sqrt{\left(\dfrac{3x_0}{2}\right)^2+\left(\dfrac{3}{x_0^2}\right)^2}
=3\sqrt{\dfrac{x_0^2}{4}+\dfrac{1}{x_0^4}}.
$$

令 $\displaystyle u=x_0^2>0$，则

$$
\displaystyle AB^2=9\left(\dfrac{u}{4}+\dfrac{1}{u^2}\right).
$$

设

$$
\displaystyle \varphi(u)=\dfrac{u}{4}+\dfrac{1}{u^2},
$$

则

$$
\displaystyle \varphi'(u)=\dfrac{1}{4}-\dfrac{2}{u^3}.
$$

令 $\displaystyle \varphi'(u)=0$，得

$$
\displaystyle u^3=8,
\qquad u=2.
$$

又

$$
\displaystyle \varphi''(u)=\dfrac{6}{u^4}>0,
$$

故 $\displaystyle u=2$ 时取得最小值。此时

$$
\displaystyle
AB_{\min}^2
=9\left(\dfrac{2}{4}+\dfrac{1}{2^2}\right)
=\dfrac{27}{4},
$$

所以

$$
\displaystyle AB_{\min}=\dfrac{3\sqrt3}{2}.
$$

当 $\displaystyle x_0=\pm\sqrt2$ 时取得该最短长度。

</details>

---

2. **【2000-2-7 分】** 已知 $\displaystyle f(x)$ 是周期为 $5$ 的连续函数，它在 $\displaystyle x=0$ 的某个邻域内满足关系式

$$
\displaystyle f(1+\sin x)-3f(1-\sin x)=8x+\alpha(x),
$$

其中 $\displaystyle \alpha(x)$ 是当 $\displaystyle x\to0$ 时比 $\displaystyle x$ 高阶的无穷小，且 $\displaystyle f(x)$ 在 $\displaystyle x=1$ 处可导，求曲线 $\displaystyle y=f(x)$ 在点 $\displaystyle (6,f(6))$ 处的切线方程。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle y=2(x-6)$。

**解析：** 由 $\displaystyle \alpha(x)=o(x)$，题设关系可写为

$$
\displaystyle f(1+\sin x)-3f(1-\sin x)=8x+o(x).
$$

令 $\displaystyle x\to0$。由 $\displaystyle f(x)$ 在 $\displaystyle x=1$ 处连续，得

$$
\displaystyle f(1)-3f(1)=0,
$$

从而

$$
\displaystyle f(1)=0.
$$

由于 $\displaystyle f(x)$ 在 $\displaystyle x=1$ 处可导，当 $\displaystyle x\to0$ 时，

$$
\displaystyle f(1+\sin x)
=f(1)+f'(1)\sin x+o(\sin x),
$$

$$
\displaystyle f(1-\sin x)
=f(1)-f'(1)\sin x+o(\sin x).
$$

代入题设，并利用 $\displaystyle f(1)=0$，得

$$
\displaystyle 4f'(1)\sin x+o(\sin x)=8x+o(x).
$$

又 $\displaystyle \sin x\sim x$，故比较一次项可得

$$
\displaystyle 4f'(1)=8,
$$

即

$$
\displaystyle f'(1)=2.
$$

因为 $\displaystyle f(x)$ 的周期为 $5$，所以

$$
\displaystyle f(6)=f(1)=0.
$$

并且

$$
\displaystyle
f'(6)
=\lim_{h\to0}\dfrac{f(6+h)-f(6)}{h}
=\lim_{h\to0}\dfrac{f(1+h)-f(1)}{h}
=f'(1)=2.
$$

因此曲线在点 $\displaystyle (6,0)$ 处的切线方程为

$$
\displaystyle y-0=2(x-6),
$$

即

$$
\displaystyle y=2(x-6).
$$

</details>

---

3. **【2002-2-6 分】** 已知曲线的极坐标方程是 $\displaystyle r=1-\cos\theta$，求该曲线上对应于 $\displaystyle \theta=\dfrac{\pi}{6}$ 处的切线与法线的直角坐标方程。

<details>
<summary>查看答案与解析</summary>

**答案：** 切线方程为

$$
\displaystyle y=x+\dfrac{5-3\sqrt3}{4};
$$

法线方程为

$$
\displaystyle x+y=\dfrac{\sqrt3-1}{4}.
$$

**解析：** 极坐标曲线可参数化为

$$
\displaystyle
\begin{cases}
x=(1-\cos\theta)\cos\theta,\\
y=(1-\cos\theta)\sin\theta.
\end{cases}
$$

当 $\displaystyle \theta=\dfrac{\pi}{6}$ 时，

$$
\displaystyle r=1-\dfrac{\sqrt3}{2}=\dfrac{2-\sqrt3}{2}.
$$

故对应点为

$$
\displaystyle
x_0=r\cos\theta
=\dfrac{2-\sqrt3}{2}\cdot\dfrac{\sqrt3}{2}
=\dfrac{2\sqrt3-3}{4},
$$

$$
\displaystyle
y_0=r\sin\theta
=\dfrac{2-\sqrt3}{2}\cdot\dfrac{1}{2}
=\dfrac{2-\sqrt3}{4}.
$$

对参数方程求导：

$$
\displaystyle
\dfrac{dx}{d\theta}
=\sin\theta\cos\theta-(1-\cos\theta)\sin\theta
=\sin\theta(2\cos\theta-1),
$$

$$
\displaystyle
\dfrac{dy}{d\theta}
=\sin^2\theta+(1-\cos\theta)\cos\theta.
$$

在 $\displaystyle \theta=\dfrac{\pi}{6}$ 时，

$$
\displaystyle
\left.\dfrac{dx}{d\theta}\right|_{\theta=\frac{\pi}{6}}
=\dfrac{\sqrt3-1}{2},
$$

$$
\displaystyle
\left.\dfrac{dy}{d\theta}\right|_{\theta=\frac{\pi}{6}}
=\dfrac{\sqrt3-1}{2}.
$$

所以切线斜率为

$$
\displaystyle
\dfrac{dy}{dx}
=\dfrac{\frac{dy}{d\theta}}{\frac{dx}{d\theta}}
=1.
$$

切线方程为

$$
\displaystyle y-y_0=x-x_0.
$$

代入 $\displaystyle (x_0,y_0)$，得

$$
\displaystyle y=x+y_0-x_0
=x+\dfrac{5-3\sqrt3}{4}.
$$

法线斜率为 $\displaystyle -1$，故法线方程为

$$
\displaystyle y-y_0=-(x-x_0),
$$

即

$$
\displaystyle x+y=x_0+y_0=\dfrac{\sqrt3-1}{4}.
$$

</details>

### （二）相切的充要条件

1. **【2002-1-7 分】** 已知两曲线 $\displaystyle y=f(x)$ 与

$$
\displaystyle y=\int_0^{\arctan x}e^{-t^2}\,\mathrm{d}t
$$

在点 $(0,0)$ 处的切线相同，写出此切线方程，并求极限

$$
\displaystyle \lim_{n\to\infty}n f\left(\dfrac{2}{n}\right).
$$

<details>
<summary>查看答案与解析</summary>

**答案：** 切线方程为 $\displaystyle y=x$；极限为 $\displaystyle 2$。

**解析：** 令

$$
\displaystyle F(x)=\int_0^{\arctan x}e^{-t^2}\,\mathrm{d}t.
$$

由变上限积分求导公式与链式法则，

$$
\displaystyle
F'(x)
=e^{-(\arctan x)^2}\cdot\dfrac{1}{1+x^2}.
$$

因此

$$
\displaystyle F(0)=0,
\qquad
F'(0)=1.
$$

故曲线 $\displaystyle y=F(x)$ 在原点处的切线为

$$
\displaystyle y=x.
$$

由于两曲线在原点处切线相同，所以

$$
\displaystyle f(0)=0,
\qquad
f'(0)=1.
$$

于是

$$
\displaystyle
\lim_{n\to\infty}n f\left(\dfrac{2}{n}\right)
=2\lim_{n\to\infty}\dfrac{f\left(\frac{2}{n}\right)-f(0)}{\frac{2}{n}}
=2f'(0)
=2.
$$

</details>

### （三）导数的物理应用（数一、数二）

1. **【2001-2-7 分】** 一个半球体状的雪堆，其体积融化的速率与半球面面积 $S$ 成正比，比例常数 $K>0$。假设在融化过程中雪堆始终保持半球体状，已知半径为 $r_0$ 的雪堆在开始融化的 $3$ 小时内，融化了其体积的 $\dfrac{7}{8}$，问雪堆全部融化需要多少小时。

<details>
<summary>查看答案与解析</summary>

**答案：** 雪堆全部融化需要 $\displaystyle 6$ 小时。

**解析：** 设时刻 $t$ 时雪堆的半径为 $r=r(t)$，体积为 $V=V(t)$。

半球体的体积和半球面面积分别为

$$
\displaystyle V=\dfrac{2}{3}\pi r^3,
\qquad
S=2\pi r^2.
$$

由题意，体积减少的速率与半球面面积成正比，故

$$
\displaystyle \dfrac{\mathrm{d}V}{\mathrm{d}t}=-KS=-2\pi Kr^2.
$$

另一方面，由复合函数求导法则，

$$
\displaystyle
\dfrac{\mathrm{d}V}{\mathrm{d}t}
=\dfrac{\mathrm{d}V}{\mathrm{d}r}\dfrac{\mathrm{d}r}{\mathrm{d}t}
=2\pi r^2\dfrac{\mathrm{d}r}{\mathrm{d}t}.
$$

因此

$$
\displaystyle
2\pi r^2\dfrac{\mathrm{d}r}{\mathrm{d}t}
=-2\pi Kr^2,
$$

即

$$
\displaystyle \dfrac{\mathrm{d}r}{\mathrm{d}t}=-K.
$$

所以半径随时间均匀减小，且由 $r(0)=r_0$ 得

$$
\displaystyle r(t)=r_0-Kt.
$$

开始融化 $3$ 小时后，已融化原体积的 $\displaystyle \dfrac{7}{8}$，故剩余体积为原体积的 $\displaystyle \dfrac{1}{8}$。于是

$$
\displaystyle
\dfrac{\frac{2}{3}\pi r^3(3)}{\frac{2}{3}\pi r_0^3}
=\dfrac18,
$$

从而

$$
\displaystyle r(3)=\dfrac{r_0}{2}.
$$

代入 $\displaystyle r(3)=r_0-3K$，得

$$
\displaystyle r_0-3K=\dfrac{r_0}{2},
$$

所以

$$
\displaystyle K=\dfrac{r_0}{6}.
$$

设雪堆全部融化所需时间为 $T$，则 $r(T)=0$，即

$$
\displaystyle r_0-KT=0.
$$

因此

$$
\displaystyle
T=\dfrac{r_0}{K}
=\dfrac{r_0}{r_0/6}
=6.
$$

故雪堆全部融化需要 $\displaystyle 6$ 小时。

</details>

---

2. **【2018-2-11 分】** 已知曲线 $\displaystyle L:y=\dfrac{4}{9}x^2\ (x\ge0)$，点 $O(0,0)$，点 $A(0,1)$。设 $P$ 是 $L$ 上的动点，$S$ 是直线 $OA$ 与直线 $AP$ 及曲线 $L$ 所围图形的面积。若 $P$ 运动到点 $(3,4)$ 时沿 $x$ 轴正方向的速度是 $4$，求此时 $S$ 关于时间 $t$ 的变化率。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \left.\dfrac{\mathrm{d}S}{\mathrm{d}t}\right|_{P=(3,4)}=10.
$$

</div>
</div>

**解析：** 设动点

$$
\displaystyle P\left(x,\dfrac{4}{9}x^2\right),
\qquad x>0.
$$

直线 $AP$ 经过点 $\displaystyle A(0,1)$ 与点 $\displaystyle P\left(x,\dfrac{4}{9}x^2\right)$，其斜率为

$$
\displaystyle
k_{AP}
=\dfrac{\frac49x^2-1}{x}
=\dfrac{4x}{9}-\dfrac1x.
$$

故直线 $AP$ 的方程为

$$
\displaystyle
y=1+\left(\dfrac{4x}{9}-\dfrac1x\right)u,
$$

其中用 $u$ 表示积分变量。

因此，直线 $OA$、直线 $AP$ 与曲线 $L$ 围成的面积为

$$
\displaystyle
S(x)
=\int_0^x
\left[
1+\left(\dfrac{4x}{9}-\dfrac1x\right)u
-\dfrac49u^2
\right]\mathrm{d}u.
$$

计算得

$$
\begin{aligned}
\displaystyle S(x)
&=x+\dfrac12\left(\dfrac{4x}{9}-\dfrac1x\right)x^2
-\dfrac{4}{27}x^3\\
&=x+\dfrac{2}{9}x^3-\dfrac{x}{2}-\dfrac{4}{27}x^3\\
&=\dfrac{x}{2}+\dfrac{2}{27}x^3.
\end{aligned}
$$

于是

$$
\displaystyle
\dfrac{\mathrm{d}S}{\mathrm{d}x}
=\dfrac12+\dfrac{2}{9}x^2.
$$

由链式法则，

$$
\displaystyle
\dfrac{\mathrm{d}S}{\mathrm{d}t}
=\dfrac{\mathrm{d}S}{\mathrm{d}x}
\dfrac{\mathrm{d}x}{\mathrm{d}t}.
$$

当 $P$ 运动到点 $(3,4)$ 时，$x=3$，且由题意

$$
\displaystyle \dfrac{\mathrm{d}x}{\mathrm{d}t}=4.
$$

故

$$
\begin{aligned}
\displaystyle
\dfrac{\mathrm{d}S}{\mathrm{d}t}
&=\left(\dfrac12+\dfrac29\times3^2\right)\times4\\
&=\left(\dfrac12+2\right)\times4\\
&=10.
\end{aligned}
$$

因此，此时面积 $S$ 关于时间 $t$ 的变化率为 $\displaystyle 10$。

</details>
