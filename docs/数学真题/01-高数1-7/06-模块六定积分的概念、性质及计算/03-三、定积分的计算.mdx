---
title: "三、定积分的计算"
sidebar_position: 3
toc_min_heading_level: 2
toc_max_heading_level: 4
---

# 三、定积分的计算

>1.无穷区间上的积分

>2.有限区间上的积分

## 小题

### （一）基本考查

1. **【1987-5-4 分】** 计算定积分
$$
\displaystyle \int_{\frac12}^{1}e^{\sqrt{2x-1}}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 1$。

**解析：** 令
$$
  \displaystyle u=\sqrt{2x-1},
$$
则
$$
  \displaystyle x=\dfrac{u^2+1}{2},
  \qquad dx=u\,du.
$$
当 $\displaystyle x=\dfrac12$ 时，$\displaystyle u=0$；当 $\displaystyle x=1$ 时，$\displaystyle u=1$。因此
$$
  \begin{aligned}
  \displaystyle \int_{\frac12}^{1}e^{\sqrt{2x-1}}\,dx
  &=\int_0^1ue^u\,du\\
  &=\left.(u-1)e^u\right|_0^1\\
  &=1.
  \end{aligned}
$$

</details>

---

2. **【1987-123-3 分】** 设 $\displaystyle f(x)$ 为已知连续函数，
$$
\displaystyle I=t\int_0^{\frac{s}{t}}f(tx)\,dx,
\qquad s>0,
\quad t>0,
$$
则 $\displaystyle I$ 的值（ ）。

<div className="choice-options">
<div className="choice-option-row">A. 依赖于 $\displaystyle s$ 和 $\displaystyle t$ &emsp;&emsp; B. 依赖于 $\displaystyle s,t,x$</div>
<div className="choice-option-row">C. 依赖于 $\displaystyle t$ 和 $\displaystyle x$，不依赖于 $\displaystyle s$ &emsp;&emsp; D. 依赖于 $\displaystyle s$，不依赖于 $\displaystyle t$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 作变量代换
$$
  \displaystyle u=tx,
  \qquad dx=\dfrac{du}{t}.
$$
当 $\displaystyle x=0$ 时，$\displaystyle u=0$；当 $\displaystyle x=\dfrac{s}{t}$ 时，$\displaystyle u=s$。于是
$$
  \begin{aligned}
  \displaystyle I
  &=t\int_0^{\frac{s}{t}}f(tx)\,dx\\
  &=t\int_0^s f(u)\dfrac{du}{t}\\
  &=\int_0^s f(u)\,du.
  \end{aligned}
$$
因此 $\displaystyle I$ 只依赖于上限 $\displaystyle s$，与 $\displaystyle t$ 无关。积分变量 $\displaystyle x$ 是哑变量，不会影响积分值，故选 D。

</details>

---

3. **【1988-3-4 分】**
$$
\displaystyle \int_0^4e^{\sqrt{x}}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2e^2+2$。

**解析：** 令
$$
  \displaystyle u=\sqrt{x},
  \qquad x=u^2,
  \qquad dx=2u\,du.
$$
当 $\displaystyle x=0$ 时，$\displaystyle u=0$；当 $\displaystyle x=4$ 时，$\displaystyle u=2$。因此
$$
  \begin{aligned}
  \displaystyle \int_0^4e^{\sqrt{x}}\,dx
  &=2\int_0^2ue^u\,du\\
  &=2\left.(u-1)e^u\right|_0^2\\
  &=2(e^2+1)\\
  &=2e^2+2.
  \end{aligned}
$$

</details>

---

4. **【1988-45-2 分】** 判断正误：等式
$$
\displaystyle \int_0^a f(x)\,dx=-\int_0^a f(a-x)\,dx
$$
对任意连续函数 $\displaystyle f(x)$、常数 $\displaystyle a$ 都成立。

<details>
<summary>查看答案与解析</summary>

**答案：** 错误。

**解析：** 在右侧积分中令
$$
  \displaystyle u=a-x,
  \qquad dx=-du.
$$
当 $\displaystyle x=0$ 时，$\displaystyle u=a$；当 $\displaystyle x=a$ 时，$\displaystyle u=0$。因此
$$
  \begin{aligned}
  \displaystyle \int_0^a f(a-x)\,dx
  &=\int_a^0f(u)(-du)\\
  &=\int_0^a f(u)\,du.
  \end{aligned}
$$
正确关系应为
$$
  \displaystyle \int_0^a f(x)\,dx=\int_0^a f(a-x)\,dx,
$$
而不是右侧带负号，故原命题错误。

</details>

---

5. **【1989-3-3 分】**
$$
\displaystyle \int_0^\pi t\sin t\,dt=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \pi$。

**解析：** 分部积分，取
$$
  \displaystyle u=t,
  \qquad dv=\sin t\,dt,
$$
则
$$
  \displaystyle du=dt,
  \qquad v=-\cos t.
$$
所以
$$
  \begin{aligned}
  \displaystyle \int_0^\pi t\sin t\,dt
  &=\left.-t\cos t\right|_0^\pi+\int_0^\pi\cos t\,dt\\
  &=\pi+\left.\sin t\right|_0^\pi\\
  &=\pi.
  \end{aligned}
$$

</details>

---

6. **【1989-123-3 分】** 设 $\displaystyle f(x)$ 是连续函数，且
$$
\displaystyle f(x)=x+2\int_0^1f(t)\,dt,
$$
则 $\displaystyle f(x)=\underline{\qquad}$。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle x-1$。

**解析：** 设
$$
  \displaystyle A=\int_0^1f(t)\,dt.
$$
则题设可写为
$$
  \displaystyle f(x)=x+2A.
$$
两边在 $\displaystyle [0,1]$ 上积分，得
$$
  \begin{aligned}
  \displaystyle A
  &=\int_0^1(x+2A)\,dx\\
  &=\dfrac12+2A.
  \end{aligned}
$$
因而
$$
  \displaystyle A=-\dfrac12.
$$
代回原式，得到
$$
  \displaystyle f(x)=x-1.
$$

</details>

---

7. **【1990-3-3 分】**
$$
\displaystyle \int_0^1x\sqrt{1-x}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{4}{15}$。

**解析：** 令
$$
  \displaystyle u=1-x,
  \qquad x=1-u,
  \qquad dx=-du.
$$
则
$$
  \begin{aligned}
  \displaystyle \int_0^1x\sqrt{1-x}\,dx
  &=\int_0^1(1-u)u^{\frac12}\,du\\
  &=\int_0^1\left(u^{\frac12}-u^{\frac32}\right)du\\
  &=\dfrac23-\dfrac25\\
  &=\dfrac{4}{15}.
  \end{aligned}
$$

</details>

---

8. **【1990-12-5 分】** 求
$$
\displaystyle \int_0^1\dfrac{\ln(1+x)}{(2-x)^2}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac13\ln2$。

**解析：** 分部积分，取
$$
  \displaystyle u=\ln(1+x),
  \qquad dv=\dfrac{dx}{(2-x)^2},
$$
则
$$
  \displaystyle du=\dfrac{dx}{1+x},
  \qquad v=\dfrac{1}{2-x}.
$$
因而
$$
  \begin{aligned}
  \displaystyle \int_0^1\dfrac{\ln(1+x)}{(2-x)^2}\,dx
  &=\left.\dfrac{\ln(1+x)}{2-x}\right|_0^1
-\int_0^1\dfrac{dx}{(1+x)(2-x)}\\
  &=\ln2-\int_0^1\dfrac{dx}{(1+x)(2-x)}.
  \end{aligned}
$$
作部分分式分解：
$$
  \displaystyle \dfrac{1}{(1+x)(2-x)}
=\dfrac13\left(\dfrac{1}{1+x}+\dfrac{1}{2-x}\right).
$$
所以
$$
  \begin{aligned}
  \displaystyle \int_0^1\dfrac{dx}{(1+x)(2-x)}
  &=\dfrac13\left[\ln(1+x)-\ln(2-x)\right]_0^1\\
  &=\dfrac23\ln2.
  \end{aligned}
$$
因此
$$
  \displaystyle \int_0^1\dfrac{\ln(1+x)}{(2-x)^2}\,dx
=\ln2-\dfrac23\ln2
=\dfrac13\ln2.
$$

</details>

---

9. **【1991-3-5 分】** 计算
$$
\displaystyle \int_{1}^{4}\dfrac{dx}{x(1+\sqrt{x})}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2\ln\dfrac{4}{3}$。

**解析：** 令
$$
  \displaystyle u=\sqrt{x},\qquad x=u^2,\qquad dx=2u\,du.
$$
当 $\displaystyle x=1$ 时，$\displaystyle u=1$；当 $\displaystyle x=4$ 时，$\displaystyle u=2$。于是
$$
  \begin{aligned}
  \displaystyle \int_{1}^{4}\dfrac{dx}{x(1+\sqrt{x})}
  &=\int_1^2\dfrac{2u}{u^2(1+u)}\,du\\
  &=2\int_1^2\dfrac{1}{u(1+u)}\,du\\
  &=2\int_1^2\left(\dfrac1u-\dfrac{1}{1+u}\right)du\\
  &=2\left[\ln u-\ln(1+u)\right]_1^2\\
  &=2\ln\dfrac{4}{3}.
  \end{aligned}
$$
本题的关键是利用 $\displaystyle u=\sqrt{x}$ 消去根式，再作部分分式分解。

</details>

---

10. **【1992-3-5 分】** 求
$$
\displaystyle \int_{0}^{\pi}\sqrt{1-\sin x}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 4\sqrt2-4$。

**解析：** 由
$$
  \displaystyle 1-\sin x=\left(\sin\dfrac{x}{2}-\cos\dfrac{x}{2}\right)^2,
$$
得
$$
  \displaystyle \sqrt{1-\sin x}
=\left|\sin\dfrac{x}{2}-\cos\dfrac{x}{2}\right|.
$$
在 $\displaystyle 0\le x\le\dfrac\pi2$ 上，$\displaystyle \sin\dfrac{x}{2}\le\cos\dfrac{x}{2}$；在 $\displaystyle \dfrac\pi2\le x\le\pi$ 上，符号相反。因此
$$
  \begin{aligned}
  \displaystyle \int_0^\pi\sqrt{1-\sin x}\,dx
  &=\int_0^{\frac\pi2}\left(\cos\dfrac{x}{2}-\sin\dfrac{x}{2}\right)dx
+\int_{\frac\pi2}^{\pi}\left(\sin\dfrac{x}{2}-\cos\dfrac{x}{2}\right)dx\\
  &=(2\sqrt2-2)+(2\sqrt2-2)\\
  &=4\sqrt2-4.
  \end{aligned}
$$
易错点是直接去掉平方根外的绝对值；必须在 $\displaystyle x=\dfrac\pi2$ 处分段。

</details>

---

11. **【1993-3-5 分】**
$$
\displaystyle \int_{0}^{\frac{\pi}{4}}\dfrac{x}{1+\cos 2x}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac\pi8-\dfrac14\ln2$。

**解析：** 利用恒等式
$$
  \displaystyle 1+\cos2x=2\cos^2x,
$$
原积分化为
$$
  \displaystyle I=\dfrac12\int_0^{\frac\pi4}x\sec^2x\,dx.
$$
分部积分，取 $\displaystyle u=x$，$\displaystyle dv=\sec^2x\,dx$，则 $\displaystyle du=dx$，$\displaystyle v=\tan x$，所以
$$
  \begin{aligned}
  \displaystyle I
  &=\dfrac12\left[x\tan x\right]_0^{\frac\pi4}
-\dfrac12\int_0^{\frac\pi4}\tan x\,dx\\
  &=\dfrac\pi8-\dfrac12\left[-\ln(\cos x)\right]_0^{\frac\pi4}\\
  &=\dfrac\pi8-\dfrac14\ln2.
  \end{aligned}
$$

</details>

---

12. **【1994-3-5 分】** 计算
$$
\displaystyle \int_{0}^{1}x(1-x^4)^{\frac32}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{3\pi}{32}$。

**解析：** 令 $\displaystyle u=x^2$，则 $\displaystyle du=2x\,dx$，于是
$$
  \displaystyle \int_0^1x(1-x^4)^{\frac32}dx
=\dfrac12\int_0^1(1-u^2)^{\frac32}du.
$$
再令 $\displaystyle u=\sin\theta$，$\displaystyle 0\le\theta\le\dfrac\pi2$，则
$$
  \begin{aligned}
  \displaystyle \dfrac12\int_0^1(1-u^2)^{\frac32}du
  &=\dfrac12\int_0^{\frac\pi2}\cos^4\theta\,d\theta\\
  &=\dfrac12\cdot\dfrac{3\pi}{16}\\
  &=\dfrac{3\pi}{32}.
  \end{aligned}
$$
其中使用了
$$
  \displaystyle \int_0^{\frac\pi2}\cos^4\theta\,d\theta=\dfrac{3\pi}{16}.
$$

</details>

---

13. **【1996-3-5 分】** 计算
$$
\displaystyle \int_{0}^{\ln2}\sqrt{1-e^{-2x}}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \ln(2+\sqrt3)-\dfrac{\sqrt3}{2}$。

**解析：** 令 $\displaystyle t=e^{-x}$，则 $\displaystyle dx=-\dfrac{dt}{t}$。当 $\displaystyle x=0$ 时 $\displaystyle t=1$，当 $\displaystyle x=\ln2$ 时 $\displaystyle t=\dfrac12$，故
$$
  \displaystyle I=\int_{\frac12}^1\dfrac{\sqrt{1-t^2}}{t}\,dt.
$$
再令 $\displaystyle t=\sin\theta$，则 $\displaystyle \theta\in\left[\dfrac\pi6,\dfrac\pi2\right]$，从而
$$
  \begin{aligned}
  \displaystyle I
  &=\int_{\frac\pi6}^{\frac\pi2}\dfrac{\cos^2\theta}{\sin\theta}\,d\theta\\
  &=\int_{\frac\pi6}^{\frac\pi2}(\csc\theta-\sin\theta)\,d\theta\\
  &=\left[\ln\tan\dfrac\theta2+\cos\theta\right]_{\frac\pi6}^{\frac\pi2}\\
  &=-\ln\tan\dfrac\pi{12}-\dfrac{\sqrt3}{2}.
  \end{aligned}
$$
因为 $\displaystyle \tan\dfrac\pi{12}=2-\sqrt3$，且 $\displaystyle \dfrac1{2-\sqrt3}=2+\sqrt3$，所以
$$
  \displaystyle I=\ln(2+\sqrt3)-\dfrac{\sqrt3}{2}.
$$

</details>

---

14. **【1997-4-3 分】** 若
$$
\displaystyle f(x)=\dfrac{1}{1+x^2}+x^3\int_0^1f(x)\,dx,
$$
则
$$
\displaystyle \int_0^1f(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac\pi3$。

**解析：** 记
$$
  \displaystyle A=\int_0^1f(x)\,dx.
$$
则题设为
$$
  \displaystyle f(x)=\dfrac{1}{1+x^2}+Ax^3.
$$
两边在 $\displaystyle [0,1]$ 上积分，得
$$
  \begin{aligned}
  \displaystyle A
  &=\int_0^1\dfrac{dx}{1+x^2}+A\int_0^1x^3dx\\
  &=\dfrac\pi4+\dfrac A4.
  \end{aligned}
$$
因此
$$
  \displaystyle \dfrac34A=\dfrac\pi4,
  \qquad A=\dfrac\pi3.
$$

</details>

---

15. **【1997-3-3 分】** 若
$$
\displaystyle f(x)=\dfrac{1}{1+x^2}+\sqrt{1-x^2}\int_0^1f(x)\,dx,
$$
则
$$
\displaystyle \int_0^1f(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\pi}{4-\pi}$。

**解析：** 记
$$
  \displaystyle A=\int_0^1f(x)\,dx.
$$
由题设
$$
  \displaystyle f(x)=\dfrac{1}{1+x^2}+A\sqrt{1-x^2}.
$$
两边在 $\displaystyle [0,1]$ 上积分，得
$$
  \begin{aligned}
  \displaystyle A
  &=\int_0^1\dfrac{dx}{1+x^2}+A\int_0^1\sqrt{1-x^2}\,dx\\
  &=\dfrac\pi4+A\cdot\dfrac\pi4.
  \end{aligned}
$$
其中 $\displaystyle \int_0^1\sqrt{1-x^2}\,dx=\dfrac\pi4$，它表示单位圆第一象限的面积。于是
$$
  \displaystyle A\left(1-\dfrac\pi4\right)=\dfrac\pi4,
$$
所以
$$
  \displaystyle A=\dfrac{\pi}{4-\pi}.
$$

</details>

---

16. **【1999-2-3 分】** 函数
$$
\displaystyle y=\dfrac{x^2}{\sqrt{1-x^2}}
$$
在区间 $\displaystyle \left[\dfrac12,\dfrac{\sqrt3}{2}\right]$ 上的平均值为 $\underline{\qquad}$。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\pi(\sqrt3+1)}{12}$。

**解析：** 函数 $\displaystyle f(x)$ 在 $\displaystyle [a,b]$ 上的平均值为
$$
  \displaystyle \overline f=\dfrac{1}{b-a}\int_a^bf(x)\,dx.
$$
令 $\displaystyle x=\sin\theta$，则 $\displaystyle dx=\cos\theta\,d\theta$。当 $\displaystyle x=\dfrac12$ 时 $\displaystyle \theta=\dfrac\pi6$；当 $\displaystyle x=\dfrac{\sqrt3}{2}$ 时 $\displaystyle \theta=\dfrac\pi3$。于是
$$
  \begin{aligned}
  \displaystyle \int_{\frac12}^{\frac{\sqrt3}{2}}\dfrac{x^2}{\sqrt{1-x^2}}\,dx
  &=\int_{\frac\pi6}^{\frac\pi3}\sin^2\theta\,d\theta\\
  &=\left[\dfrac\theta2-\dfrac{\sin2\theta}{4}\right]_{\frac\pi6}^{\frac\pi3}\\
  &=\dfrac\pi{12}.
  \end{aligned}
$$
区间长度为
$$
  \displaystyle \dfrac{\sqrt3}{2}-\dfrac12=\dfrac{\sqrt3-1}{2}.
$$
因此平均值为
$$
  \begin{aligned}
  \displaystyle \overline f
  &=\dfrac{\frac\pi{12}}{\frac{\sqrt3-1}{2}}\\
  &=\dfrac{\pi}{6(\sqrt3-1)}\\
  &=\dfrac{\pi(\sqrt3+1)}{12}.
  \end{aligned}
$$

</details>

---

17. **【2024-2-5 分】** 某物体以速度
$$
\displaystyle v(t)=t+k\sin\pi t
$$
做直线运动，若它从 $\displaystyle t=0$ 到 $\displaystyle t=3$ 的时间段内平均速度是 $\displaystyle \dfrac52$，则 $\displaystyle k=\underline{\qquad}$。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{3\pi}{2}$。

**解析：** 在时间区间 $\displaystyle [0,3]$ 上，平均速度为
$$
  \displaystyle \overline v=\dfrac13\int_0^3v(t)\,dt.
$$
因而
$$
  \begin{aligned}
  \displaystyle \dfrac52
  &=\dfrac13\int_0^3(t+k\sin\pi t)\,dt\\
  &=\dfrac13\left(\dfrac92+k\int_0^3\sin\pi t\,dt\right).
  \end{aligned}
$$
又
$$
  \displaystyle \int_0^3\sin\pi t\,dt
=\left[-\dfrac{\cos\pi t}{\pi}\right]_0^3
=\dfrac2\pi.
$$
所以
$$
  \displaystyle \dfrac52=\dfrac32+\dfrac{2k}{3\pi}.
$$
解得
$$
  \displaystyle k=\dfrac{3\pi}{2}.
$$

</details>

---

18. **【2000-1-3 分】**
$$
\displaystyle \int_0^1\sqrt{2x-x^2}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac\pi4$。

**解析：** 配方得
$$
  \displaystyle 2x-x^2=1-(x-1)^2.
$$
因而
$$
  \displaystyle \int_0^1\sqrt{2x-x^2}\,dx
=\int_0^1\sqrt{1-(x-1)^2}\,dx.
$$
曲线
$$
  \displaystyle y=\sqrt{1-(x-1)^2}
$$
是圆 $\displaystyle (x-1)^2+y^2=1$ 的上半圆。在 $\displaystyle 0\le x\le1$ 上所对应的图形为半径为 $\displaystyle 1$ 的四分之一圆，因此
$$
  \displaystyle \int_0^1\sqrt{2x-x^2}\,dx=\dfrac14\pi\cdot1^2=\dfrac\pi4.
$$

</details>

---

19. **【2007-1-4 分】**
$$
\displaystyle \int_{1}^{2}\dfrac{1}{x^{3}}e^{\frac1x}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\sqrt e}{2}$。

**解析：** 令 $\displaystyle u=\dfrac1x$，则
$$
  \displaystyle x=\dfrac1u,\qquad dx=-\dfrac1{u^2}\,du.
$$
因而
$$
  \displaystyle \dfrac1{x^3}\,dx=u^3\left(-\dfrac1{u^2}\,du\right)=-u\,du.
$$
当 $\displaystyle x=1$ 时，$\displaystyle u=1$；当 $\displaystyle x=2$ 时，$\displaystyle u=\dfrac12$。于是
$$
  \begin{aligned}
  \displaystyle \int_1^2\dfrac{1}{x^3}e^{\frac1x}\,dx
  &=\int_1^{\frac12}-ue^u\,du\\
  &=\int_{\frac12}^1ue^u\,du\\
  &=\left[(u-1)e^u\right]_{\frac12}^1\\
  &=\dfrac{\sqrt e}{2}.
  \end{aligned}
$$

</details>

---

20. **【2008-3-4 分】** 设
$$
\displaystyle f\left(x+\dfrac1x\right)=\dfrac{x+x^3}{1+x^4},
$$
则
$$
\displaystyle \int_2^{2\sqrt2}f(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac12\ln3$。

**解析：** 令
$$
  \displaystyle u=x+\dfrac1x,\qquad x\ge1.
$$
则
$$
  \displaystyle du=\left(1-\dfrac1{x^2}\right)dx=\dfrac{x^2-1}{x^2}\,dx.
$$
当 $\displaystyle u=2$ 时，$\displaystyle x=1$；当 $\displaystyle u=2\sqrt2$ 时，由
$$
  \displaystyle x+\dfrac1x=2\sqrt2
$$
且 $\displaystyle x\ge1$，得 $\displaystyle x=1+\sqrt2$。因此
$$
  \begin{aligned}
  \displaystyle \int_2^{2\sqrt2}f(u)\,du
  &=\int_1^{1+\sqrt2}\dfrac{x+x^3}{1+x^4}\cdot\dfrac{x^2-1}{x^2}\,dx\\
  &=\int_1^{1+\sqrt2}\dfrac{x^4-1}{x(x^4+1)}\,dx.
  \end{aligned}
$$
注意到
$$
  \displaystyle \dfrac{x^4-1}{x(x^4+1)}
=\dfrac12\left(\ln\dfrac{x^4+1}{x^2}\right)',
$$
故
$$
  \begin{aligned}
  \displaystyle \int_2^{2\sqrt2}f(u)\,du
  &=\dfrac12\left[\ln\dfrac{x^4+1}{x^2}\right]_1^{1+\sqrt2}\\
  &=\dfrac12\ln\dfrac{(1+\sqrt2)^2+(1+\sqrt2)^{-2}}{2}.
  \end{aligned}
$$
又因为
$$
  \displaystyle (1+\sqrt2)+\dfrac1{1+\sqrt2}=2\sqrt2,
$$
所以
$$
  \displaystyle (1+\sqrt2)^2+(1+\sqrt2)^{-2}=(2\sqrt2)^2-2=6.
$$
因此
$$
  \displaystyle \int_2^{2\sqrt2}f(x)\,dx=\dfrac12\ln\dfrac62=\dfrac12\ln3.
$$

</details>

---

21. **【2009-2-4 分】**
$$
\displaystyle \lim_{n\to\infty}\int_0^1e^{-x}\sin nx\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 0$。

**解析：** 分部积分，取
$$
  \displaystyle u=e^{-x},\qquad dv=\sin nx\,dx,
$$
则
$$
  \displaystyle du=-e^{-x}\,dx,\qquad v=-\dfrac{\cos nx}{n}.
$$
因而
$$
  \begin{aligned}
  \displaystyle \int_0^1e^{-x}\sin nx\,dx
  &=-\left.\dfrac{e^{-x}\cos nx}{n}\right|_0^1
-\dfrac1n\int_0^1e^{-x}\cos nx\,dx.
  \end{aligned}
$$
于是
$$
  \begin{aligned}
  \displaystyle \left|\int_0^1e^{-x}\sin nx\,dx\right|
  &\le\dfrac{1+e^{-1}}n+\dfrac1n\int_0^1e^{-x}\,dx\\
  &\le\dfrac3n\to0.
  \end{aligned}
$$
由夹逼定理，所求极限为 $\displaystyle 0$。

</details>

---

22. **【2010-1-4 分】**
$$
\displaystyle \int_0^{\pi^2}\sqrt{x}\cos\sqrt{x}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle -4\pi$。

**解析：** 令 $\displaystyle t=\sqrt{x}$，则 $\displaystyle x=t^2$，$\displaystyle dx=2t\,dt$。积分上下限由 $\displaystyle x=0,\pi^2$ 变为 $\displaystyle t=0,\pi$，故
$$
  \displaystyle \int_0^{\pi^2}\sqrt{x}\cos\sqrt{x}\,dx
=2\int_0^\pi t^2\cos t\,dt.
$$
连续分部积分可得
$$
  \displaystyle \int t^2\cos t\,dt=t^2\sin t+2t\cos t-2\sin t+C.
$$
因此
$$
  \begin{aligned}
  \displaystyle 2\int_0^\pi t^2\cos t\,dt
  &=2\left[t^2\sin t+2t\cos t-2\sin t\right]_0^\pi\\
  &=2(-2\pi)\\
  &=-4\pi.
  \end{aligned}
$$

</details>

---

23. **【2012-1-4 分】**
$$
\displaystyle \int_0^2x\sqrt{2x-x^2}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac\pi2$。

**解析：** 配方得
$$
  \displaystyle 2x-x^2=1-(x-1)^2.
$$
令 $\displaystyle t=x-1$，则
$$
  \begin{aligned}
  \displaystyle \int_0^2x\sqrt{2x-x^2}\,dx
  &=\int_{-1}^1(t+1)\sqrt{1-t^2}\,dt\\
  &=\int_{-1}^1t\sqrt{1-t^2}\,dt+
  \int_{-1}^1\sqrt{1-t^2}\,dt.
  \end{aligned}
$$
第一项的被积函数为奇函数，在对称区间上的积分为 $\displaystyle 0$；第二项表示单位圆上半圆的面积，等于 $\displaystyle \dfrac\pi2$。故
$$
  \displaystyle \int_0^2x\sqrt{2x-x^2}\,dx=\dfrac\pi2.
$$

</details>

---

24. **【2014-3-4 分】** 设
$$
\displaystyle \int_0^axe^{2x}\,dx=\dfrac14,
$$
则 $\displaystyle a=\underline{\qquad}$。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac12$。

**解析：** 分部积分得
$$
  \displaystyle \int xe^{2x}\,dx=\dfrac{2x-1}{4}e^{2x}+C.
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_0^axe^{2x}\,dx
  &=\left[\dfrac{2x-1}{4}e^{2x}\right]_0^a\\
  &=\dfrac{(2a-1)e^{2a}+1}{4}.
  \end{aligned}
$$
由题意
$$
  \displaystyle \dfrac{(2a-1)e^{2a}+1}{4}=\dfrac14,
$$
从而
$$
  \displaystyle (2a-1)e^{2a}=0.
$$
因为 $\displaystyle e^{2a}>0$，所以
$$
  \displaystyle a=\dfrac12.
$$

</details>

---

25. **【2020-2-4 分】**
$$
\displaystyle \int_0^1\dfrac{\arcsin\sqrt{x}}{\sqrt{x(1-x)}}\,dx=(\quad)
$$

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle \dfrac{\pi^2}{4}$ &emsp;&emsp; B. $\displaystyle \dfrac{\pi^2}{8}$ &emsp;&emsp; C. $\displaystyle \dfrac\pi4$ &emsp;&emsp; D. $\displaystyle \dfrac\pi8$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A。

**解析：** 令
$$
  \displaystyle \sqrt{x}=\sin t,
$$
则
$$
  \displaystyle x=\sin^2t,
  \qquad dx=2\sin t\cos t\,dt.
$$
当 $\displaystyle x$ 从 $\displaystyle 0$ 变到 $\displaystyle 1$ 时，$\displaystyle t$ 从 $\displaystyle 0$ 变到 $\displaystyle \dfrac\pi2$。并且
$$
  \displaystyle \arcsin\sqrt{x}=t,
  \qquad \sqrt{x(1-x)}=\sin t\cos t.
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_0^1\dfrac{\arcsin\sqrt{x}}{\sqrt{x(1-x)}}\,dx
  &=\int_0^{\frac\pi2}\dfrac{t}{\sin t\cos t}\cdot2\sin t\cos t\,dt\\
  &=2\int_0^{\frac\pi2}t\,dt\\
  &=\left[t^2\right]_0^{\frac\pi2}\\
  &=\dfrac{\pi^2}{4}.
  \end{aligned}
$$
故选 A。其余选项均与换元后的积分值不符。

</details>

---

26. **【2022-1-5 分】**
$$
\displaystyle \int_1^{e^2}\dfrac{\ln x}{\sqrt{x}}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 4$。

**解析：** 令 $\displaystyle x=t^2$，则 $\displaystyle dx=2t\,dt$，$\displaystyle \sqrt{x}=t$。积分上下限由 $\displaystyle x=1,e^2$ 变为 $\displaystyle t=1,e$。于是
$$
  \begin{aligned}
  \displaystyle \int_1^{e^2}\dfrac{\ln x}{\sqrt{x}}\,dx
  &=\int_1^e\dfrac{\ln t^2}{t}\cdot2t\,dt\\
  &=4\int_1^e\ln t\,dt\\
  &=4\left[t\ln t-t\right]_1^e\\
  &=4.
  \end{aligned}
$$

</details>

---

27. **【2022-2-5 分】**
$$
\displaystyle \int_0^1\dfrac{2x+3}{x^2-x+1}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{8\pi}{3\sqrt3}$。

**解析：** 将分子拆成分母导数与常数项：
$$
  \displaystyle 2x+3=(2x-1)+4.
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_0^1\dfrac{2x+3}{x^2-x+1}\,dx
  &=\int_0^1\dfrac{2x-1}{x^2-x+1}\,dx
+4\int_0^1\dfrac{dx}{x^2-x+1}.
  \end{aligned}
$$
第一项为
$$
  \displaystyle \left[\ln(x^2-x+1)\right]_0^1=0.
$$
又
$$
  \displaystyle x^2-x+1=\left(x-\dfrac12\right)^2+\dfrac34,
$$
故
$$
  \begin{aligned}
  \displaystyle 4\int_0^1\dfrac{dx}{x^2-x+1}
  &=\dfrac8{\sqrt3}
  \left[\arctan\dfrac{2x-1}{\sqrt3}\right]_0^1\\
  &=\dfrac8{\sqrt3}\left(\dfrac\pi6+\dfrac\pi6\right)\\
  &=\dfrac{8\pi}{3\sqrt3}.
  \end{aligned}
$$

</details>

---

28. **【2022-3-5 分】**
$$
\displaystyle \int_0^2\dfrac{2x-4}{x^2+2x+4}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \ln3-\dfrac\pi{\sqrt3}$。

**解析：** 将分子改写为
$$
  \displaystyle 2x-4=(2x+2)-6.
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_0^2\dfrac{2x-4}{x^2+2x+4}\,dx
  &=\int_0^2\dfrac{2x+2}{x^2+2x+4}\,dx
-6\int_0^2\dfrac{dx}{x^2+2x+4}\\
  &=\left[\ln(x^2+2x+4)\right]_0^2
-6\int_0^2\dfrac{dx}{(x+1)^2+3}.
  \end{aligned}
$$
第一项为
$$
  \displaystyle \ln12-\ln4=\ln3.
$$
第二项为
$$
  \begin{aligned}
  \displaystyle 6\int_0^2\dfrac{dx}{(x+1)^2+3}
  &=2\sqrt3\left[\arctan\dfrac{x+1}{\sqrt3}\right]_0^2\\
  &=2\sqrt3\left(\dfrac\pi3-\dfrac\pi6\right)\\
  &=\dfrac\pi{\sqrt3}.
  \end{aligned}
$$
所以
$$
  \displaystyle \int_0^2\dfrac{2x-4}{x^2+2x+4}\,dx
=\ln3-\dfrac\pi{\sqrt3}.
$$

</details>

---

29. **【2023-12-5 分】** 设连续函数 $\displaystyle f(x)$ 满足：$\displaystyle f(x+2)-f(x)=x$，$\displaystyle \int_0^2 f(x)\,dx=0$，则
$$
\displaystyle \int_1^3 f(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac12$。

**解析：** 将积分在 $\displaystyle x=2$ 处分开：
$$
  \displaystyle \int_1^3 f(x)\,dx
=\int_1^2 f(x)\,dx+\int_2^3 f(x)\,dx.
$$
对第二项令 $\displaystyle x=t+2$，则
$$
  \begin{aligned}
  \displaystyle \int_2^3 f(x)\,dx
  &=\int_0^1 f(t+2)\,dt\\
  &=\int_0^1[f(t)+t] \,dt.
  \end{aligned}
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_1^3 f(x)\,dx
  &=\int_1^2 f(x)\,dx+\int_0^1 f(x)\,dx+\int_0^1x\,dx\\
  &=\int_0^2f(x)\,dx+\dfrac12\\
  &=\dfrac12.
  \end{aligned}
$$

</details>

---

30. **【2025-123-5 分】** 计算
$$
\displaystyle \int_0^1\dfrac{1}{(x+1)(x^2-2x+2)}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\pi}{10}+\dfrac{3}{10}\ln2$。

**解析：** 作部分分式分解：
$$
  \begin{aligned}
  \displaystyle \dfrac{1}{(x+1)(x^2-2x+2)}
  &=\dfrac{A}{x+1}+\dfrac{Bx+C}{x^2-2x+2}.
  \end{aligned}
$$
比较系数得
$$
  \displaystyle A=\dfrac15,\qquad B=-\dfrac15,\qquad C=\dfrac35.
$$
故
$$
  \begin{aligned}
  \displaystyle \dfrac{1}{(x+1)(x^2-2x+2)}
  &=\dfrac1{5(x+1)}+\dfrac{-x+3}{5[(x-1)^2+1]}\\
  &=\dfrac1{5(x+1)}-\dfrac{2x-2}{10[(x-1)^2+1]}
+\dfrac{2}{5[(x-1)^2+1]}.
  \end{aligned}
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_0^1\dfrac{dx}{(x+1)(x^2-2x+2)}
  &=\dfrac15\left[\ln(x+1)-\dfrac12\ln((x-1)^2+1)
+2\arctan(x-1)\right]_0^1\\
  &=\dfrac15\left(\dfrac32\ln2+\dfrac\pi2\right)\\
  &=\dfrac{3}{10}\ln2+\dfrac\pi{10}.
  \end{aligned}
$$

</details>

### （二）显式分段函数

1. **【1991-3-3 分】** 设函数
$$
\displaystyle
f(x)=
\begin{cases}
x^2,&0\le x\le1,\\
2-x,&1<x\le2,
\end{cases}
\qquad
F(x)=\int_0^x f(t)\,dt,\quad 0\le x\le2,
$$
则（ ）。
$$
\begin{aligned}
&A.\quad F(x)=
\begin{cases}
\displaystyle \dfrac{x^3}{3},&0\le x\le1,\\
\displaystyle \dfrac13+2x-\dfrac{x^2}{2},&1<x\le2;
\end{cases}\\[2mm]
&B.\quad F(x)=
\begin{cases}
\displaystyle \dfrac{x^3}{3},&0\le x\le1,\\
\displaystyle -\dfrac76+2x-\dfrac{x^2}{2},&1<x\le2;
\end{cases}\\[2mm]
&C.\quad F(x)=
\begin{cases}
\displaystyle \dfrac{x^3}{3},&0\le x\le1,\\
\displaystyle \dfrac{x^3}{3}+2x-\dfrac{x^2}{2},&1<x\le2;
\end{cases}\\[2mm]
&D.\quad F(x)=
\begin{cases}
\displaystyle \dfrac{x^3}{3},&0\le x\le1,\\
\displaystyle 2x-\dfrac{x^2}{2},&1<x\le2.
\end{cases}
\end{aligned}
$$

<details>
<summary>查看答案与解析</summary>

**答案：** B。

**解析：** 当 $\displaystyle 0\le x\le1$ 时，
$$
  \displaystyle F(x)=\int_0^x t^2\,dt=\dfrac{x^3}{3}.
$$
当 $\displaystyle 1<x\le2$ 时，积分区间跨过分段点 $\displaystyle t=1$，故
$$
  \begin{aligned}
  \displaystyle F(x)
  &=\int_0^1t^2\,dt+\int_1^x(2-t)\,dt\\
  &=\dfrac13+\left[2t-\dfrac{t^2}{2}\right]_1^x\\
  &=-\dfrac76+2x-\dfrac{x^2}{2}.
  \end{aligned}
$$
因此选 B。A 未扣除第二段积分在下限 $\displaystyle t=1$ 处的值；C、D 均未正确处理跨分段点积分。

</details>

---

2. **【1992-13-5 分】** 设
$$
\displaystyle
f(x)=
\begin{cases}
1+x^2,&x\le0,\\
e^{-x},&x>0,
\end{cases}
$$
求
$$
\displaystyle \int_1^3f(x-2)\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac73-\dfrac1e$。

**解析：** 令 $\displaystyle t=x-2$，则 $\displaystyle dx=dt$，上下限由 $\displaystyle x=1,3$ 变为 $\displaystyle t=-1,1$。由于被积函数在 $\displaystyle t=0$ 处分段，故
$$
  \begin{aligned}
  \displaystyle \int_1^3f(x-2)\,dx
  &=\int_{-1}^1f(t)\,dt\\
  &=\int_{-1}^0(1+t^2)\,dt+\int_0^1e^{-t}\,dt\\
  &=\left[t+\dfrac{t^3}{3}\right]_{-1}^0+\left[-e^{-t}\right]_0^1\\
  &=\dfrac43+1-\dfrac1e\\
  &=\dfrac73-\dfrac1e.
  \end{aligned}
$$

</details>

---

3. **【1993-3-3 分】** 已知
$$
\displaystyle
f(x)=
\begin{cases}
x^2,&0\le x<1,\\
1,&1\le x\le2,
\end{cases}
\qquad
F(x)=\int_1^x f(t)\,dt\quad(0\le x\le2),
$$
则 $\displaystyle F(x)$ 为（ ）。
$$
\begin{aligned}
&A.\quad
\begin{cases}
\displaystyle \dfrac{x^3}{3},&0\le x<1,\\
x,&1\le x\le2;
\end{cases}\\[2mm]
&B.\quad
\begin{cases}
\displaystyle \dfrac{x^3}{3}-\dfrac13,&0\le x<1,\\
x,&1\le x\le2;
\end{cases}\\[2mm]
&C.\quad
\begin{cases}
\displaystyle \dfrac{x^3}{3},&0\le x<1,\\
x-1,&1\le x\le2;
\end{cases}\\[2mm]
&D.\quad
\begin{cases}
\displaystyle \dfrac{x^3}{3}-\dfrac13,&0\le x<1,\\
x-1,&1\le x\le2.
\end{cases}
\end{aligned}
$$

<details>
<summary>查看答案与解析</summary>

**答案：** D。

**解析：** 当 $\displaystyle 0\le x<1$ 时，积分上限小于下限，故
$$
  \begin{aligned}
  \displaystyle F(x)
  &=\int_1^x t^2\,dt
=-\int_x^1t^2\,dt
=\dfrac{x^3}{3}-\dfrac13.
  \end{aligned}
$$
当 $\displaystyle 1\le x\le2$ 时，
$$
  \displaystyle F(x)=\int_1^x1\,dt=x-1.
$$
因此选 D。A、C 忽略了第一段积分的下限为 $\displaystyle 1$；A、B 又错误地把第二段写成 $\displaystyle x$。

</details>

---

4. **【2004-34-4 分】** 设
$$
\displaystyle
f(x)=
\begin{cases}
xe^{x^2},&-\dfrac12\le x<\dfrac12,\\
-1,&x\ge\dfrac12,
\end{cases}
$$
则
$$
\displaystyle \int_{\frac12}^{2}f(x-1)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle -\dfrac12$。

**解析：** 令 $\displaystyle t=x-1$，则积分区间变为 $\displaystyle \left[-\dfrac12,1\right]$，并在 $\displaystyle t=\dfrac12$ 处分段：
$$
  \begin{aligned}
  \displaystyle \int_{\frac12}^{2}f(x-1)\,dx
  &=\int_{-\frac12}^{\frac12}te^{t^2}\,dt+\int_{\frac12}^{1}(-1)\,dt.
  \end{aligned}
$$
第一项的被积函数 $\displaystyle te^{t^2}$ 为奇函数，在对称区间上的积分为 $\displaystyle 0$；第二项为
$$
  \displaystyle -\left(1-\dfrac12\right)=-\dfrac12.
$$
故原积分为 $\displaystyle -\dfrac12$。

</details>

### （三）隐式分段函数

1. **【2021-3-5 分】**
$$
\displaystyle \int_{\sqrt5}^{5}\dfrac{x}{\sqrt{|x^2-9|}}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 6$。

**解析：** 绝对值内部 $\displaystyle x^2-9$ 在 $\displaystyle x=3$ 处变号，而 $\displaystyle \sqrt5<3<5$，故应分段积分：
$$
  \begin{aligned}
  \displaystyle \int_{\sqrt5}^{5}\dfrac{x}{\sqrt{|x^2-9|}}\,dx
  &=\int_{\sqrt5}^{3}\dfrac{x}{\sqrt{9-x^2}}\,dx
+\int_3^5\dfrac{x}{\sqrt{x^2-9}}\,dx\\
  &=\left[-\sqrt{9-x^2}\right]_{\sqrt5}^{3}
+\left[\sqrt{x^2-9}\right]_3^5\\
  &=2+4\\
  &=6.
  \end{aligned}
$$

</details>

### （四）积分区间对称且被积函数为奇（偶）函数

1. **【1987-45-2 分】** 判断正误：
$$
\displaystyle \int_{-\pi}^{\pi}x^4\sin x\,dx=0.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** 正确。

**解析：** $\displaystyle x^4$ 是偶函数，$\displaystyle \sin x$ 是奇函数，所以 $\displaystyle x^4\sin x$ 是奇函数。奇函数在关于原点对称的区间上的定积分为零，故
$$
  \displaystyle \int_{-\pi}^{\pi}x^4\sin x\,dx=0.
$$

</details>

---

2. **【1991-5-5 分】** 求定积分
$$
\displaystyle I=\int_{-1}^{1}(2x+|x|+1)^2\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{22}{3}$。

**解析：** 按 $\displaystyle x=0$ 分段。若 $\displaystyle -1\le x<0$，则 $\displaystyle |x|=-x$，故
$$
  \displaystyle 2x+|x|+1=x+1.
$$
若 $\displaystyle 0\le x\le1$，则 $\displaystyle |x|=x$，故
$$
  \displaystyle 2x+|x|+1=3x+1.
$$
因此
$$
  \begin{aligned}
  \displaystyle I
  &=\int_{-1}^0(x+1)^2\,dx+\int_0^1(3x+1)^2\,dx\\
  &=\left[\dfrac{(x+1)^3}{3}\right]_{-1}^{0}
+\int_0^1(9x^2+6x+1)\,dx\\
  &=\dfrac13+(3+3+1)\\
  &=\dfrac{22}{3}.
  \end{aligned}
$$

</details>

---

3. **【1994-45-3 分】**
$$
\displaystyle \int_{-2}^{2}\dfrac{x+|x|}{2+x^2}\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \ln3$。

**解析：** 当 $\displaystyle x<0$ 时，$\displaystyle |x|=-x$，所以 $\displaystyle x+|x|=0$；当 $\displaystyle x\ge0$ 时，$\displaystyle |x|=x$，所以 $\displaystyle x+|x|=2x$。因此
$$
  \begin{aligned}
  \displaystyle \int_{-2}^{2}\dfrac{x+|x|}{2+x^2}\,dx
  &=\int_0^2\dfrac{2x}{2+x^2}\,dx\\
  &=\left[\ln(2+x^2)\right]_0^2\\
  &=\ln6-\ln2\\
  &=\ln3.
  \end{aligned}
$$

</details>

---

4. **【1996-3-3 分】**
$$
\displaystyle \int_{-1}^{1}\left(x+\sqrt{1-x^2}\right)^2\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2$。

**解析：** 展开被积函数：
$$
  \begin{aligned}
  \displaystyle \left(x+\sqrt{1-x^2}\right)^2
  &=x^2+(1-x^2)+2x\sqrt{1-x^2}\\
  &=1+2x\sqrt{1-x^2}.
  \end{aligned}
$$
其中 $\displaystyle 2x\sqrt{1-x^2}$ 是奇函数，在对称区间 $\displaystyle [-1,1]$ 上积分为零。因此
$$
  \begin{aligned}
  \displaystyle \int_{-1}^{1}\left(x+\sqrt{1-x^2}\right)^2\,dx
  &=\int_{-1}^{1}1\,dx
+\int_{-1}^{1}2x\sqrt{1-x^2}\,dx\\
  &=2+0=2.
  \end{aligned}
$$

</details>

---

5. **【2001-2-3 分】**
$$
\displaystyle \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}
\left(x^3+\sin^2x\right)\cos^2x\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\pi}{8}$。

**解析：** 将积分拆开：
$$
  \displaystyle I=
  \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}x^3\cos^2x\,dx
+\int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}\sin^2x\cos^2x\,dx.
$$
因为 $\displaystyle x^3\cos^2x$ 为奇函数，第一项为零。又
$$
  \displaystyle \sin^2x\cos^2x=\dfrac14\sin^22x
=\dfrac18(1-\cos4x),
$$
所以
$$
  \begin{aligned}
  \displaystyle I
  &=\dfrac18\int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}(1-\cos4x)\,dx\\
  &=\dfrac18\left[x-\dfrac14\sin4x\right]_{-\frac{\pi}{2}}^{\frac{\pi}{2}}\\
  &=\dfrac{\pi}{8}.
  \end{aligned}
$$

</details>

---

6. **【2003-4-4 分】**
$$
\displaystyle \int_{-1}^{1}\left(xe^{x^{2}}+e^{-x}\right)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle e-e^{-1}$。

**解析：** 函数 $\displaystyle xe^{x^{2}}$ 为奇函数，因此
$$
  \displaystyle \int_{-1}^{1}xe^{x^{2}}\,dx=0.
$$
所以
$$
  \begin{aligned}
  \displaystyle \int_{-1}^{1}\left(xe^{x^{2}}+e^{-x}\right)\,dx
  &=\int_{-1}^{1}e^{-x}\,dx\\
  &=\left[-e^{-x}\right]_{-1}^{1}\\
  &=e-e^{-1}.
  \end{aligned}
$$

</details>

---

7. **【2014-1-4 分】** 若
$$
\displaystyle
\int_{-\pi}^{\pi}\left(x-a_1\cos x-b_1\sin x\right)^2\,dx
=\min_{a,b\in\mathbb R}
\left\{\int_{-\pi}^{\pi}(x-a\cos x-b\sin x)^2\,dx\right\},
$$
则 $\displaystyle a_1\cos x+b_1\sin x=$（ ）。

<div className="choice-options">
<div className="choice-option-row">A. $\displaystyle 2\sin x$ &emsp;&emsp; B. $\displaystyle 2\cos x$ &emsp;&emsp; C. $\displaystyle 2\pi\sin x$ &emsp;&emsp; D. $\displaystyle 2\pi\cos x$</div>
</div>

<details>
<summary>查看答案与解析</summary>

**答案：** A，即 $\displaystyle 2\sin x$。

**解析：** 令
$$
  \displaystyle J(a,b)=\int_{-\pi}^{\pi}(x-a\cos x-b\sin x)^2\,dx.
$$
在最小点 $\displaystyle (a_1,b_1)$ 处有
$$
  \displaystyle \dfrac{\partial J}{\partial a}=0,
  \qquad
  \dfrac{\partial J}{\partial b}=0.
$$
因此
$$
  \displaystyle \int_{-\pi}^{\pi}(x-a_1\cos x-b_1\sin x)\cos x\,dx=0,
$$
$$
  \displaystyle \int_{-\pi}^{\pi}(x-a_1\cos x-b_1\sin x)\sin x\,dx=0.
$$
利用奇偶性，
$$
  \displaystyle \int_{-\pi}^{\pi}x\cos x\,dx=0,
  \quad
  \int_{-\pi}^{\pi}\sin x\cos x\,dx=0,
$$
且 $\displaystyle \int_{-\pi}^{\pi}\cos^2x\,dx=\pi$，故 $\displaystyle a_1=0$。

又
$$
  \begin{aligned}
  \displaystyle \int_{-\pi}^{\pi}x\sin x\,dx
  &=2\int_0^\pi x\sin x\,dx\\
  &=2\left[-x\cos x+\sin x\right]_0^\pi\\
  &=2\pi,
  \end{aligned}
$$
而 $\displaystyle \int_{-\pi}^{\pi}\sin^2x\,dx=\pi$，所以
$$
  \displaystyle b_1=\dfrac{2\pi}{\pi}=2.
$$
因而 $\displaystyle a_1\cos x+b_1\sin x=2\sin x$。B、D 含有余弦项，与奇偶性所得 $\displaystyle a_1=0$ 矛盾；C 的系数多乘了 $\displaystyle \pi$。

</details>

---

8. **【2015-1-4 分】**
$$
\displaystyle \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}
\left(\dfrac{\sin x}{1+\cos x}+|x|\right)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\pi^2}{4}$。

**解析：** 因为 $\displaystyle 1+\cos x$ 为偶函数、$\displaystyle \sin x$ 为奇函数，所以
$$
  \displaystyle \dfrac{\sin x}{1+\cos x}
$$
是奇函数，在对称区间上的积分为零。$\displaystyle |x|$ 是偶函数，因此
$$
  \begin{aligned}
  \displaystyle \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}
  \left(\dfrac{\sin x}{1+\cos x}+|x|\right)\,dx
  &=2\int_0^{\frac{\pi}{2}}x\,dx\\
  &=\left[x^2\right]_0^{\frac{\pi}{2}}\\
  &=\dfrac{\pi^2}{4}.
  \end{aligned}
$$

</details>

---

9. **【2017-3-4 分】**
$$
\displaystyle \int_{-\pi}^{\pi}\left(\sin^3x+\sqrt{\pi^2-x^2}\right)\,dx
=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\pi^3}{2}$。

**解析：** $\displaystyle \sin^3x$ 为奇函数，故
$$
  \displaystyle \int_{-\pi}^{\pi}\sin^3x\,dx=0.
$$
曲线 $\displaystyle y=\sqrt{\pi^2-x^2}$ 是半径为 $\displaystyle \pi$ 的上半圆，因此
$$
  \displaystyle \int_{-\pi}^{\pi}\sqrt{\pi^2-x^2}\,dx
=\dfrac12\pi\cdot\pi^2
=\dfrac{\pi^3}{2}.
$$
所以原积分等于 $\displaystyle \dfrac{\pi^3}{2}$。

</details>

### （五）含有抽象函数导数的积分

1. **【1989-3-4 分】** 已知
$$
\displaystyle f(2)=\dfrac12,
\qquad f'(2)=0,
\qquad \int_0^2f(x)\,dx=1,
$$
求
$$
\displaystyle \int_0^1x^2f''(2x)\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 0$。

**解析：** 令 $\displaystyle u=2x$，则 $\displaystyle x=\dfrac{u}{2}$，$\displaystyle dx=\dfrac12du$，故
$$
  \displaystyle \int_0^1x^2f''(2x)\,dx
=\dfrac18\int_0^2u^2f''(u)\,du.
$$
连续两次使用分部积分：
$$
  \begin{aligned}
  \displaystyle \int_0^2u^2f''(u)\,du
  &=\left[u^2f'(u)\right]_0^2-2\int_0^2u f'(u)\,du\\
  &=4f'(2)-2\left(\left[uf(u)\right]_0^2-\int_0^2f(u)\,du\right)\\
  &=0-2\left(2\cdot\dfrac12-1\right)\\
  &=0.
  \end{aligned}
$$
因而原积分为 $\displaystyle 0$。

</details>

---

2. **【1999-3-3 分】** 设 $\displaystyle f(x)$ 有一个原函数
$$
\displaystyle F(x)=\dfrac{\sin x}{x},
$$
则
$$
\displaystyle \int_{\frac{\pi}{2}}^\pi x f'(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac4\pi-1$。

**解析：** 由 $\displaystyle F'(x)=f(x)$ 得
$$
  \displaystyle f(x)=\left(\dfrac{\sin x}{x}\right)'
=\dfrac{x\cos x-\sin x}{x^2}.
$$
对原积分作分部积分：
$$
  \begin{aligned}
  \displaystyle \int_{\frac{\pi}{2}}^\pi x f'(x)\,dx
  &=\left[xf(x)\right]_{\frac{\pi}{2}}^\pi
-\int_{\frac{\pi}{2}}^\pi f(x)\,dx\\
  &=\left[xf(x)\right]_{\frac{\pi}{2}}^\pi
-\left[F(x)\right]_{\frac{\pi}{2}}^\pi.
  \end{aligned}
$$
计算各端点：
$$
  \displaystyle \pi f(\pi)=-1,
  \qquad
  \dfrac{\pi}{2}f\left(\dfrac{\pi}{2}\right)=-\dfrac2\pi,
$$
$$
  \displaystyle F(\pi)=0,
  \qquad
F\left(\dfrac{\pi}{2}\right)=\dfrac2\pi.
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_{\frac{\pi}{2}}^\pi x f'(x)\,dx
  &=\left(-1+\dfrac2\pi\right)-\left(-\dfrac2\pi\right)\\
  &=\dfrac4\pi-1.
  \end{aligned}
$$

</details>

---

3. **【2018-1-4 分】** 设函数 $\displaystyle f(x)$ 具有二阶连续导数，若曲线 $\displaystyle y=f(x)$ 过点 $\displaystyle (0,0)$，且与曲线 $\displaystyle y=2^x$ 在点 $\displaystyle (1,2)$ 处相切，则
$$
\displaystyle \int_0^1x f''(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2\ln2-2$。

**解析：** 曲线经过 $\displaystyle (0,0)$，故 $\displaystyle f(0)=0$。两曲线在 $\displaystyle (1,2)$ 处相切，所以
$$
  \displaystyle f(1)=2,
  \qquad
f'(1)=\left.(2^x)'\right|_{x=1}=2\ln2.
$$
分部积分得
$$
  \begin{aligned}
  \displaystyle \int_0^1x f''(x)\,dx
  &=\left[xf'(x)\right]_0^1-\int_0^1f'(x)\,dx\\
  &=f'(1)-f(1)+f(0)\\
  &=2\ln2-2.
  \end{aligned}
$$

</details>

### （六）含有变限积分的积分

1. **【2019-2-4 分】** 设
$$
\displaystyle f(x)=\int_1^x\sqrt{1+t^4}\,dt,
$$
则
$$
\displaystyle \int_0^1x^2f(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{1-2\sqrt2}{18}$。

**解析：** 当 $\displaystyle 0\le x\le1$ 时，
$$
  \displaystyle f(x)=-\int_x^1\sqrt{1+t^4}\,dt.
$$
因此交换积分次序可得
$$
  \begin{aligned}
  \displaystyle \int_0^1x^2f(x)\,dx
  &=-\int_0^1x^2\left(\int_x^1\sqrt{1+t^4}\,dt\right)dx\\
  &=-\int_0^1\sqrt{1+t^4}\left(\int_0^t x^2\,dx\right)dt\\
  &=-\dfrac13\int_0^1t^3\sqrt{1+t^4}\,dt.
  \end{aligned}
$$
令 $\displaystyle u=1+t^4$，则 $\displaystyle du=4t^3dt$，故
$$
  \begin{aligned}
  \displaystyle \int_0^1x^2f(x)\,dx
  &=-\dfrac1{12}\int_1^2u^{\frac12}\,du\\
  &=-\dfrac1{18}\left[u^{\frac32}\right]_1^2\\
  &=\dfrac{1-2\sqrt2}{18}.
  \end{aligned}
$$

</details>

---

2. **【2019-3-4 分】** 设
$$
\displaystyle f(x)=x\int_1^x\dfrac{\sin t^2}{t}\,dt,
$$
则
$$
\displaystyle \int_0^1f(x)\,dx=\underline{\qquad}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\cos1-1}{4}$。

**解析：** 在 $\displaystyle 0\le x\le1$ 上，
$$
  \displaystyle \int_1^x\dfrac{\sin t^2}{t}\,dt
=-\int_x^1\dfrac{\sin t^2}{t}\,dt.
$$
于是
$$
  \begin{aligned}
  \displaystyle \int_0^1f(x)\,dx
  &=-\int_0^1x\left(\int_x^1\dfrac{\sin t^2}{t}\,dt\right)dx\\
  &=-\int_0^1\dfrac{\sin t^2}{t}\left(\int_0^t x\,dx\right)dt\\
  &=-\dfrac12\int_0^1t\sin t^2\,dt.
  \end{aligned}
$$
令 $\displaystyle u=t^2$，则 $\displaystyle du=2t\,dt$，所以
$$
  \begin{aligned}
  \displaystyle \int_0^1f(x)\,dx
  &=-\dfrac14\int_0^1\sin u\,du\\
  &=\dfrac{\cos1-1}{4}.
  \end{aligned}
$$

</details>

## 大题

### （一）基本考查

1. **【1987-3-8 分】** 计算定积分
$$
\displaystyle \int_0^1 x\arcsin x\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{\pi}{8}$。

**解析：** 采用分部积分。令
$$
  \displaystyle u=\arcsin x,\qquad dv=x\,dx,
$$
则
$$
  \displaystyle du=\dfrac{dx}{\sqrt{1-x^2}},\qquad v=\dfrac{x^2}{2}.
$$
因而
$$
  \begin{aligned}
  \displaystyle \int_0^1x\arcsin x\,dx
  &=\left.\dfrac{x^2}{2}\arcsin x\right|_0^1
-\dfrac12\int_0^1\dfrac{x^2}{\sqrt{1-x^2}}\,dx\\
  &=\dfrac{\pi}{4}-\dfrac12\int_0^1\dfrac{x^2}{\sqrt{1-x^2}}\,dx.
  \end{aligned}
$$
对后一积分令 $\displaystyle x=\sin t$，则 $\displaystyle dx=\cos t\,dt$，且 $\displaystyle t\in\left[0,\dfrac{\pi}{2}\right]$，所以
$$
  \begin{aligned}
  \displaystyle \int_0^1\dfrac{x^2}{\sqrt{1-x^2}}\,dx
  &=\int_0^{\frac{\pi}{2}}\sin^2t\,dt\\
  &=\dfrac{\pi}{4}.
  \end{aligned}
$$
因此
$$
  \displaystyle \int_0^1x\arcsin x\,dx
=\dfrac{\pi}{4}-\dfrac12\cdot\dfrac{\pi}{4}
=\dfrac{\pi}{8}.
$$

</details>

---

2. **【1990-3-10 分】** 设
$$
\displaystyle f(x)=\int_1^x\dfrac{\ln t}{1+t}\,dt,
\qquad x>0,
$$
求 $\displaystyle f(x)+f\left(\dfrac1x\right)$。

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac12\ln^2x$。

**解析：** 令
$$
  \displaystyle F(x)=f(x)+f\left(\dfrac1x\right),\qquad x>0.
$$
由变限积分求导公式，
$$
  \displaystyle f'(x)=\dfrac{\ln x}{1+x}.
$$
因此
$$
  \begin{aligned}
  \displaystyle F'(x)
  &=f'(x)+f'\left(\dfrac1x\right)\left(-\dfrac1{x^2}\right)\\
  &=\dfrac{\ln x}{1+x}
+\dfrac{\ln\frac1x}{1+\frac1x}\left(-\dfrac1{x^2}\right)\\
  &=\dfrac{\ln x}{1+x}+\dfrac{\ln x}{x(1+x)}\\
  &=\dfrac{\ln x}{x}.
  \end{aligned}
$$
于是
$$
  \displaystyle F(x)=\dfrac12\ln^2x+C.
$$
又 $\displaystyle f(1)=0$，故 $\displaystyle F(1)=0$，从而 $\displaystyle C=0$。所以
$$
  \displaystyle f(x)+f\left(\dfrac1x\right)=\dfrac12\ln^2x.
$$

</details>

### （二）显式分段函数

1. **【2002-2-7 分】** 设
$$
f(x)=
\begin{cases}
\displaystyle 2x+\dfrac32x^2, & -1\le x<0,\\[4pt]
\displaystyle \dfrac{xe^x}{(e^x+1)^2}, & 0\le x\le1,
\end{cases}
$$
求函数
$$
\displaystyle F(x)=\int_{-1}^x f(t)\,dt
$$
的表达式。

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
F(x)=
\begin{cases}
\displaystyle x^2+\dfrac{x^3}{2}-\dfrac12, & -1\le x<0,\\[8pt]
\displaystyle -\dfrac12+\dfrac{xe^x}{1+e^x}-\ln\dfrac{1+e^x}{2}, & 0\le x\le1.
\end{cases}
$$

</div>
</div>

**解析：** 当 $\displaystyle -1\le x<0$ 时，
$$
  \begin{aligned}
  \displaystyle F(x)
  &=\int_{-1}^x\left(2t+\dfrac32t^2\right)dt\\
  &=\left[t^2+\dfrac12t^3\right]_{-1}^x\\
  &=x^2+\dfrac{x^3}{2}-\dfrac12.
  \end{aligned}
$$
当 $\displaystyle 0\le x\le1$ 时，应在分点 $\displaystyle 0$ 处分段：
$$
  \displaystyle F(x)=\int_{-1}^0\left(2t+\dfrac32t^2\right)dt
+\int_0^x\dfrac{te^t}{(e^t+1)^2}\,dt.
$$
第一项为 $\displaystyle -\dfrac12$。又
$$
  \displaystyle \left(\dfrac{t}{1+e^t}\right)'
=\dfrac1{1+e^t}-\dfrac{te^t}{(1+e^t)^2},
$$
所以
$$
  \begin{aligned}
  \displaystyle \int_0^x\dfrac{te^t}{(1+e^t)^2}\,dt
  &=\int_0^x\dfrac{dt}{1+e^t}-\left.\dfrac{t}{1+e^t}\right|_0^x\\
  &=\left[t-\ln(1+e^t)\right]_0^x-\dfrac{x}{1+e^x}\\
  &=x-\ln(1+e^x)+\ln2-\dfrac{x}{1+e^x}\\
  &=\dfrac{xe^x}{1+e^x}-\ln\dfrac{1+e^x}{2}.
  \end{aligned}
$$
合并即得所求分段表达式。两段在 $\displaystyle x=0$ 处均取值 $\displaystyle -\dfrac12$，结果连续。

</details>

### （三）隐式分段函数

1. **【1988-3-7 分】** 求
$$
\displaystyle \int_{-1}^{x}(1-|t|)\,dt,
\qquad x\ge-1.
$$

<details>
<summary>查看答案与解析</summary>

<div className="exercise-label-row">
<strong>答案：</strong>
<div className="exercise-label-body">

$$
\displaystyle \int_{-1}^{x}(1-|t|)\,dt
=
\begin{cases}
\displaystyle \dfrac{(x+1)^2}{2}, & -1\le x<0,\\[8pt]
\displaystyle \dfrac12+x-\dfrac{x^2}{2}, & x\ge0.
\end{cases}
$$

</div>
</div>

**解析：** 绝对值使被积函数在 $\displaystyle t=0$ 处分段。

当 $\displaystyle -1\le x<0$ 时，$\displaystyle |t|=-t$，故
$$
  \begin{aligned}
  \displaystyle \int_{-1}^{x}(1-|t|)\,dt
  &=\int_{-1}^{x}(1+t)\,dt\\
  &=\left[t+\dfrac{t^2}{2}\right]_{-1}^{x}\\
  &=\dfrac{(x+1)^2}{2}.
  \end{aligned}
$$

当 $\displaystyle x\ge0$ 时，
$$
  \begin{aligned}
  \displaystyle \int_{-1}^{x}(1-|t|)\,dt
  &=\int_{-1}^{0}(1+t)\,dt+\int_0^x(1-t)\,dt\\
  &=\dfrac12+x-\dfrac{x^2}{2}.
  \end{aligned}
$$
注意第二段对所有 $\displaystyle x\ge0$ 均成立，即使 $\displaystyle x>1$ 时被积函数变为负值，也不影响积分公式。

</details>

---

2. **【1991-3-9 分】** 设函数 $\displaystyle f(x)$ 在 $\displaystyle (-\infty,+\infty)$ 内满足
$$
\displaystyle f(x)=f(x-\pi)+\sin x,
$$
且
$$
\displaystyle f(x)=x,
\qquad x\in[0,\pi),
$$
计算
$$
\displaystyle \int_\pi^{3\pi}f(x)\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \pi^2-2$。

**解析：** 先由递推关系分别确定两个区间上的函数表达式。

当 $\displaystyle x\in[\pi,2\pi)$ 时，$\displaystyle x-\pi\in[0,\pi)$，所以
$$
  \displaystyle f(x)=f(x-\pi)+\sin x=x-\pi+\sin x.
$$

当 $\displaystyle x\in[2\pi,3\pi)$ 时，$\displaystyle x-\pi\in[\pi,2\pi)$，故
$$
  \begin{aligned}
  \displaystyle f(x)
  &=f(x-\pi)+\sin x\\
  &=(x-2\pi)+\sin(x-\pi)+\sin x\\
  &=x-2\pi.
  \end{aligned}
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_\pi^{3\pi}f(x)\,dx
  &=\int_\pi^{2\pi}(x-\pi+\sin x)\,dx
+\int_{2\pi}^{3\pi}(x-2\pi)\,dx\\
  &=\left(\dfrac{\pi^2}{2}-2\right)+\dfrac{\pi^2}{2}\\
  &=\pi^2-2.
  \end{aligned}
$$

</details>

### （四）积分区间对称且被积函数为奇（偶）函数

1. **【1987-2-6 分】** 计算定积分
$$
\displaystyle \int_{-2}^{2}(|x|+x)e^{-|x|}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2-\dfrac{6}{e^2}$。

**解析：** 绝对值在 $\displaystyle x=0$ 处分段。

当 $\displaystyle -2\le x<0$ 时，$\displaystyle |x|=-x$，故
$$
  \displaystyle |x|+x=-x+x=0.
$$
当 $\displaystyle 0\le x\le2$ 时，$\displaystyle |x|=x$，故
$$
  \displaystyle (|x|+x)e^{-|x|}=2xe^{-x}.
$$
因此
$$
  \begin{aligned}
  \displaystyle \int_{-2}^{2}(|x|+x)e^{-|x|}\,dx
  &=2\int_0^2xe^{-x}\,dx\\
  &=2\left[-(x+1)e^{-x}\right]_0^2\\
  &=2\left(1-\dfrac{3}{e^2}\right)\\
  &=2-\dfrac{6}{e^2}.
  \end{aligned}
$$
本题也可先观察：负半轴上的被积函数恒为零，只需计算正半轴部分。

</details>

### （五）积分区间对称且被积函数不为奇（偶）函数

1. **【1995-45-6 分】** 设 $\displaystyle f(x),g(x)$ 在区间 $\displaystyle [-a,a]\ (a>0)$ 上连续，$\displaystyle g(x)$ 为偶函数，且 $\displaystyle f(x)$ 满足
$$
\displaystyle f(x)+f(-x)=A,
$$
其中 $\displaystyle A$ 为常数。

（1）证明：
$$
\displaystyle \int_{-a}^{a}f(x)g(x)\,dx
=A\int_0^ag(x)\,dx;
$$

（2）利用（1）的结论计算
$$
\displaystyle \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}|\sin x|\arctan e^x\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** （1）结论成立；

（2）$\displaystyle \dfrac{\pi}{2}$。

**解析：** **（1）证明。** 记
$$
\displaystyle I=\int_{-a}^{a}f(x)g(x)\,dx.
$$
作代换 $\displaystyle x=-t$，并利用 $\displaystyle g(x)$ 为偶函数，得
$$
\begin{aligned}
\displaystyle I
&=\int_{-a}^{a}f(-x)g(-x)\,dx\\
&=\int_{-a}^{a}f(-x)g(x)\,dx.
\end{aligned}
$$
两式相加，得
$$
\begin{aligned}
\displaystyle 2I
&=\int_{-a}^{a}[f(x)+f(-x)]g(x)\,dx\\
&=A\int_{-a}^{a}g(x)\,dx.
\end{aligned}
$$
又因为 $\displaystyle g(x)$ 为偶函数，
$$
\displaystyle \int_{-a}^{a}g(x)\,dx=2\int_0^ag(x)\,dx,
$$
所以
$$
\displaystyle I=A\int_0^ag(x)\,dx.
$$

**（2）计算。** 取
$$
\displaystyle f(x)=\arctan e^x,
\qquad
g(x)=|\sin x|.
$$
显然 $\displaystyle g(x)$ 为偶函数。由于 $\displaystyle e^x>0$，利用
$$
\displaystyle \arctan u+\arctan\dfrac1u=\dfrac{\pi}{2}
\qquad (u>0),
$$
有
$$
\displaystyle f(x)+f(-x)
=\arctan e^x+\arctan e^{-x}
=\dfrac{\pi}{2}.
$$
故由（1），
$$
\begin{aligned}
\displaystyle \int_{-\frac{\pi}{2}}^{\frac{\pi}{2}}|\sin x|\arctan e^x\,dx
&=\dfrac{\pi}{2}\int_0^{\frac{\pi}{2}}|\sin x|\,dx\\
&=\dfrac{\pi}{2}\int_0^{\frac{\pi}{2}}\sin x\,dx\\
&=\dfrac{\pi}{2}.
\end{aligned}
$$

</details>

### （六）含有抽象函数导数的积分

1. **【2001-2-7 分】** 设函数 $\displaystyle f(x),g(x)$ 满足
$$
\displaystyle f'(x)=g(x),
\qquad
\displaystyle g'(x)=2e^x-f(x),
$$
且
$$
\displaystyle f(0)=0,
\qquad
\displaystyle g(0)=2.
$$
求
$$
\displaystyle \int_0^\pi\left[\dfrac{g(x)}{1+x}-\dfrac{f(x)}{(1+x)^2}\right]dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle \dfrac{e^\pi+1}{\pi+1}$。

**解析：** 由 $\displaystyle f'(x)=g(x)$，被积函数恰为
$$
  \displaystyle \left[\dfrac{f(x)}{1+x}\right]'
=\dfrac{f'(x)}{1+x}-\dfrac{f(x)}{(1+x)^2}
=\dfrac{g(x)}{1+x}-\dfrac{f(x)}{(1+x)^2}.
$$
因而
$$
  \displaystyle \int_0^\pi\left[\dfrac{g(x)}{1+x}-\dfrac{f(x)}{(1+x)^2}\right]dx
=\dfrac{f(\pi)}{1+\pi}-f(0).
$$
还需由已知条件求 $\displaystyle f(\pi)$。对 $\displaystyle f'(x)=g(x)$ 求导，并代入 $\displaystyle g'(x)=2e^x-f(x)$，得
$$
  \displaystyle f''(x)+f(x)=2e^x.
$$
又
$$
  \displaystyle f(0)=0,
  \qquad
  \displaystyle f'(0)=g(0)=2.
$$
微分方程的通解为
$$
  \displaystyle f(x)=C_1\cos x+C_2\sin x+e^x.
$$
代入初始条件：
$$
  \displaystyle C_1+1=0,
  \qquad
  \displaystyle C_2+1=2,
$$
所以
$$
  \displaystyle C_1=-1,
  \qquad
  \displaystyle C_2=1,
$$
从而
$$
  \displaystyle f(x)=e^x-\cos x+\sin x.
$$
因此
$$
  \displaystyle f(\pi)=e^\pi-\cos\pi+\sin\pi=e^\pi+1.
$$
最终
$$
  \displaystyle \int_0^\pi\left[\dfrac{g(x)}{1+x}-\dfrac{f(x)}{(1+x)^2}\right]dx
=\dfrac{e^\pi+1}{\pi+1}.
$$

</details>

---

2. **【2005-12-11 分】** 如图，曲线 $\displaystyle C$ 的方程为 $\displaystyle y=f(x)$，点 $\displaystyle (3,2)$ 是它的一个拐点，直线 $\displaystyle l_1,l_2$ 分别是曲线 $\displaystyle C$ 在点 $\displaystyle (0,0)$ 与 $\displaystyle (3,2)$ 处的切线，其交点为 $\displaystyle (2,4)$。设函数 $\displaystyle f(x)$ 具有三阶连续导数，计算
$$
\displaystyle \int_0^3(x^2+x)f'''(x)\,dx.
$$

![2005-12-11 题图](高数真题图片/gs_down_2005_12_11.png)

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 20$。

**解析：** 由图中两条切线及其交点可确定端点处的一阶导数。

过 $\displaystyle (0,0)$ 与 $\displaystyle (2,4)$ 的切线斜率为
$$
  \displaystyle f'(0)=\dfrac{4-0}{2-0}=2.
$$
过 $\displaystyle (3,2)$ 与 $\displaystyle (2,4)$ 的切线斜率为
$$
  \displaystyle f'(3)=\dfrac{2-4}{3-2}=-2.
$$
同时
$$
  \displaystyle f(0)=0,
  \qquad
  \displaystyle f(3)=2.
$$
因为 $\displaystyle (3,2)$ 是拐点，且 $\displaystyle f(x)$ 具有三阶连续导数，所以 $\displaystyle f''(x)$ 连续，并有
$$
  \displaystyle f''(3)=0.
$$

记
$$
  \displaystyle I=\int_0^3(x^2+x)f'''(x)\,dx.
$$
第一次分部积分：
$$
  \begin{aligned}
  \displaystyle I
  &=\left[(x^2+x)f''(x)\right]_0^3
-\int_0^3(2x+1)f''(x)\,dx\\
  &=-\int_0^3(2x+1)f''(x)\,dx,
  \end{aligned}
$$
其中边界项因 $\displaystyle f''(3)=0$ 且 $\displaystyle x^2+x$ 在 $\displaystyle x=0$ 时为零而消失。

再次分部积分：
$$
  \begin{aligned}
  \displaystyle I
  &=-\left[(2x+1)f'(x)\right]_0^3
+2\int_0^3f'(x)\,dx\\
  &=-\left[7f'(3)-f'(0)\right]
+2[f(3)-f(0)]\\
  &=-\left[7(-2)-2\right]+2(2-0)\\
  &=20.
  \end{aligned}
$$

</details>

### （七）含有变限积分的积分

1. **【1995-3-8 分】** 设
$$
\displaystyle f(x)=\int_0^x\dfrac{\sin t}{\pi-t}\,dt,
$$
计算
$$
\displaystyle \int_0^\pi f(x)\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 2$。

**解析：** 当 $\displaystyle t\to\pi^-$ 时，
$$
  \displaystyle \dfrac{\sin t}{\pi-t}\to1,
$$
因而被积函数在 $\displaystyle t=\pi$ 处为可去间断，可补充定义为 $\displaystyle 1$ 后视为连续函数。

将 $\displaystyle f(x)$ 代入：
$$
  \displaystyle \int_0^\pi f(x)\,dx
=\int_0^\pi\left(\int_0^x\dfrac{\sin t}{\pi-t}\,dt\right)dx.
$$
积分区域为
$$
  \displaystyle 0\le t\le x\le\pi.
$$
交换积分次序，得
$$
  \begin{aligned}
  \displaystyle \int_0^\pi f(x)\,dx
  &=\int_0^\pi\left(\int_t^\pi dx\right)\dfrac{\sin t}{\pi-t}\,dt\\
  &=\int_0^\pi(\pi-t)\dfrac{\sin t}{\pi-t}\,dt\\
  &=\int_0^\pi\sin t\,dt\\
  &=2.
  \end{aligned}
$$
关键在于交换积分次序后，内层积分长度正好为 $\displaystyle \pi-t$，与分母约去。

</details>

---

2. **【2013-1-10 分】** 设
$$
\displaystyle f(x)=\int_1^x\dfrac{\ln(t+1)}{t}\,dt,
$$
计算
$$
\displaystyle \int_0^1\dfrac{f(x)}{\sqrt{x}}\,dx.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** $\displaystyle 8-2\pi-4\ln 2$。

**解析：** 记
$$
  \displaystyle I=\int_0^1\dfrac{f(x)}{\sqrt{x}}\,dx.
$$
当 $\displaystyle 0<x<1$ 时，
$$
  \displaystyle f(x)=\int_1^x\dfrac{\ln(t+1)}{t}\,dt
=-\int_x^1\dfrac{\ln(t+1)}{t}\,dt.
$$
因而
$$
  \displaystyle I=-\int_0^1\dfrac{1}{\sqrt{x}}
  \left(\int_x^1\dfrac{\ln(t+1)}{t}\,dt\right)dx.
$$
对积分区域
$$
  \displaystyle 0\le x\le t\le1
$$
交换积分次序，得
$$
  \begin{aligned}
  \displaystyle I
  &=-\int_0^1\dfrac{\ln(t+1)}{t}
  \left(\int_0^t\dfrac{dx}{\sqrt{x}}\right)dt\\
  &=-2\int_0^1\dfrac{\ln(t+1)}{\sqrt{t}}\,dt.
  \end{aligned}
$$
令 $\displaystyle t=u^2$，则 $\displaystyle dt=2u\,du$，从而
$$
  \displaystyle I=-4\int_0^1\ln(1+u^2)\,du.
$$
对该积分分部积分：
$$
  \begin{aligned}
  \displaystyle \int_0^1\ln(1+u^2)\,du
  &=\left[u\ln(1+u^2)\right]_0^1
-\int_0^1\dfrac{2u^2}{1+u^2}\,du\\
  &=\ln2-2\int_0^1\left(1-\dfrac{1}{1+u^2}\right)du\\
  &=\ln2-2+\dfrac{\pi}{2}.
  \end{aligned}
$$
因此
$$
  \begin{aligned}
  \displaystyle I
  &=-4\left(\ln2-2+\dfrac{\pi}{2}\right)\\
  &=8-2\pi-4\ln2.
  \end{aligned}
$$
本题的关键是先将变限积分改写为累次积分，再交换积分次序。

</details>

### （八）运用递推公式

1. **【2019-13-10 分】** 设
$$
\displaystyle a_n=\int_0^1x^n\sqrt{1-x^2}\,dx
\qquad(n=0,1,2,\cdots).
$$

（1）证明数列 $\displaystyle \{a_n\}$ 单调减少，且
$$
\displaystyle a_n=\dfrac{n-1}{n+2}a_{n-2}
\qquad(n=2,3,\cdots);
$$

（2）求
$$
\displaystyle \lim_{n\to\infty}\dfrac{a_n}{a_{n-1}}.
$$

<details>
<summary>查看答案与解析</summary>

**答案：** （1）$\displaystyle \{a_n\}$ 严格单调减少，且 $\displaystyle a_n=\dfrac{n-1}{n+2}a_{n-2}$；

（2）$\displaystyle 1$。

**解析：** **（1）证明单调性及递推公式。**

因为当 $\displaystyle 0<x<1$ 时，
$$
\displaystyle x^{n+1}<x^n,
$$
且 $\displaystyle \sqrt{1-x^2}>0$，所以
$$
\displaystyle x^{n+1}\sqrt{1-x^2}
<x^n\sqrt{1-x^2}.
$$
在 $\displaystyle [0,1]$ 上积分，得
$$
\displaystyle a_{n+1}<a_n.
$$
故数列 $\displaystyle \{a_n\}$ 严格单调减少。

当 $\displaystyle n\ge2$ 时，将
$$
\displaystyle a_n=\int_0^1x^{n-1}\cdot x\sqrt{1-x^2}\,dx
$$
作分部积分。取
$$
\displaystyle u=x^{n-1},
\qquad
\displaystyle dv=x\sqrt{1-x^2}\,dx,
$$
则
$$
\displaystyle du=(n-1)x^{n-2}\,dx,
\qquad
\displaystyle v=-\dfrac13(1-x^2)^{\frac32}.
$$
由于边界项为零，故
$$
\begin{aligned}
\displaystyle a_n
&=\dfrac{n-1}{3}\int_0^1x^{n-2}(1-x^2)^{\frac32}\,dx\\
&=\dfrac{n-1}{3}\int_0^1x^{n-2}(1-x^2)\sqrt{1-x^2}\,dx\\
&=\dfrac{n-1}{3}\left(
\int_0^1x^{n-2}\sqrt{1-x^2}\,dx
-\int_0^1x^n\sqrt{1-x^2}\,dx
\right)\\
&=\dfrac{n-1}{3}(a_{n-2}-a_n).
\end{aligned}
$$
整理得
$$
\displaystyle (n+2)a_n=(n-1)a_{n-2},
$$
即
$$
\displaystyle a_n=\dfrac{n-1}{n+2}a_{n-2}.
$$

**（2）求相邻两项之比的极限。**

由 $\displaystyle a_n>0$ 且数列严格减少，得
$$
\displaystyle 0<\dfrac{a_n}{a_{n-1}}<1.
$$
又由递推公式，
$$
\displaystyle \dfrac{a_n}{a_{n-2}}=\dfrac{n-1}{n+2}.
$$
将左端拆开：
$$
\displaystyle \dfrac{a_n}{a_{n-1}}\cdot
\dfrac{a_{n-1}}{a_{n-2}}
=\dfrac{n-1}{n+2}.
$$
因为 $\displaystyle \dfrac{a_{n-1}}{a_{n-2}}<1$，所以
$$
\displaystyle \dfrac{a_n}{a_{n-1}}
>\dfrac{n-1}{n+2}.
$$
于是
$$
\displaystyle \dfrac{n-1}{n+2}
<\dfrac{a_n}{a_{n-1}}<1.
$$
当 $\displaystyle n\to\infty$ 时，两端均趋于 $\displaystyle 1$，由夹逼定理，
$$
\displaystyle \lim_{n\to\infty}\dfrac{a_n}{a_{n-1}}=1.
$$

</details>
