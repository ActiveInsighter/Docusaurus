# AGENTS.md

## 项目背景

本项目是一个基于 Docusaurus + React + TypeScript 的个人知识文档站，用于发布学习笔记、技术文章、算法总结、开发经验和优质资料整理。

项目目标不是简单搭建一个静态博客，而是逐步建设成一个可长期维护、可扩展、适合学习和复习的知识平台。后续可能会加入动态交互能力，例如点赞、收藏、评论、阅读统计、AI 阅读助手、浮动工具栏、代码示例增强、Supabase 后端数据存储等功能。

项目使用Github action运行脚本抓取解析md文档，推送到相应文件夹，然后Cloudflare page自动部署

当前项目技术栈主要包括：

- Docusaurus
- React
- TypeScript
- MDX
- Markdown
- CSS Modules / 全局 CSS
- Github action
- Cloudfalre page
- 后续接入 Supabase后端数据库、AI SDK、搜索增强、评论系统等

请把本项目视为一个长期维护的文档站项目，而不是一次性的演示项目。

---

## 项目定位

本项目主要面向以下内容：

1. 软件工程、计算机基础、算法、数据结构、前端、后端、移动端开发等学习笔记。
2. 对优秀技术资料进行结构化整理和再表达。
3. 构建适合个人复习、检索和长期积累的知识库。
4. 使用 React / MDX 扩展文档交互能力。
5. 后续支持 AI 辅助阅读、总结、问答和知识导航。

请优先保证：

- 内容结构清晰；
- 组件可维护；
- 样式简洁统一；
- 文档适合长期扩展；
- 不要为了炫技引入过重依赖；
- 不要破坏 Docusaurus 官方推荐结构。

---

## 代码风格要求

### React / TypeScript

编写 React 组件时，请遵守以下原则：

1. 优先使用函数组件。
2. 优先使用 TypeScript 类型声明。
3. 组件职责要单一，不要把过多逻辑堆在一个文件里。
4. 状态逻辑和 UI 展示尽量分离。
5. 命名要清晰，避免无意义变量名。
6. 不要滥用全局变量。
7. 不要在 Docusaurus 配置文件中使用浏览器 API。
8. 涉及 Hooks、状态、事件、表单、组件结构时，优先采用 React 官方推荐写法。
9. 本项目已经接入context 7 mcp，需要Docusaurus，react,Supabase等相关文档时，优先使用mcp获取 最新文档。如果mcp没有的可以搜索官方网站或仓库。注意阅读文档要找准关键词，不要读很多无关的文档

---

## 文档组件设计方向


组件设计要求：

1. 外观简洁,美观。
2. 适配亮色和暗色模式。
3. 不影响 Docusaurus 默认布局。
4. 可在 MDX 中直接使用。
5. 不要为了单个页面写不可复用组件。
6. 样式应尽量统一到同一套设计语言中。

---

## 样式要求

样式设计应符合以下方向：

1. 简洁、清爽、适合阅读。
2. 优先保证正文可读性。
3. 适配移动端。
4. 支持暗色模式。
5. 避免过多动画。
6. 避免大面积高饱和颜色。
7. 代码块、引用块、提示块要有明确层次。
8. 不要破坏 Docusaurus 默认主题结构。
9. 自定义样式优先通过 CSS 变量控制。

---

## 功能开发原则

后续新增功能时，请遵守以下原则：

1. 先保证最小可用版本。
2. 不要一次性引入过多复杂功能。
3. 优先保持静态站点的稳定性和访问速度。
4. 动态能力尽量模块化，例如阅读统计、点赞、评论、AI 助手应拆分实现。
5. 新功能要考虑移动端体验。

---

## Git 与提交要求

提交代码时，请尽量使用清晰的提交信息。

推荐格式：

```txt
feat: add custom note component
fix: correct docusaurus config
docs: update algorithm notes
style: improve code block theme
refactor: split sidebar components
chore: update dependencies
````

不要提交：

* `node_modules/`
* 构建产物目录
* `.env`
* API Key
* 临时调试文件
* 大量无关格式化改动

---

## 依赖管理

添加新依赖前请先判断是否必要。

优先原则：

1. Docusaurus 已内置能力能解决的，不额外安装。
2. 简单 CSS 能解决的，不引入大型 UI 库。
3. 只为一个小功能服务的依赖要谨慎添加。
4. 新依赖要考虑维护活跃度、体积和兼容性。
5. 不要随意升级主版本依赖，除非明确需要。

---

## 构建与检查命令

常用命令：

```bash
npm install
npm run start
npm run build
```

提交前建议至少运行：

```bash
npm run build
```

如果构建失败，请优先修复：

1. TypeScript 类型错误；
2. Docusaurus 配置错误；
3. Markdown / MDX 语法错误；
4. 链接错误；
5. 路由和侧边栏错误。

---

## AI 助手工作方式

当 AI 助手参与本项目开发时，请遵守：

1. 先理解现有项目结构，再修改代码。
2. 不要盲目重构整个项目。
3. 优先做局部、小步、可验证修改。
4. 修改前说明要改哪些文件。
5. 修改后说明改了什么、为什么这样改、如何验证。
6. 遇到不确定的框架行为时，优先参考官方文档。
7. 不要编造不存在的 API。
8. 不要把 Docusaurus 当成普通 Vite React 项目处理。
9. 不要把 Next.js 的写法直接套用到 Docusaurus。
10. 不要在服务端配置文件里写浏览器端代码。

---


## 总体目标

请始终围绕以下目标工作：

> 构建一个结构清晰、阅读体验好、长期可维护、适合学习积累，并能逐步扩展动态交互和 AI 阅读能力的 Docusaurus 文档站。

任何修改都应该服务于这个目标。
